# CLAUDE.md Key-Files draft — DE-FR-PROPOSAL-IDENTITY-001 (2026-08-20)

Not applied. For PM/team-lead review and manual application to `.claude/CLAUDE.md`'s `## Key Files` table. Three amendments — append the bracketed text to the existing cell for each row (all three rows already exist in the table).

## `app.py` row — append

> **Frontrunner proposal-identity render prefetch (`DE-FR-PROPOSAL-IDENTITY-001`, 2026-08-20):** `ai_advisor_tab()`'s `frontrunner_proposals` prefetch loop additively resolves each row's incumbent display name (hash→name via one request-scoped `database.load_state()` call — not per row — honest hash fallback when the incumbent is absent from `bot_state`), normalizes a non-`dict` `metrics_json` to `{}` (AC-6 — closing a real latent crash this cycle found and fixed alongside the normalization: several stats-table cells used `is not none` on values that become Jinja `Undefined`, not literal `None`, once `metrics_json` degrades to `{}`, which then crashed the `format()` filter — fixed with `is defined and` guards on all 7 same-shaped template cells), and builds a bounded `overlay_tree_preview` (`_FR_OVERLAY_PREVIEW_MAX_CHARS=4000`) mirroring the existing `candidate_tree_preview` transform, without popping `overlay_tree` off `metrics_json` (the template still needs `replaced_node_id`/`overlay_summary` from the same dict). See `docs/generated/app.md`'s "Proposal-identity render prefetch" subsection and `DE-FR-PROPOSAL-IDENTITY-001` in `DECISIONS.md`.

## `advisors/frontrunner_{detector,builder,acceptance}.py`, `advisors/composer_draft_client.py` row — append

> **Proposal identity & provenance (`DE-FR-PROPOSAL-IDENTITY-001`, 2026-08-20):** `frontrunner_builder.py` gains three pure helpers replacing the old generic `"Frontrunner Candidate — <hash>"` Composer draft label with an honest identity — `build_proposal_symphony_name` (`"{incumbent} + FR overlay (full copy, #{id})"` / retrofit's `"Strategy Builder candidate for {incumbent} (from scratch, #{id})"`, bounded `MAX_PROPOSAL_NAME_CHARS=100`), `build_proposal_symphony_description` (full provenance + the operator-mandated "Full standalone copy — the original symphony is not modified." sentence, made UNCONDITIONAL for `frontrunner_builder` rows regardless of overlay-metrics presence — a deliberate deviation from the plan's original conditional wording, locked at implementation since the full-copy property is a fact about the splice itself, not about whether metrics were recorded; bounded `MAX_PROPOSAL_DESCRIPTION_CHARS=1000`), and `summarize_overlay` (a specific one-liner from the raw pre-compile/pre-graft overlay node, e.g. "relative-strength-index(SPY,10) gt 30 rotates into VIXY", degrading to the exact literal "compound condition overlay" on any underivable shape). `_run_build_for_symphony`'s persist site additively stamps `metrics_json["overlay_tree"]` (= the pre-graft node, adversarially guarded against the post-splice `spliced` object which embeds the entire incumbent core), `["replaced_node_id"]`, `["overlay_summary"]` — no schema migration. `approve_frontrunner_proposal` resolves the incumbent's display name and calls the new builders in place of the old hardcoded literals — the bare-hash format no longer occurs, including on legacy proposals (AC-6 honest degrade: "overlay not recorded for this proposal"). Zero change to detection, splice, gating, or acceptance math (AC-8); `composer_draft_client.save_symphony`'s payload changes only in the `name`/`description` string arguments. **Deferred, tracked, explicitly OUT of scope:** `frontrunner_acceptance.py`'s SIMPLIFY path (`MATERIAL_SIMPLIFICATION_MAX_RATIO=0.50` compared against whole-symphony node counts, effectively unreachable for a single-cascade splice) and `composer_draft_client.py`'s hardcoded `_DEFAULT_ASSET_CLASS="EQUITIES"` — both found this same day, both a future cycle's scope. See `docs/generated/advisors_frontrunner_builder.md`'s "Proposal Identity & Provenance" section and `DE-FR-PROPOSAL-IDENTITY-001` in `DECISIONS.md`.

## `templates/ai_advisor.html` row — append

> **Proposal-identity cards (`DE-FR-PROPOSAL-IDENTITY-001`, 2026-08-20):** `frontrunner_builder`-source pending-approval cards gain an identity line ("Full standalone copy of {incumbent} with one new frontrunner overlay"), an explainer line ("Approve creates a NEW undeployed Composer symphony — the incumbent is never modified."), an overlay-summary line (honest empty-state fallback "overlay not recorded for this proposal" for legacy rows), and a second collapsed-by-default overlay-preview `<details>` (`fr-overlay-preview`) distinct from the existing raw-candidate-preview `<details>`. `strategy_builder_retrofit`-source cards gain their own from-scratch identity line ("does not contain the incumbent's logic"). The raw-preview `<details>` `<summary>` label now branches by `proposal_source` — "Full spliced symphony (preview)" for `frontrunner_builder` cards, "Proposed symphony (preview)" for `strategy_builder_retrofit` cards (a retrofit candidate is from-scratch, so "spliced" would itself be dishonest). All 7 stats-table cells (Calmar/CAGR/MDD × incumbent/candidate, plus node-count-delta) gain `is defined and` guards against a Jinja `Undefined`-vs-`format()` crash exposed by the AC-6 `metrics_json={}` normalization. All new fields `| e`-escaped, no `| safe`. See `DE-FR-PROPOSAL-IDENTITY-001` in `DECISIONS.md`.
