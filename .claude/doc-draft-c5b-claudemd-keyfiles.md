# CLAUDE.md Key-Files Draft — C5b Overfitting-Cull Strengthening

**Generated by:** sb5b-doc (doc-writer)
**Date:** 2026-06-20
**Branch:** feat/strategy-builder-real
**HEAD:** ddcbb24

This file is a DRAFT for the PM's cycle-end batch. Do NOT apply directly — CLAUDE.md key-files rows for the full Strategy Builder feature ship as ONE coherent entry when the entire feature merges to origin.

---

## Proposed update: `advisors/backtest_gate_engine.py` row (ADD to key-files table)

```
| `advisors/backtest_gate_engine.py` | M2 fold-transform + batch BHY/Yekutieli FDR gate: `evaluate_candidate_batch(candidates, *, incumbent_oos_alpha, default_oos_alpha, spy_returns_fn) -> GatedBatch` — fold-transforms Composer backtest series into walk-forward folds (60/20/20 + PURGE_DAYS), BHY-corrects over the FULL batch (`n_effective=len(candidates)`), and calls `acceptance_gate.evaluate_acceptance_gate` per candidate; **C5b (DE-SB-CULL-001):** batch PBO wired (`math_engine.compute_pbo` over `BacktestCandidate.dated_returns` intersection → `pbo=_batch_pbo` in gate, fires when `K>=_PBO_MIN_CONFIGS=2` AND `>=_PBO_MIN_ALIGNED_DATES=8` intersection dates); SPY-OOS-fold baseline via `spy_returns_fn` seam (date-aligned to candidate span, same `_fold_transform_single`, `float("-inf")` on unavailable — no silent fallback to beats-zero); per-candidate `rejection_reason` with stage-order precedence: `pbo_veto` (Stage-1 dominant) → `below_spy_alpha` → `fdr_not_winner` → `None` (survivor); `BacktestCandidate.dated_returns: dict[str, float]` field (default `{}`); `CandidateGateResult.rejection_reason: str | None`; `SPY_BENCHMARK_TICKER="SPY"`; off-execution-path (never imported from `alpha_bot_execution.py`) |
```

## Proposed update: `advisors/strategy_builder_engine.py` row (UPDATE existing)

The existing row should append the C5b gate note. Recommended update to the existing row's description (add after the existing text):

```
**C5b (DE-SB-CULL-001):** `evaluate_candidate_batch` call site now wires `spy_returns_fn` (SPY-OOS-fold baseline) and `BacktestCandidate.dated_returns` (batch PBO); callers populating `dated_returns` and wiring `spy_returns_fn` receive autotuner-grade overfitting cull (PBO veto + SPY alpha gate + `rejection_reason`); callers omitting these degrade safely (PBO=None, alpha gate WITHHOLDS conservatively)
```

---

## Notes for the PM

- These rows are drafted only — do NOT commit to CLAUDE.md until the full Strategy Builder feature (all components: universe provider, build-plan generator, plan-tree compiler, weekly scheduler, C5b) is ready to merge in a single coherent PR.
- The `advisors/backtest_gate_engine.py` row is entirely NET-NEW to the key-files table (it currently appears only in the `advisors/` catch-all row without module-specific detail).
- Verify line numbers in `app.py` if the Strategy Builder route wiring adds a `spy_returns_fn` injection at the call site — that would belong in the `app.py` row, not here.
