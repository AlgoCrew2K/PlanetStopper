# CLAUDE.md Key-Files Draft — C5b + C4 Strategy Builder

**Generated by:** sb5b-doc (doc-writer)
**Date:** 2026-06-20
**Branch:** feat/strategy-builder-real
**HEAD:** a0aca12 (C4 body swap 5ae6c8c + scheduler 4867c1c + blast-radius a0aca12; C5b: gate-engine ddcbb24 + production-wiring 5d6e04a + edge-14 fix 4ccea92)

This file is a DRAFT for the PM's cycle-end batch. Do NOT apply directly — CLAUDE.md key-files rows for the full Strategy Builder feature ship as ONE coherent entry when the entire feature merges to origin.

---

## Proposed update: `advisors/backtest_gate_engine.py` row (ADD to key-files table)

```
| `advisors/backtest_gate_engine.py` | M2 fold-transform + batch BHY/Yekutieli FDR gate: `evaluate_candidate_batch(candidates, *, incumbent_oos_alpha, default_oos_alpha, spy_returns_fn) -> GatedBatch` — fold-transforms Composer backtest series into walk-forward folds (60/20/20 + PURGE_DAYS), BHY-corrects over the FULL batch (`n_effective=len(candidates)`), and calls `acceptance_gate.evaluate_acceptance_gate` per candidate; **C5b (DE-SB-CULL-001):** batch PBO wired (`math_engine.compute_pbo` over `BacktestCandidate.dated_returns` intersection → `pbo=_batch_pbo` in gate, fires when `K>=_PBO_MIN_CONFIGS=2` AND `>=_PBO_MIN_ALIGNED_DATES=8` intersection dates); SPY-OOS-fold baseline via `spy_returns_fn` seam (date-aligned to candidate span, same `_fold_transform_single`, `float("+inf")` sentinel on SPY-unavailable → `oos_alpha <= +inf` always-true for finite candidates → conservative WITHHOLD with `rejection_reason="below_spy_alpha"`, never silent fallback to beats-zero; edge-14 bug: original was `float("-inf")` which made clause always-false, fixed in 4ccea92); per-candidate `rejection_reason` with stage-order precedence: `pbo_veto` (Stage-1 dominant) → `below_spy_alpha` → `fdr_not_winner` → `None` (survivor); `BacktestCandidate.dated_returns: dict[str, float]` field (default `{}`); `CandidateGateResult.rejection_reason: str | None`; `SPY_BENCHMARK_TICKER="SPY"`; off-execution-path (never imported from `alpha_bot_execution.py`) |
```

## Proposed REPLACEMENT: `advisors/strategy_builder_engine.py` row (REPLACE existing)

The existing row in CLAUDE.md says "builds candidate trees from 7 templates (T1-T7)" and "T6/T7 sort-by-fn values are a PM-accepted unverified-grammar deviation" — both are stale after the C4 body swap. Replace the entire row description with:

```
| `advisors/strategy_builder_engine.py` | Phase-2 Strategy Builder proposal engine: **C4 body swap (DE-SB-C4-001):** `_generate_candidate_trees` drives real C1 (`universe_provider.get_tradeable_set()`, self-sourced when `universe=[]`) → C2 (`build_plan_generator.generate_build_plans`) → C3 (`plan_tree_compiler.compile_plan`) pipeline — no more T1-T7 template stamper; built-new candidates carry `template_id=plan.provenance` (never T1-T7); 4-value `Objective` enum (adds `volatility_mitigation`, Q1-A); `universe=[]` triggers C1 self-sourcing (Q2-A), route and scheduler both pass `[]`; AC-20: `propose_strategies` public signature unchanged; sole production callers: `app.py:3816` (on-demand route) + `advisors/strategy_builder_scheduler.py` (weekly). Community candidates admitted via `community_candidate_infos(community_result, *, max_candidates) -> list[CandidateInfo]` adapter (`template_id="community"`, provenance in `params`). Single-batch FDR gate (built-new + community together, anti-overfit invariant; screens never shrink gate input). **C5b (DE-SB-CULL-001, DE-SB-CULL-001-ADDENDUM-A/B):** `propose_strategies` internally sources SPY OOS series via `run_backtest` on a 100%-SPY tree (Step 2a), populates `BacktestCandidate.dated_returns` pct-scaled, passes `spy_returns_fn=lambda: _spy_returns_dict` to gate — callers do NOT wire SPY or `dated_returns`. SPY-unavailable → `float("+inf")` sentinel → conservative WITHHOLD (edge-14 fix 4ccea92). Gate enforces batch PBO veto + SPY-OOS-fold baseline + `rejection_reason` per candidate. `ScreenConfig` post-gate screens. Persists via `database.insert_advisor_observation`. Never-raises; off-execution-path; advisory-only. |
```

## Proposed NEW row: `advisors/strategy_builder_scheduler.py` (ADD to key-files table)

```
| `advisors/strategy_builder_scheduler.py` | Weekly Strategy Builder Scheduler (AC-18, DE-SB-C4-001): `run_weekly_build()` runs `propose_strategies` for all four `Objective` values once per ISO week; `_already_ran_this_week()` idempotency seam (checks `STRATEGY_BUILDER` rows in `advisor_observations`, patchable for tests); `MAX_ATTEMPTS=3` bounded retry per objective; D-1 never-raises (failed objective logged, next continues); off-execution-path; advisory-only; invoke: `python -m advisors.strategy_builder_scheduler` |
```

---

## Notes for the PM

- These rows are DRAFT only — do NOT commit to CLAUDE.md until the full Strategy Builder feature merges to origin/main via the reviewed PR gate.
- The `advisors/backtest_gate_engine.py` row is entirely NET-NEW to the key-files table.
- The `advisors/strategy_builder_engine.py` row must REPLACE the existing row (not append) — the existing row says "7 templates (T1-T7)" which is now wrong.
- The `advisors/strategy_builder_scheduler.py` row is entirely NET-NEW.
- `autotuner.py` does NOT call `propose_strategies` — the prior CLAUDE.md row text implied it via "called post-walk-forward from autotuner.py" in `advisors/` catch-all. Verify and correct that catch-all row description at merge time.
- Edge-14 (+inf vs -inf): the key-files row must cite the `+inf` sentinel; `-inf` was the bug (4ccea92).
- Verify line numbers in `app.py` for the route (`app.py:3816` as of a0aca12 — may drift before merge).
