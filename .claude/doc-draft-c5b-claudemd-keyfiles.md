# CLAUDE.md Key-Files Draft — C5b Overfitting-Cull Strengthening

**Generated by:** sb5b-doc (doc-writer)
**Date:** 2026-06-20
**Branch:** feat/strategy-builder-real
**HEAD:** f037c83 (gate-engine ddcbb24 + production-wiring 5d6e04a + edge-14 fix 4ccea92)

This file is a DRAFT for the PM's cycle-end batch. Do NOT apply directly — CLAUDE.md key-files rows for the full Strategy Builder feature ship as ONE coherent entry when the entire feature merges to origin.

---

## Proposed update: `advisors/backtest_gate_engine.py` row (ADD to key-files table)

```
| `advisors/backtest_gate_engine.py` | M2 fold-transform + batch BHY/Yekutieli FDR gate: `evaluate_candidate_batch(candidates, *, incumbent_oos_alpha, default_oos_alpha, spy_returns_fn) -> GatedBatch` — fold-transforms Composer backtest series into walk-forward folds (60/20/20 + PURGE_DAYS), BHY-corrects over the FULL batch (`n_effective=len(candidates)`), and calls `acceptance_gate.evaluate_acceptance_gate` per candidate; **C5b (DE-SB-CULL-001):** batch PBO wired (`math_engine.compute_pbo` over `BacktestCandidate.dated_returns` intersection → `pbo=_batch_pbo` in gate, fires when `K>=_PBO_MIN_CONFIGS=2` AND `>=_PBO_MIN_ALIGNED_DATES=8` intersection dates); SPY-OOS-fold baseline via `spy_returns_fn` seam (date-aligned to candidate span, same `_fold_transform_single`, `float("+inf")` sentinel on SPY-unavailable → `oos_alpha <= +inf` always-true for finite candidates → conservative WITHHOLD with `rejection_reason="below_spy_alpha"`, never silent fallback to beats-zero; edge-14 bug: original was `float("-inf")` which made clause always-false, fixed in 4ccea92); per-candidate `rejection_reason` with stage-order precedence: `pbo_veto` (Stage-1 dominant) → `below_spy_alpha` → `fdr_not_winner` → `None` (survivor); `BacktestCandidate.dated_returns: dict[str, float]` field (default `{}`); `CandidateGateResult.rejection_reason: str | None`; `SPY_BENCHMARK_TICKER="SPY"`; off-execution-path (never imported from `alpha_bot_execution.py`) |
```

## Proposed update: `advisors/strategy_builder_engine.py` row (UPDATE existing)

The existing row should append the C5b gate note and the production-wiring details. Recommended addition to the existing row's description:

```
**C5b (DE-SB-CULL-001, DE-SB-CULL-001-ADDENDUM-A):** `propose_strategies` now internally sources SPY OOS series via `run_backtest` on a 100%-SPY tree before the candidate loop (Step 2a), populates `BacktestCandidate.dated_returns` from `result.daily_returns` with date keys preserved pct-scaled (×100), and passes `spy_returns_fn=lambda: _spy_returns_dict` to `evaluate_candidate_batch` — callers do NOT need to wire SPY or `dated_returns` (AC-20: public signature unchanged). The gate now enforces PBO veto (batch, `math_engine.compute_pbo`) + real SPY-OOS-fold baseline + `rejection_reason` per candidate. SPY-unavailable (`_spy_returns_dict={}`) → conservative WITHHOLD via `_SPY_UNAVAILABLE_DEFAULT_OOS_ALPHA=float("+inf")` sentinel (edge-14 fix, 4ccea92).
```

---

## Notes for the PM

- These rows are DRAFT only — do NOT commit to CLAUDE.md until the full Strategy Builder feature (all components: universe provider, build-plan generator, plan-tree compiler, weekly scheduler, C5b) is ready to merge in a single coherent PR.
- The `advisors/backtest_gate_engine.py` row is entirely NET-NEW to the key-files table (it currently appears only in the `advisors/` catch-all row without module-specific detail).
- Edge-14 (+inf vs -inf): the key-files row must cite the `+inf` sentinel; `-inf` was the bug, `+inf` is the fix (4ccea92). Both appear in the git history — the CLAUDE.md row documents the final shipped state.
- Verify line numbers in `app.py` if the Strategy Builder route wiring adds a `spy_returns_fn` injection at the call site — that would belong in the `app.py` row, not here. (Current: `propose_strategies` handles this internally; the app.py `ai_advisor_strategy_builder_run` route does NOT pass `spy_returns_fn` and does not need to.)
