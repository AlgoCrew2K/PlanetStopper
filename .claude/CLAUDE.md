# Planet Stopper — Project-Local CLAUDE.md
# EXTENDS ~/.claude/CLAUDE.md — do NOT duplicate global content.

## Project Identity
- **Name:** Planet Stopper
- **Purpose:** Institutional-grade algorithmic risk engine; monitors live Composer.trade portfolios; executes intelligent trailing stops ("Guard Alpha")
- **Stack:** Python 3 / Flask monolithic daemon; SQLite (state DB + optimization DB); Optuna; Composer.trade + Alpaca + Discord integrations
- **Workflow:** Hard fork — never re-syncing upstream; full autonomy within global guidelines
- **PM autonomy (operator directive 2026-06-11):** never pause to ask the operator mid-backlog — proceed phase to phase until the backlog is exhausted or blocked on an operator-only input (credentials, scope change). Document assumptions as `[PM-ASSUMED]` in contract docs instead of asking.
- **PM status integrity (operator directive 2026-06-12):** NEVER report team/agent status without validating it first in the same turn — check the process table (`pgrep`), file mtimes, and `git log` before every claim. Inference is never reported as fact; if something is unverified, it is labeled unverified. A missing completion notification is NOT evidence of progress.
- **PM compaction resilience (operator directive):** Compaction can drop "what's mine" from context. Keep `.claude/PM-ACTIVE-WORK.md` as a durable on-disk ledger of owned teams (+ their `leadSessionId`), branches, in-flight agents, and open inputs; update it on every create/teardown. On EVERY wake/resume/compaction-recovery, READ that ledger FIRST. Before calling any team/branch/process "rogue/foreign", run the ownership check: a team whose `leadSessionId` == this session id is YOURS; one claude.exe in the process table means no concurrent external driver. Never raise a rogue-session alarm without failing that check first.
- **Built capabilities (DONE — this is NOT a TODO list; verify built-state before ever calling any of these "open"):** historical analysis = **History tab** (`templates/history.html` + `GET /api/history`, app.py:2450); live-vs-Planet-Stopper comparison stats = **Performance tab** (`templates/performance.html` + `GET /api/performance` app.py:2485 + `/api/performance/symphonies` 2583 — bot-vs-held Chart.js cumulative + quantstats deltas Sharpe/Sortino/MDD/Calmar/CAGR, if-held vs Bot vs Delta, per-symphony toggle); charts/graphs in the Flask dashboard = the History strip chart + the Performance Chart.js (client-side; QuickChart is Discord-only); pytest + GitHub Actions test harness = `.github/workflows/tests.yml` (ruff + full pytest, GREEN since #68). Guard-Alpha comparison math lives in `analytics.py` (epoch-aware). Only OPTIONAL operator-gated enrichments remain (NOT open work): on-dashboard $-saved time-series, per-symphony $-saved historical rollup, durable multi-year history (`shadow_history` is retention-pruned).
- **Advisor reasoning-fidelity audit trail:** `docs/audit/ADVISOR-INTENT-AUDIT.md` (2026-07-13) is the canonical source for "what actually reasons vs what's deterministic" across the AI Advisor suite — six findings (F2/F3/F4/F5/F6/F7/F8) on attribution honesty, statistical-gate substance, and gate transparency, closed by `DE-ADVISOR-R1-001` (see `DECISIONS.md`). `docs/audit/CLOSEOUT-VERDICT.md` (2026-06-17) is a superseded, narrower structural-wiring pass — see its banner.

## Key Files (quick reference for workers)
| File | Role |
|------|------|
| `app.py` | Flask dashboard + minute-by-minute scheduler; `_DISMISS_EXECUTOR` (background dismiss thread) + `_FLUSH_STATE_LOCK` (flush serialization); spawns `alpha_bot_execution.py` at :00; CSRF infrastructure (`_validate_csrf`, `_csrf_before_request`); `_SETTINGS_WRITE_ALLOWLIST` gates the two guarded write paths; AI Advisor routes — unified SPA at `GET /ai-advisor` renders all 6 tabs; all 5 GET sub-routes 302-redirect to `/ai-advisor`; **RF-1:** `ai_advisor_tab()` pre-humanizes `per_lens_digest` summaries in-place via `advisors.prism_render.humanize_lens_summary` (app.py:2966–2984, R1) and stamps `_preview_text` on each non-MARKET_PRISM observation via `humanize_obs_preview` (app.py:2892–2902, R2) before `render_template` — no new context key added; POST action routes unchanged; **dashboard auth gate (DE-AUTH-001, PR #55):** `_auth_before_request` hook registered BEFORE CSRF — unauth HTML→302 `/login`, `/api`+XHR→401 JSON, authed session passes; `_resolve_dashboard_credential()` returns `DASHBOARD_PASSWORD_HASH` (werkzeug-format, preferred) or `DASHBOARD_PASSWORD`; constant-time compare (`hmac.compare_digest` plaintext / `check_password_hash` for `pbkdf2:`/`scrypt:`/`bcrypt:` hashes); `login()` (GET renders `login.html` w/ CSRF token, AC-13 authed→dashboard; POST: throttle→compare→`session.clear()`+`authenticated`→redirect) + `logout()`; **fail-closed** on missing credential OR `SECRET_KEY`/`FLASK_SECRET_KEY` (loud log, 503 API / redirect HTML, never serve-open); `_AUTH_EXEMPT_ENDPOINTS` frozenset (login/logout/static/get_csrf_token/health); proxy-aware throttle (`_AUTH_FAILED_ATTEMPTS`, `_AUTH_MAX_ATTEMPTS=10`/`_AUTH_LOCKOUT_SECONDS=300`, XFF-keyed only when `TRUST_PROXY` set else `remote_addr`); cookie flags `SESSION_COOKIE_HTTPONLY`/`SAMESITE=Lax`/`SECURE` (env-gated); **`_validate_csrf` dual-channel** — `X-CSRF-Token` header (all content types) OR `csrf_token` form field (form-encoded content-types ONLY — JSON/XHR never touch `request.form`, preserving the CSRF-before-body-size guard ordering; dc6b8c7). Disabled in tests via `_disable_auth_for_tests` autouse fixture. **Deploy gate (T1):** needs TLS + `SESSION_COOKIE_SECURE=true` + `DASHBOARD_PASSWORD`/`SECRET_KEY` before public go-live; **Guard-Alpha $-saved panel (PR #56, 2026-06-19):** read-only `GET /api/guard-alpha-summary` (`app.py:2172` `guard_alpha_summary()`) aggregates Σ`saved_dollars` + guard-event count from `post_mortem_*.json` in `analytics._POST_MORTEMS_DIR` (advisory-only, NOT in `_SETTINGS_WRITE_ALLOWLIST`, no `LIVE_EXECUTION`, auth via the global hook, malformed-file-safe, honest empty-state) → renders the `dollar-saved-panel` headline ("$X saved across N exits — snapshot-time basis") below the hero on the dashboard via `fetchGuardAlphaSummary()` in `static/index.js` (401-guarded, writes `dollar-saved-headline`, kept distinct from the windowed `guard-alpha-headline`); **DISABLE_DAEMON_LENS_PIPELINE gate (DE-PRISM-GATE-001, PR #59):** `_run_lens_pipeline()` (`app.py:686`) returns early when `os.environ.get("DISABLE_DAEMON_LENS_PIPELINE")` is truthy (empty/absent = no skip) — silences the daemon's 03:00 lens-pipeline slot so `prism_scheduler.py` (Option B council) is the SOLE nightly `MARKET_PRISM` producer on the droplet; **safe transition order:** set the flag in droplet `.env` + restart daemon BEFORE registering the council systemd timer (no idempotency guard between the two paths — concurrent active = two `MARKET_PRISM` rows/night); **Strategy Builder on-demand route (C5, 2026-06-20, commit 1d5dd48):** `POST /ai-advisor/strategy-builder/run` (`app.py:3759`) rewired to the real dual-mode builder — lazy-imports `build_plan_generator.load_atlas_candidates(objective)` (CC-2) for objective-matched Atlas injection; `load_atlas_candidates` is D-1 + bill-protected (`force_refresh=False`); calls `propose_strategies(community_candidates=...)` with both built-new + atlas-suggested in ONE FDR batch (AC-21); response JSON carries `template_id: "built-new"|"atlas-suggested"` provenance on every survivor/rejected candidate (AC-13); `run.error` branch sanitized to static `"strategy-builder-error"` token (AC-23 — never echoes `run.error` verbatim which is set from `str(exc)`); CSRF-protected, advisory-only, not in `_SETTINGS_WRITE_ALLOWLIST`, no `LIVE_EXECUTION`; **`data_as_of` derivation (AC-7, 2026-06-23):** both `_compute_portfolio_strip` (app.py:1283) and `GET /api/state` top-level (app.py:2123) read `last_successful_cycle_at` from the **top level** of `bot_state`/`state_data` (engine writes it there: `alpha_bot_execution.py:948/1092/1878`); never from per-symphony sub-dicts — prior code iterated `values()` and always fell through to `datetime.now()`; **DE-PRISM-SOURCES-001 (Overview citation overlay):** `ai_advisor_tab()` additively fetches the `MARKET_PRISM_SOURCES` row via `database.get_latest_market_prism_sources_for_run(run_id)` (where `run_id` comes from the MARKET_PRISM row's `raw_response`) and merges `article_corpus = [{url, title, published}]` into `per_lens_digest` entries before template render — honest empty-state (no article links) when the SOURCES row is absent or run_id mismatches; template unchanged; **DE-ADVISOR-LATENCY (2026-06-29):** `ai_advisor_suggest` response JSON gains read-only `lens_data_as_of` + `lens_data_stale` (the AC-3 "market context as of <ts>" staleness stamp surfaced in `static/ai_advisor.js`); **DE-EOD-BASIS-001 (EOD account-basis unification, 2026-07-02):** both the live `_compute_portfolio_strip()` and the frozen/EOD `get_state()` closed-branch recompute now resolve today-change, cumulative-return, and `account_value` through an independent two-tier stale-cache fallback (Tier-1 `_account_totals_last_good` plain-dict snapshot + `_account_totals_last_success_at` ET timestamp, survives `_account_totals_cache.mark_stale()`; Tier-2 honest `portfolio_strip["basis"]="value_weighted"` floor) instead of a single combined TC-gates-everything check — each field (TC/CR/account_value) is wrapped through `analytics.get_portfolio_today_change_account_basis`/`get_portfolio_cumulative_return_account_basis` independently so a cold field can never collaterally null or mislabel a warm sibling field; `_ACCOUNT_TOTALS_HTTP_TIMEOUT_S` promotes the Composer fetch timeout to a named constant; display/aggregation only, engine (`alpha_bot_execution.py`/`math_engine.py`) byte-unchanged; **DE-PRISM-NUMERIC-VERIFY-001 (2026-07-02):** `ai_advisor_tab()` additively fetches the MARKET_PRISM_VERIFICATION row by run_id and attaches per-check fact-check badges (`market_prism_verification` context key) to the Overview render — honest empty-state, never mutates the MARKET_PRISM row; **candidate-alert cycle (2026-07-12):** new `GET /api/candidate-alert` (read-only, `{new_valid_count, last_run}`, each accessor call independently try/except'd so a DB failure degrades only that field, always 200) + CSRF-protected `POST /api/candidate-alert/mark-viewed` (zero required args — marker value is server-computed only, so a caller can't set it to an arbitrary observation id; advisory-only, NOT in `_SETTINGS_WRITE_ALLOWLIST`) back the always-visible header candidate-alert indicator (`templates/_chrome.html` + `static/chrome.js`); badges NEW weekly-suggestion survivors where `verdict == "ADOPT_CANDIDATE"` (stricter than the feature plan's `!= "REJECT_VETO_FAILED"` wording — also correctly excludes `KEEP_INCUMBENT`); see DE-CANDIDATE-ALERT-001; **advisor-outage-degrade AC-4/AC-5 (`DE-SB-DEGRADE-001`, commit `14adb451`, 2026-07-13):** `POST /ai-advisor/strategy-builder/run` response JSON gains `backtest_unavailable`/`backtest_unavailable_count`/`backtest_unavailable_notice` — read directly off `ProposalRun.backtest_unavailable`/`.backtest_unavailable_count` (same pattern as the existing `run.error`/`run.error_category` reads), deliberately NOT recomputed from `run.candidates` (which excludes exactly the outage population under a sustained Composer outage — Step 2's own per-candidate backtest call hits the same failing seam first); honest empty-state (all three fields absent/false/None on both error branches); **R2-1 reasoning-context + provenance (`DE-ADVISOR-R2-1-001`, 2026-07-13):** same route additionally calls `ai_advisor.build_reasoning_context(symphony_id, objective, composer_symphony_id=<resolved hash>)` for symphony-scoped runs only (NAME→hash resolution mirrors the asset-swap route's existing pattern; from-scratch runs never call it — zero extra I/O, AC-8 byte-preservation) and threads the result into `propose_strategies(reasoning_context=, reasoning_manifest=)`; response JSON gains a `provenance` object (`{generation_model, mode, evidence_injected, run_id}`) on the success path only — absent (never `null`) on both error branches; route-boundary `isinstance(provenance, dict)` guard defends `jsonify()` against a bare `MagicMock` `ProposalRun` test stand-in (`getattr(...,default)` alone doesn't fire — `MagicMock` auto-vivifies missing attributes instead of raising); **R2-2 Logic Changes reuse (`DE-ADVISOR-R2-2-001`, 2026-07-14):** `POST /ai-advisor/logic-changes/evaluate` (`ai_advisor_logic_changes_evaluate()`) similarly calls `ai_advisor.build_reasoning_context` for symphony-scoped runs and threads the result into `propose_operator_logic_change(reasoning_context=, reasoning_manifest=)`; mints a route-level default `provenance` present on EVERY return path including every early-exit error branch — deliberately STRICTER than the SB route above, which only carries `provenance` on success; success-path provenance reads the engine's own value via the same `isinstance(dict)` MagicMock-safety guard, falling back to the route default (never `None`) rather than `None`; **R2-3 Asset Swaps reuse (`DE-ADVISOR-R2-3-001`, 2026-07-14):** `POST /ai-advisor/asset-swaps/evaluate` (`ai_advisor_asset_swaps_evaluate()`) gains a genuine three-outcome ticket contract — both `from_ticker`/`to_ticker` supplied → byte-preserved EXPLICIT-PAIR mode; neither supplied → objective-only REASONED mode (array-shaped `survivors_detail`/`rejected_detail`, mirrors the logic-changes route); exactly one supplied → an honest 200 error, checked before hash resolution (team-lead ruling: never silently reinterpreted). Calls `ai_advisor.build_reasoning_context` unconditionally for both modes and mints a route-level default `provenance` present on EVERY return path — adopts R2-2's stricter shape, not R2-1's success-only shape; success-path provenance reads the engine's own value via the same `isinstance(dict)` guard, falling back to the route default; **Frontrunner Builder wave-2 routes (2026-07-11, `eb1b612`; applied to this table 2026-07-17):** `GET /ai-advisor/frontrunner-builder` → 302 redirect (no standalone page, mirrors the strategy-builder stub); `POST /ai-advisor/frontrunner-builder/run` — async 202 dispatch to a dedicated `_FRONTRUNNER_BUILD_EXECUTOR` (single-worker, `atexit`-registered, deliberately separate from `_DISMISS_EXECUTOR`), fail-fast on missing `ANTHROPIC_API_KEY` before submit; `POST /ai-advisor/proposal/approve` — generic/source-agnostic (`proposal_id`-keyed), the ONLY route in the app that can reach `composer_draft_client.save_symphony` (exclusively via `advisors.frontrunner_builder.approve_frontrunner_proposal`); `POST /ai-advisor/proposal/reject` — status-only DB write. `ai_advisor_tab()` additively prefetches `database.get_pending_frontrunner_proposals()`, bounding `candidate_tree` to a 4000-char JSON preview before template render. See `docs/generated/app.md` §"Frontrunner Builder Routes" and `DE-FRONTRUNNER-002`. **Frontrunner-signals cycle addition, built then removed (2026-07-16, `ae5fe22d` build / `f563f16c`+`6715d654` removal):** `ai_advisor_tab()` briefly additively read `advisors.frontrunner_signals.get_latest_classifications()`/`get_latest_run_marker(symphony_id=<id>)` to render AC-7's "Live Signal Classification" subsection on the Frontrunner Builder tab. Removed the same day per the operator's de-productization ruling — `ai_advisor_tab()` now carries ZERO diff related to this surface (do NOT re-add it). See `DE-FR-SIGNALS-001`. **Math Remediation R0 (2026-07-17, `DE-MATH-R0-001` AC-3..8/AC-8b, closes DE-MATH-AUDIT-001 MA-6/MA-7/MAPERF-03/04/05/06/13):** `/api/performance` `scope=symphony` now sources `analytics.get_symphony_bot_and_held_daily_returns` (new, per-symphony `shadow_history` per-day rows) instead of the post-mortem trigger-array path, which annualized a selection-biased K-trigger event sample as K consecutive trading days (AC-3); both day-1 fallback tiers in that route are now scope-gated to `scope=='aggregate'` so a zero-trigger symphony renders an honest empty state instead of the whole portfolio's metrics under its name (AC-4); `/api/hero-chart/<window>`, `/api/strip/<window>`, and `/api/performance`'s `ytd` token now share ONE calendar-window cutoff (`analytics._window_cutoff_date` via new `_slice_series_by_window_cutoff`) — pre-fix the same picker click windowed the hero chart by trading days and the strip by calendar days, a ~40% mismatch at 1y (AC-5); `/api/history/<days>`'s Detail column has one semantic (guard-alpha pp) across both its post-mortem and intraday-fallback sources via a new `_guard_alpha_detail` helper, closing a silent time-of-day-dependent swap between two different quantities under the same rendered value (AC-6); `/api/strip` and `/api/guard-alpha-summary`'s intraday fallbacks use an explicit `guard_alpha is None` check (never falsy — a legitimate `$0.00` window no longer gets clobbered) and are day-filtered to the current ET trading day, closing a cross-day-incoherent subtraction (AC-8). `analytics.compute_windowed_symphony_guard_alpha` now returns `None` (not a fabricated `0.0`) for its pre-existing `<2`-in-window-rows conservatism floor — a mid-cycle escalation (AC-8b) the PM ruled in-scope under the AC-G2-6 no-self-regression precedent, since AC-8's `is None` check would otherwise have silently regressed real thin-window divergence to a flat zero. See `DE-MATH-R0-001` for the full record; **Post-mortem data-integrity guard (F-008, DE-POSTMORTEM-INTEGRITY-001, 2026-07-20):** `guard_alpha_summary()` routes each trigger entry through `analytics.is_valid_post_mortem_entry` before summing — an entry lacking a recognized `if_held_source` provenance stamp is excluded (new `excluded_invalid_count` response field), closing the gap where 2 historical contaminated post-mortem days (real 2026-06-22 capture + a 2026-07-09-style day) rode unguarded into the live $-saved headline. |
| `alpha_bot_execution.py` | Core engine — per-cycle execution; wired to canonical THEORY spec bundle via `get_or_create_phase1_theory_bundle_id`; **App-Math Audit findings (DE-MATH-AUDIT-001, 2026-07-17):** MA-4 (HIGH) — FIXED in Math Remediation R3-b (`DE-MATH-R3B-001`, 2026-07-18): the live trailing-stop disarm band (previously inline at `:1339-1347`, inverted vs. H1-corrected MC semantics — disarmed on `prob>30 AND ret>0`, i.e. WORSENING not recovered) is now delegated to `math_engine.compute_arm_disarm_decision` (`~main() :1383-1411`), disarming only on genuine recovery (`prob` back below `TAKE_PROFIT_MC_PCT`) sustained for `DISARM_CONFIRM_TICKS=3` consecutive ticks; `current_return` no longer plays any role in the disarm decision. The identical inverted disarm, independently duplicated in the autotuner replay (`autotuner.py:_replay_exit_tick`), is fixed via the SAME shared seam — production⇄replay parity is now structural. See `docs/generated/math_engine.md` + `DE-MATH-R3B-001`. MA-11 (HIGH) — **FIXED in Math Remediation R3-c (`DE-MATH-R3C-001`, 2026-07-18):** `MAX_SQUEEZE_FLOOR` (previously a dead operator/advisor-suggestible knob, one whole-repo occurrence, its own assignment at `:1236`, never consumed) is now wired as a post-squeeze lower clamp on the trailing-stop DISTANCE — `math_engine.compute_active_trailing_stop` gains an optional `squeeze_floor` param (`:463`), scoped inside the squeeze branch with a no-widening invariant (`active = max(active*sq, min(squeeze_floor, pre_squeeze_active))`, `:518-519`); production (`:1476`) and the autotuner replay (`_replay_squeeze_floor_default()`, `autotuner.py:76`+`:1276`) share the identical default source. See `docs/generated/math_engine.md` + `DE-MATH-R3C-001`; **Math Remediation R1 (DE-MATH-R1-001, 2026-07-17):** confirmed ZERO diff for the replay-fidelity fix (MA-1/MA-10/F5) — `current_holdings` construction sites (`:888-894`/`:1557-1560`) remain ticker+allocation-only by explicit ruling (the live per-tick lpc fix lives entirely in `synthetic_history.build_replay_day`, never this file); `EXECUTION_START_TIME` (`:124`) is now also read by the autotuner replay via this same module attribute (AC-5/F6), never a replay-local mirror |
| `math_engine.py` | Risk math: volatility scaling, sqrt-time squeeze (`1-sqrt(1-t)`), parabolic ratchet, MC gating, VWAP, breakeven, exit confirm, regime-match guard; CRRA-EU utility (`compute_crra_eu_objective`); CVaR diagnostic (`compute_portfolio_cvar`, `CVaRAssessment`); PBO (`compute_pbo`); 6-layer exit decision (`resolve_trigger_priority`); trailing-stop arm/disarm decision (`compute_arm_disarm_decision`, frozen `DISARM_CONFIRM_TICKS=3`, Math Remediation R3-b `DE-MATH-R3B-001`, 2026-07-18 — fixes MA-4's inverted disarm, shared by production + autotuner replay); post-squeeze stop-distance floor (`compute_active_trailing_stop`'s optional `squeeze_floor` param, no-widening clamp, Math Remediation R3-c `DE-MATH-R3C-001`, 2026-07-18 — fixes MA-11's dead knob, shared by production + autotuner replay via `alpha_bot_execution.MAX_SQUEEZE_FLOOR`) |
| `autotuner.py` | Optuna walk-forward (250 trading days, 500 trials per symphony); CPCV folds (`_generate_cpcv_folds`, N=6, k=2, 15 splits, 5 paths); CRRA-EU `_haircut_select` objective with `compute_n_effective` additive accounting; PHASE-3 PBO gate (`compute_pbo`, top-20 pre-BHY, PBO>0.5 veto); **Math Remediation R2 (DE-MATH-R2-001, 2026-07-17) — MA-2/MA-5/MA-9 fixed:** CPCV trial scoring moved from 5-path aggregation to direct split-level scoring of the 15 real `_generate_cpcv_folds` splits (`_aggregate_cpcv_paths` DELETED — canonical backtest paths are mathematically forced identical without per-fold refit, no aggregation fix could have worked); the adoption cascade's OOS validation reads `history_validation_full` on a genuine train-only-purged holdout with a real (not hardcoded) `purge_integrity_ok`; frozen-eval now produces a real CRRA-EU metric under the production objective; R1's undated-path regime-tick residual is closed (`run_simulation`/`_collect_sim_returns` now call `_replay_resolve_regime_exit_ticks` same as the dated path) — see `docs/generated/autotuner.md`; `compute_crra_eu_tstat` lives here (not math_engine); NN1 spec-freeze enforcement at entry; invokes Overfitting Conscience + Spec Critic + Divergence Explainer post-walk-forward; OC reads `prior_runs` via `advisor_ro_query`; `save_autotune_run` returns the inserted row id; **DE-AUTOTUNE-OOM (2026-06-29):** `_AUTOTUNE_REPLAY_N_JOBS = 1` module-level constant (source-commented w/ the AC-1 empirical profile); `run_autotuner` passes `n_jobs=_AUTOTUNE_REPLAY_N_JOBS` to `generate_synthetic_history` (the single autotune call site) — bounds the intraday-replay parallelism to prevent OOM on the 2-core / `MemoryMax=3.0 GiB` droplet (the unset `ALPHABOT_MAX_JOBS`→-1 all-core fan-out OOM-killed the weekly run → empty `autotune_runs`; n_jobs=1 peak 2.03 GiB, 990 MB headroom; AC-3 chunking ruled OUT). Other `generate_synthetic_history` callers untouched; MemoryMax NOT raised; **Math Remediation R1 (DE-MATH-R1-001, MA-10/F5/F6, 2026-07-17):** `_replay_exit_tick` gains fail-open arming on MC-absent ticks (mirrors `alpha_bot_execution.py:1324-1326`) + an explicit `exit_confirm_ticks` param; new `_replay_resolve_regime_exit_ticks` recomputes regime-conditional exit ticks (2/5/3) fresh per replay day via `regime_classifier.classify_regime()`, no lookahead — `database.get_cached_regime_label` is FORBIDDEN in replay code (single-row live cache would inject today's label into every historical day); new `_replay_in_action_phase` gates the exit loop on `EXECUTION_START_TIME`; regime-tick wiring reaches only the dated `_collect_sim_returns_dated` CSCV/PBO selection path — the undated Optuna search-score path (`run_simulation`/`_collect_sim_returns`) was an accepted, `xfail`-tripwired R2-deferred residual — CLOSED by R2, see above; **Math Remediation R3-a (DE-MATH-R3A-001, 2026-07-18):** pre-retune checklist item (a) — walk-forward objective-variance coverage extended to all 6 tuned dims via new `scripts/objective_variance_probe.py`; source-derived enumeration + AST suggest-call drift-guard confirms `OPTUNA_SEARCH_SPACE_KEYS` matches the real `trial.suggest_*` calls; config-robust across `EXECUTION_START_TIME`∈{09:30,9:35}; zero diff to autotuner.py itself; **Math Remediation R3-b (`DE-MATH-R3B-001`, 2026-07-18):** `_replay_exit_tick`'s arm/disarm block delegated to the shared `math_engine.compute_arm_disarm_decision` seam (structural parity with production), fixing MA-4's inverted disarm (previously duplicated inline); new `disarm_confirm_count` key in `_fresh_replay_state`; **Math Remediation R3-c (`DE-MATH-R3C-001`, 2026-07-18):** `_replay_exit_tick`'s `compute_active_trailing_stop` call passes `squeeze_floor=p.get("MAX_SQUEEZE_FLOOR", _replay_squeeze_floor_default())`; new `_replay_squeeze_floor_default()` helper (`:76`) reads `alpha_bot_execution.MAX_SQUEEZE_FLOOR` live, matching production's own fallback source — fixes MA-11's dead knob with structural production⇄replay parity; **DE-AUTOTUNE-REPORTING-001 (F-004, 2026-07-19):** all three `run_autotuner` graceful-abort return sites (history-shortfall exception, empty synthetic history, `<2` WFA days) now uniformly return `{"aborted": True, "reason": ...}` — no abort path returns bare `None` (consumed by `reporting.py`'s aborted-vs-no-change embed branch + the `alpha_bot_execution.py:1166-1192` abort-discriminator) |
| `database.py` | State DB: 32 numbered migration SQL files (001–032); `_MIGRATION_FILES` wires 29 entries (004–032, 021 precedes 020 — intentional, see ARCH-002 inline comment); public accessors include Phase-1 originals (`record_cvar_diagnostic`, `read_cvar_diagnostic_for_symphony`, `get_or_create_phase1_theory_bundle_id`, `insert_dof_ledger_row`, `query_wall_breach_tripwire`) plus post-Sprint-3 additions: `insert_advisor_observation` (accepts `symphony_id`), `get_advisor_observations_for_symphony`, `get_symphony_live_mode`, `set_symphony_live_mode`, `save_regime_label`, `get_cached_regime_label`, `get_or_create_phase15_m3_bundle_id`; `compute_composition_hash` promoted here from deleted `port_selector.py`; **Prism Phase 1 (migration 032)**: `insert_prism_audit_entry(run_id, agent_role, phase, content) -> int`, `get_prism_audit_for_run(run_id) -> list[dict]` — append-only deliberation trail keyed by `run_id`; **pytest sentinel guard**: `_db_file()` raises `RuntimeError` when `"pytest" in sys.modules` AND basename == `alphabot_state.db` — tests MUST set `DB_PATH` via `tests/conftest.py`; **DE-PRISM-SOURCES-001**: `get_latest_market_prism_sources_for_run(run_id: str) -> dict | None` — exact `json_extract(raw_response,'$.run_id')=?` match, returns None on mismatch (stale-citation-bleed guard); `update_advisor_observation_raw_response` DELETED (v1 rejection — append-only invariant); `get_latest_market_prism_sources()` DELETED (zero prod callers); `MARKET_PRISM_SOURCES` deliberately NOT in app.py `_ADVISOR_ROLES` (keeps it out of Overview observations loop app.py:3604 + `_preview_text` stamp app.py:3633); **DE-ADVISOR-LATENCY (2026-06-29):** `get_latest_market_lens_cache() -> dict | None` — latest `MARKET_LENS_CACHE` role row (`ORDER BY id DESC LIMIT 1`), deserialized; `get_ro_connection()`; D-1 never-raises; returns None before the first nightly run (the advisor cache-serve read accessor; `MARKET_LENS_CACHE` likewise NOT in `_ADVISOR_ROLES`); **DE-PRISM-NUMERIC-VERIFY-001:** `get_latest_market_prism_verification_for_run(run_id)` RO accessor, structural mirror of the SOURCES accessor, no migration; **candidate-alert cycle (2026-07-12, migration 033):** single-row `candidate_alert_state` (viewed-marker) + 5 accessors — `get_candidate_alert_viewed_marker`, `set_candidate_alert_viewed_marker` (monotonic UPSERT via `MAX(existing, new)`, AC-5 idempotency), `mark_candidate_alert_viewed` (zero-arg, computes `MAX(id)` over the three weekly-suggestion roles itself), `get_candidate_alert_new_valid_count`, `get_candidate_alert_last_run` (calendar-date-grouped batch aggregate — no run_id column exists for these three roles) — back the header candidate-alert indicator; see DE-CANDIDATE-ALERT-001 |
| `reporting.py` | Discord webhooks + QuickChart embeds; two-stage daily post-mortem producer: **Stage 1 (15:54 ET)** freezes guard-alpha math — `saved_dollars = current_value * (exit_return - current_return) / 100` per triggered symphony, sourcing `current_return` (if-held trajectory) from the engine's `shadow_history` (`alpha_bot_execution.py:1189-1203`); **Stage 2 (16:00 ET, post-rebalance)** injects tomorrow's target holdings from Composer; output `post_mortems/post_mortem_YYYY-MM-DD.json`, consumed by `/api/guard-alpha-summary` (app.py), History + Performance tabs (analytics.py), and the Discord embed. **DE-GUARD-ALPHA-SAVED-001 (2026-06-22):** the old basket reconstruction (`triggered_basket_snapshot + live_prices`) was removed — basket prices were frozen at exit level → post-trigger move ≈ 0 → understated saved ($2.96 vs ~$207 true); fix = source if-held from `sym["current_return"]` (`reporting.py:57`, commit 0d0d4f3). **Field-semantics gotcha:** `shadow_history.current_return` = the LIVE if-held trajectory (use this); `shadow_history.shadow_return` = the FROZEN exit value (NOT if-held). Engine exit-decision math is unaffected (reporting/aggregation only); **DE-AUTOTUNE-REPORTING-001 (F-015, 2026-07-19):** `send_eod_discord_post`'s per-symphony changes loop shape-guards each entry (`isinstance(vals, dict) and "old"/"new" in vals`) before indexing — a non-delta sibling entry (e.g. `autotuner.py`'s `eval_window_days`) is skipped, not fatal; the `except` tuple is widened to include `KeyError`; the aborted-vs-no-change distinction (F-004) renders as two visibly different Discord embeds instead of one shared "No optimization changes" message; **Post-mortem read-time consumer guard (F-008, DE-POSTMORTEM-INTEGRITY-001, 2026-07-20; reporting.py itself carries zero diff):** the `if_held_source` field this module stamps (three tiers: `shadow_history` / `shadow_history_post_cutoff` / `bot_state_fallback`) is now enforced at READ TIME by the shared `analytics.is_valid_post_mortem_entry` helper across all 3 live post-mortem consumers (`app.guard_alpha_summary`, `analytics.load_post_mortem_history`, `analytics.get_history_summary`) — an entry missing this field or carrying an unrecognized value is excluded from every live aggregate rather than silently summed. |
| `synthetic_history.py` | 250-day live Alpaca historical fetcher (parallel + file cache); feeds autotuner replay; **DE-AUTOTUNE-OOM (2026-06-29):** `generate_synthetic_history` gains keyword-only `n_jobs=None` param; `effective_n_jobs = n_jobs if n_jobs is not None else _resolve_replay_n_jobs()` — the autotune path passes `n_jobs=_AUTOTUNE_REPLAY_N_JOBS` (=1), all OTHER callers inherit env behavior (`ALPHABOT_MAX_JOBS`, default -1); `n_jobs=1` = joblib sequential backend (no fork) → peak 2.03 GiB on the 2-core / 3.0 GiB-cgroup droplet (AC-1 empirical). `_resolve_replay_n_jobs()` body unchanged. Guard `tests/synthetic_history/test_bounded_replay_parallelism.py::test_no_bare_all_core_parallel_call` (positive Name\|Call allowlist — catches a literal/`-1` UnaryOp); **Math Remediation R1 (DE-MATH-R1-001 AC-1/MA-1, CRITICAL, 2026-07-17):** `build_replay_day` stamps a real per-tick `last_percent_change` into a fresh non-mutating `priced_holdings` list before every `run_monte_carlo` call — fixes the day-constant degenerate MC baseline that made Trailing-Stop/Take-Profit exits unreachable in the walk-forward replay (previously `run_monte_carlo` received holdings with no lpc at all, silently excluded by `math_engine.py:1162-1166`'s unchanged exclusion contract); the fix is confined ENTIRELY to this module — `alpha_bot_execution.py`/`math_engine.py` carry zero diff, structurally enforced by an adversarial source-scan test; **Math Remediation R3-a (DE-MATH-R3A-001, 2026-07-18):** pre-retune checklist item (b) — new `scripts/mc_band_edge_stability_probe.py` probed `_MC_REPLAY_SIMULATION_PATHS`'s arm-decision stability near the arm-band boundary; value UNCHANGED (300) — near-boundary instability proximity-driven, not path-reducible for the committed scenario; see `docs/generated/mc-band-edge-stability.md` |
| `scripts/objective_variance_probe.py` | R3-a pre-retune checklist deliverable (a): walk-forward objective-variance probe, all 6 tuned dims; source-derived enumeration + AST `trial.suggest_*` drift-guard + `force_inert` non-vacuity control; config-robust across `EXECUTION_START_TIME`∈{09:30,9:35}; off-execution-path, D-1, consumed only by its test file |
| `scripts/mc_band_edge_stability_probe.py` | R3-a pre-retune checklist deliverable (b): 300-path MC band-edge arm-decision stability probe + committed bump-vs-accept recommendation artifact (`docs/generated/mc-band-edge-stability.{md,json}`); off-execution-path, D-1, never imports `alpha_bot_execution` |
| `acceptance_gate.py` | Reusable overfitting acceptance gate — used by autotuner and AI Advisor proposal suite |
| `ai_advisor.py` | Claude-backed config advisor: `assemble_advisor_context` (accepts `composer_symphony_id` + `autotune_run` params; `autotune_run` is HONORED — pass a pre-fetched row to skip the internal DB fetch, or use the default `_SENTINEL` to fetch internally), `build_assessment_from_context` (per-symphony informative empty-state), `request_suggestions` (D-1 fully honored: all error paths return `type(exc).__name__` only), C2 safety gates (4 gates on the accept path: allowlist/Gate-1 blocks, risk-direction/Gate-2 logs-only non-blocking, OOS-revalidation/Gate-3 blocks, locked-var/Gate-4 blocks); 7-item suggestible allowlist (6 Optuna search-space keys + MAX_SQUEEZE_FLOOR); **fundamentals lens portfolio fan-out (DE-FUND-001)**: `_build_fundamentals_section(ticker=None)` (the 03:00 nightly Prism + `assemble_advisor_context` path) fans out SEC EDGAR companyfacts over live `logic_holdings` ∪ `_FUNDAMENTALS_PROXY_UNIVERSE` (8 large-cap COMPANY tickers — NOT ETFs, which lack companyfacts) via the extracted `_fetch_fundamentals_for_ticker(ticker)` helper; single-ticker path byte-preserved; per-ticker honest degradation; bounded SEC fan-out; no invented composite ratios; **vintage-correct selection (DE-FUND-002)**: `_SEC_KEY_CONCEPTS` restructured to `(label, candidate_tags)` tuples — Revenues unions `RevenueFromContractWithCustomerExcludingAssessedTax`/`SalesRevenueNet`/`Revenues` so migrated XBRL tags are not frozen (Mode A); selection loop sorts unioned candidate-tag entries by `(end desc, filed desc)` to pick the latest reporting period, not the oldest comparative entry from a 10-K bundle (Mode B); `key_facts` output keys stable (`ai_advisor.py:361-374`, `ai_advisor.py:1011-1073`); **DE-ADVISOR-LATENCY (cache-serve, 2026-06-29):** `assemble_advisor_context` now SERVES the 5 market-wide lenses from the nightly `MARKET_LENS_CACHE` row (`database.get_latest_market_lens_cache`) instead of 17-29 live per-click fetches — the 5 `_build_*_section` builders are NOT called on the serve path (the operator's 6-min hang fix, AC-1); freshness window `_LENS_CACHE_MAX_AGE_HOURS=36`; a valid ≥5-key bundle is ALWAYS served — missing/unparseable `captured_at` → `lens_data_stale=True`+`lens_data_as_of=None` (loud-stale, never discarded; plan edge case); cold-start (no row) → per-lens `available=False, reason="lens_cache_unavailable"` (never the 5-builder fan-out, AC-5); new producer `persist_market_lens_cache(sections)` (append-only `MARKET_LENS_CACHE` advisor_observation, D-1, called from `prism_scheduler._patch_provenance`); `lens_data_as_of`/`lens_data_stale` threaded to suggest JSON→JS `#advisor-lens-as-of`; backward-compat aliases (`context["technicals"]` etc.) preserved; `build_assessment_from_context` empty-state reworded (AC-8); **R2-1 reasoning-context assembler (`DE-ADVISOR-R2-1-001`, 2026-07-13):** new `build_reasoning_context(symphony_id, objective, *, composer_symphony_id=None) -> tuple[str, dict]` assembles a bounded operator-context block for Strategy Builder generation (real tree via `symphony_schema.render_rules_text` + live Optuna stats + the 5 lens blocks via the existing `assemble_advisor_context` cache-serve path) paired with an honest per-source manifest (`tree`/`stats`: present|absent; the 5 lenses: available|stale|absent — the honesty artifact this function exists to produce); zero I/O + D-1 on a falsy `symphony_id`; `_MAX_TREE_RENDER_CHARS=6000` is a dedicated INPUT-context bound (cost/context-window) — NOT coupled to `build_plan_generator.MAX_OUTPUT_TOKENS`, a separate module's OUTPUT-side ceiling; shared cross-cutting enabler; **R2-2/R2-3 reuse confirmed:** called verbatim by both `app.py`'s Logic Changes (`DE-ADVISOR-R2-2-001`) and Asset Swaps (`DE-ADVISOR-R2-3-001`, 2026-07-14) evaluate routes — this file carries ZERO diff across either cycle, pure reuse; the R2 program (3 sub-cycles: SB/Logic Changes/Asset Swaps) is now complete |
| `advisors/symphony_schema.py` | Phase-1 Strategy Builder schema layer: constructs synthetic Composer `raw_value` trees + inspects arbitrary ones (built or real `/score`). Pure stdlib. Never-raising read-only `validate_tree` (HARD errors) / `lint_tree` (soft warnings — size/depth caps and unknown indicator fns are lint-only per handoff amendments 1–7) / `extract_tickers` / deterministic `render_rules_text`; 16 constructors (original 10: `make_root`, `make_asset`, `make_weight_*`, `make_inverse_vol`, `make_group`, `make_filter`, `make_indicator`, `make_condition`, `make_if`; 6 compound AC-4..AC-8: `make_condition_operand`, `make_constant_rhs`, `make_binary_condition`, `make_binary_compound_condition`, `make_compound_condition`, `make_if_compound`) emitting fresh uuid4 ids + deep-copied children; **`make_root` emits `"description": ""`** + **`make_inverse_vol` emits `"window-days": 30`** (both live-required by Composer `/backtest` API — HTTP 400 / HTTP 422 without them; added 2026-06-18); **constructor count stays at 16** — `make_weight_marketcap` NOT added because Composer deprecated market-cap weighting (HTTP 422 `node-type-not-supported` / "Market cap weighting is no longer supported", 2026-06-20; evidence: `tests/fixtures/strategy_builder/wt_marketcap_deprecated_envelope.json`; see DE-SB-MARKETCAP-DEPRECATED). Vocabulary pinned by `feature-plans/strategy-builder-composer-grammar.md`. Iterative traversal (depth-230 fixtures safe). |
| `advisors/universe_provider.py` | Tradeable Universe Provider (Strategy Builder Component 1): `get_tradeable_set() -> frozenset[str]` — fetches the full active US-equity tradeable set from `GET https://paper-api.alpaca.markets/v2/assets?status=active&asset_class=us_equity` (APCA-API-KEY-ID / APCA-API-SECRET-KEY; **paper host only** — live host `api.alpaca.markets` 401s with paper keys; `ALPACA_TRADING_BASE_URL` constant distinct from data-host `ALPACA_BASE_URL`); filters to `tradable==True` AND `exchange` in {NASDAQ, NYSE, ARCA, BATS, AMEX} (~12,748 symbols); weekly-cached via `atlas_cache.cached_pull` (TTL 7 days, `force_refresh` escape hatch); membership lookup only — NO ranking, NO dollar-volume, NO top-N palette; each weekly snapshot persisted to the warehouse third-DB pattern (`lens_warehouse` style — separate append-only `alphabot_warehouse.db`, `_strip_secrets` applied, no cross-DB join); D-1 / never-raises (source failure → `available=False, reason=type(exc).__name__`); off-execution-path; advisory-only; Composer `/backtest` is the final tradeability arbiter |
| `advisors/build_plan_generator.py` | Build-Plan Generator (accessor-driven generation model — currently Fable via `model_config.get_advisor_suggestion_model()`, default `claude-fable-5`, overridable via `ADVISOR_SUGGESTION_MODEL`) (Strategy Builder Component 2+2b): SDK structured tool-use generation of `N_PLANS_PER_OBJECTIVE=12` diverse objective-shaped build-plans in the build-plan DSL (NOT raw Composer JSON — a constraint-typed 1:1 pre-image of the `symphony_schema` constructor API); `MAX_OUTPUT_TOKENS=16384` (raised from 4096 — DE-SB-GEN-TRUNCATION; empirical worst-case 5,015 tokens/12-plan run; 16384 ≈ 3.3x headroom); `MAX_GENERATION_ATTEMPTS=3` bounded retry fires on `stop_reason=="max_tokens"`, degrades D-1 on exhaustion; `_build_generation_prompt` seam (5 layers: DSL grammar + flat if.condition + compound if_compound union + THREE compiler-verified examples + `_OBJECTIVE_SIGNATURES`); full Composer condition grammar (binary/binary_compound/compound) generation-reachable and schema-constrained; tightened `_EMIT_BUILD_PLANS_TOOL` (kind/scheme/condition-union enum-constrained); AC-8 via `plan_matches_objective()`; AC-9 membership prune + zero-ticker guard + `_collect_condition_tickers` canonical-flat binary-leaf fix; `admit_community_candidates(community_result, objective, *, max_candidates) -> list[CandidateInfo]` — objective-matched Atlas ranking (cut_drawdown=lowest drawdown, vol_mit=lowest vol, lift_ra=best sharpe, diversify=low Jaccard overlap), AC-12 kept-last for missing-stat docs; `load_atlas_candidates(objective, *, max_candidates) -> list[CandidateInfo]` — convenience wrapper (`load_community_strategies(force_refresh=False)` then `admit_community_candidates`); **sole canonical community-admission path** for both the route (`app.py:3807`) and the weekly scheduler (`strategy_builder_scheduler.py:134`); provenance tags: built-new/atlas-suggested; D-1 never-raises; off-execution-path; advisory-only; **Context-blindness caveat (advisor-intent audit, DE-ADVISOR-R1-001) — CLOSED for the symphony-scoped path by R2-1 (`DE-ADVISOR-R2-1-001`, 2026-07-13):** at R1 time the generation prompt carried none of the operator's live symphony tree, portfolio composition, backtest statistics, or lens data — only objective name, DSL grammar, worked examples, and a 20-ticker universe sample. R2-1 closes this for symphony-scoped runs via an additive `reasoning_context: str | None = None` keyword on `_build_generation_prompt`/`generate_build_plans` (a `## OPERATOR CONTEXT` section, only when truthy); the from-scratch path is BYTE-IDENTICAL when `reasoning_context` is omitted/`None` (AC-8, pinned against a golden fixture captured pre-R2-1) — it remains context-blind by design. |
| `advisors/plan_tree_compiler.py` | Plan-to-Tree Compiler (Strategy Builder Component 3): `compile_plan(plan: dict) -> CompileResult` — deterministically translates each build-plan DSL node into a valid Composer `raw_value` tree via `symphony_schema` constructors; full grammar coverage (nested `make_group`, equal/specified/inverse-vol weighting, filters, simple + compound conditions via `make_if`/`make_if_compound`/`make_compound_condition`/`make_binary_compound_condition`); `_has_market_cap(plan)` pre-check drops any plan with `scheme=="market_cap"` before compilation (`CompileResult(reason="market_cap_scheme_deprecated")`) — Composer retired market-cap weighting (HTTP 422, `DE-SB-MARKETCAP-DEPRECATED`); every compiled tree gated by `symphony_schema.validate_tree` before return; tradeability repair loop: parses `composer_backtest_client` error envelope to distinguish grammar-422 (AC-15 path) from tradeability-400 (AC-16 ticker-prune+retry path); bounded repair attempts (`MAX_REPAIR_ATTEMPTS` named constant); unrepairable plans return `CompileResult(tree=None, reason=...)` — run continues; D-1 / never-raises; off-execution-path; advisory-only; `symphony_schema.KNOWN_STEPS` constructor count stays at 16 (no `wt-marketcap`) |
| `advisors/strategy_builder_engine.py` | Phase-2 Strategy Builder proposal engine (real C1→C2→C3 pipeline since C4): `propose_strategies(objective, universe, screen_config, live_returns, symphony_id, *, incumbent_oas_alpha, default_oas_alpha, community_candidates) -> ProposalRun` — generates candidate trees via the real C1 (`universe_provider.get_tradeable_set()`, self-sourced when `universe=[]`) → C2 (`build_plan_generator.generate_build_plans`) → C3 (`plan_tree_compiler.compile_plan`) pipeline, backtests via `composer_backtest_client` (1 req/s), gates via Harvey-Liu BHY FDR + C5b PBO veto + real SPY-OOS baseline, applies `ScreenConfig` post-gate screens, persists survivors as advisory observations (`is_advisory_only=1`); `universe=[]` → C1 self-source from `universe_provider.get_tradeable_set()` (route + scheduler both pass `[]`); `community_candidates=` kwarg accepts `list[CandidateInfo]` from `build_plan_generator.load_atlas_candidates(objective)` — pooled with built-new in ONE FDR batch (AC-21); **C5b cull (DE-SB-CULL-001):** `math_engine.compute_pbo` wired per candidate (veto `pbo>0.5`; `pbo=None` passes); real SPY-OOS baseline sourced by `run_backtest` on 100%-SPY tree once per run (`_SPY_UNAVAILABLE_DEFAULT_OOS_ALPHA=float("+inf")` → conservative WITHHOLD on failure, edge-14 fix — original `-inf` made clause always-false); `rejection_reason` field: `None`/`"pbo_veto"`/`"below_spy_alpha"`/`"fdr_not_winner"`; `community_candidate_infos` adapter DELETED (70 lines, zero production callers after C5 route rewire); provenance tags: built-new/atlas-suggested (never "T1"–"T7", never "community"); 4-value `Objective` enum (diversify/cut_drawdown/lift_risk_adjusted/volatility_mitigation); `MAX_CANDIDATES_PER_RUN=30`; `MAX_COMMUNITY_CANDIDATES_PER_RUN=20`; sole production callers: `app.py:3813` (route) + `strategy_builder_scheduler.py` (weekly); `autotuner.py` does NOT call `propose_strategies`; off-execution-path; advisory-only; never raises; **Advisor-remediation-r1 additions (`DE-ADVISOR-R1-001`, 2026-07-13):** `rejection_reason` gains a 4th value, `oos_inferior_to_incumbent` (AC-7b, `3fa2e7f8`) — a genuine BHY/FDR winner that still loses to the incumbent on the OOS-superiority precondition, previously mislabeled `fdr_not_winner`; full precedence now `pbo_veto` > `below_spy_alpha` > `fdr_not_winner` (explicit) > `oos_inferior_to_incumbent` > `None` (survivor). `ProposalRun` gains `error_category: str | None` (AC-11, `59e86f9a`) alongside the existing sanitized `error` string. The route-level response (not this module) additionally surfaces `built_new_count`/`atlas_count` provenance rollup + a `mode_notice` degraded-run indicator (AC-11) and `screens_skipped`/`screens_skipped_reason` (AC-12, `_generate_candidate_trees` now calls `plan_tree_compiler.compile_plan(plan, backtest_fn=run_backtest)`, reviving the previously-dead tradeability-repair loop); **R2-1 provenance contract (`DE-ADVISOR-R2-1-001`, 2026-07-13):** `ProposalRun` gains `run_id: str` + `provenance: dict | None` (`{generation_model, mode, evidence_injected, run_id}`), minted UNCONDITIONALLY before the try block so every return path — including the earliest error returns — carries it, never fabricated/never `None` by omission; `propose_strategies` gains keyword-only `reasoning_context`/`reasoning_manifest`/`run_id` (all additive, default `None`, threaded to `_generate_candidate_trees`/`build_plan_generator.generate_build_plans`); cross-cutting contract R2-2/R2-3 reuse. **Accepted transitive import (r2-review Finding-2):** the new module-level `import ai_advisor` transitively imports `alpha_bot_execution` via `ai_advisor.py`'s own `import symphony_logic` → `symphony_logic.py`'s `from alpha_bot_execution import COMPOSER_BASE_URL, get_composer_headers` — ACCEPTED (import-only/no cycle, pre-existing on `ai_advisor`'s own side not new, Architecture Constraint #1 intact since this module is itself lazy-imported at the route layer); known follow-up: strengthen the SB import-guard test to a full-source-text transitive scan (see `tests/ai_advisor/test_correlation_diagnostic_guards.py` precedent) |
| `advisors/strategy_builder_scheduler.py` | Weekly Strategy Builder Scheduler (Strategy Builder Component 4, AC-18): `run_weekly_build()` — runs the real dual-mode builder (built-new + atlas-suggested) for all four objectives once per ISO week; same-ISO-week idempotency (`_already_ran_this_week()` seam, patchable); per-objective `build_plan_generator.load_atlas_candidates(objective)` injection INSIDE the per-objective loop (each objective ranked by its own stat — cut_drawdown=lowest drawdown, vol_mit=lowest vol, etc.); inner try/except degrades to `community_candidates=[]` on Atlas error (built-new always runs); `MAX_ATTEMPTS=3` bounded retry per objective (failed objective logged, run continues to next); all observations keyed to `symphony_id=""`; D-1 / never-raises; off-execution-path; advisory-only; invoke: `python -m advisors.strategy_builder_scheduler`; **A.1 dedup fix (advisor-rewire 2026-07-12, DE-ADVISOR-REWIRE-A): `_already_ran_this_week()` now calls `database.get_advisor_observations_for_role("STRATEGY_BUILDER", ...)` — was a mis-kwarg'd `get_advisor_observations_for_symphony(...)` that raised a swallowed TypeError so dedup always returned False; now driven by `weekly_suggestions_scheduler.run_weekly_suggestions()`** |
| `advisors/weekly_suggestions_scheduler.py` | Weekly Suggestions Orchestrator + per-symphony loop callers (advisor-rewire cycle, 2026-07-12, shipped `a4a73f47`): `run_weekly_suggestions()` calls, each D-1 isolated, `strategy_builder_scheduler.run_weekly_build()` → `run_weekly_asset_swap_suggestions()` (first production caller of `asset_swap_engine.suggest_swaps`) → `run_weekly_logic_change_suggestions()` (first production caller of `logic_change_engine.suggest_logic_changes`). **Two live-E2E-caught fixes closed the chain to real data:** `_fetch_lens_scores()` sources real per-ticker lens evidence from the nightly `MARKET_LENS_CACHE` (via `asset_swap_engine.extract_lens_scores`, which parses `technicals.payload["momentum"]` → `_squash_momentum_to_unit_interval` = `0.5+0.5*tanh(m/_MOMENTUM_SQUASH_SCALE=0.10)`; the prior parser read a fabricated `ticker_scores` key no producer emits — DE-LENS-SCORE-SHAPE-001); `_build_base_candidate_pool()` sources the candidate pool from `lens_technicals._PROXY_UNIVERSE` ∪ live `logic_holdings` with per-symphony own-holdings exclusion — NOT `universe_provider.get_tradeable_set()` (dropped entirely; an alphabetical `sorted(tradeable)[:15]` sample never overlapped the lens-scored tickers → `lens_evidence` stayed `{}` — DE-LENS-CANDIDATE-POOL-001). Both fixes were invisible to 441 green mocked tests and caught only by a live E2E against a copy of the real droplet DB. `python -m advisors.weekly_suggestions_scheduler`. Deployed via systemd oneshot + weekly timer (`docs/DEPLOYMENT.md` Step 9, Mondays 04:00 ET, `.env`-only metered-key path); **droplet timer registration is a separate operator-gated deploy step (activates weekly job + API spend).** Off-execution-path; advisory-only; never raises |
| `advisors/backtest_gate_engine.py` | M2 fold-transform + batch BHY/Yekutieli FDR gate: `evaluate_candidate_batch(candidates, *, incumbent_oos_alpha, default_oas_alpha, spy_returns_fn) -> GatedBatch` — fold-transforms Composer backtest series into walk-forward folds (60/20/20 + PURGE_DAYS), BHY-corrects over the FULL batch (`n_effective=len(candidates)`), and calls `acceptance_gate.evaluate_acceptance_gate` per candidate; **C5b (DE-SB-CULL-001):** batch PBO wired (`math_engine.compute_pbo` over `BacktestCandidate.dated_returns` intersection → `pbo=_batch_pbo` in gate, fires when `K>=_PBO_MIN_CONFIGS=2` AND `>=_PBO_MIN_ALIGNED_DATES=8` intersection dates); SPY-OOS-fold baseline via `spy_returns_fn` seam (date-aligned to candidate span, same `_fold_transform_single`, `float("+inf")` sentinel on SPY-unavailable → `oos_alpha <= +inf` always-true for finite candidates → conservative WITHHOLD with `rejection_reason="below_spy_alpha"`, never silent fallback to beats-zero; edge-14 fix: original was `float("-inf")` which made clause always-false, fixed in 4ccea92); per-candidate `rejection_reason` with stage-order precedence: `pbo_veto` (Stage-1 dominant) → `below_spy_alpha` → `fdr_not_winner` (explicit BHY-winner-is-None) → `oos_inferior_to_incumbent` (a genuine FDR winner that still loses the OOS-superiority precondition, AC-7b) → `None` (survivor); `BacktestCandidate.dated_returns: dict[str, float]` field (default `{}`); `CandidateGateResult.rejection_reason: str | None`; `SPY_BENCHMARK_TICKER="SPY"`; off-execution-path (never imported from `alpha_bot_execution.py`); **Panel-tie neutralization (AC-17, 2026-07-13):** all three advisor engines (this module's only production consumers) constructed `BacktestCandidate` with structurally empty `candidate_params`/`incumbent_params` on every real call path, making the panel-comparison clause a constant 0.5-vs-0.75 that failed unconditionally — `ADOPT_CANDIDATE` was mathematically unreachable regardless of candidate quality. Fixed: when both sides are structurally empty, `cand_stability` ties `inc_stability` exactly, so adoption rests entirely on the OOS-superiority precondition plus the three hard vetoes. `acceptance_gate.py` and `autotuner.py` received zero diff — contained to this module. See [advisors_backtest_gate_engine.md](../generated/advisors_backtest_gate_engine.md)'s "Panel-Tie Neutralization" section for the full proof. **Math Remediation R0 (2026-07-17, `DE-MATH-R0-001` AC-1/AC-2, closes DE-MATH-AUDIT-001 MA-3 CRITICAL + M2):** the C5b batch-PBO boundary (`evaluate_candidate_batch`) now converts every candidate's percent-scale `dated_returns` to decimal (`pct / RETURN_PCT_TO_FRACTION`, a fresh dict, never mutating the caller's dict) before calling `math_engine.compute_pbo`, which requires decimal returns — pre-fix, a single -2% day saturated `compute_crra_eu_objective`'s wealth floor, corrupting the IS-best/OOS ranking the overfitting veto depends on and flipping veto decisions arbitrarily w.r.t. an accidental unit scale (golden fixture: `tests/fixtures/math/pbo_unit_boundary_flip.json`, PBO 0.1714 (pass) at percent scale vs 0.8714 (veto) at decimal scale, identical data). `_BATCH_PBO_GAMMA` is now `float(database.PHASE1_THEORY_GAMMA)` (2.0), replacing a comment that cited a nonexistent `autotuner.py: GAMMA = 1.0` constant. **math-r2 (2026-07-17, `DE-MATH-R2-001` AC-5 rider):** `_fold_transform_single.oos_alpha` now compounds genuinely (`(prod(1+r/100)-1)*100`) instead of naive-summing — exact under the old log-return convention, an approximation under the new simple-return one that could flip close accept/reject rankings. |
| `advisors/composer_backtest_client.py` | M2 inline-backtest client: `run_backtest`/`submit_backtest` POST to Composer's `/api/v0.1/backtest`, bounded exponential-backoff retry, never raises. **math-r2 (`DE-MATH-R2-001` AC-5):** `_extract_returns` emits simple returns (`curr_val/prev_val-1`) instead of log returns — the producer-side fix for the audit's M1 finding, also closing an untraced second instance of the same category error in the PBO/CRRA-EU gate path. First `docs/generated/` coverage this cycle — see `docs/generated/advisors_composer_backtest_client.md`. |
| `advisors/community_strats.py` | Community symphonies sourced from **algo-db.com** (read via its `captplanet.strategies` MongoDB Atlas collection, weekly-cached). Community strategies loader: `load_community_strategies(*, limit, min_oos_sharpe, client, force_refresh) -> dict`; routes the `captplanet.strategies` Atlas read through `atlas_cache.cached_pull` (weekly TTL, operator bill-protection directive); **`_PROJECTION` includes `"_id": 0`** (suppresses BSON ObjectId — without it `atlas_cache`'s `json.dumps` raised `TypeError` so the cache row was silently never written, DE-ATLAS-CACHE-001 Bug 1); **`_fetch_fn` applies server-side sort+limit** (`_MAX_FETCH_DOCS=500`, `oos_metrics.sharpe` DESC, `allow_disk_use=True` — the unbounded 11k-doc fetch OOM-killed the 4 GB droplet, Bug 2; `atlas_cache.cached_pull` upsert hardened with `json.dumps(..., default=str)` defense-in-depth); **live Atlas fetch is wall-clock-bounded** by `_bounded_fetch_fn` (`ThreadPoolExecutor(max_workers=1)`, `fut.result(timeout=_ATLAS_FETCH_TIMEOUT_S)`, `shutdown(wait=False)`) — a `mongodb+srv://` SRV/DNS hang degrades to `available=False, reason="AtlasFetchTimeout"` within `_ATLAS_FETCH_TIMEOUT_S=12.0 s` instead of blocking indefinitely (`serverSelectionTimeoutMS` cannot bound SRV DNS resolution — see DE-CS-002); validates via `symphony_schema.validate_tree`; deduplicates by tree-structural SHA-256 hash (`_strip_ids` → `json.dumps(sort_keys=True)` → sha256 — NOT `database.compute_composition_hash`, which hashes `list[str]` symphony IDs); sharpe-filters keeping no-sharpe docs; returns `{available, candidates, stats, source}`; never-raising D-1 contract; consumed via `build_plan_generator.load_atlas_candidates` (objective-matched ranking + `admit_community_candidates` adapter) — callers do NOT use `load_community_strategies` directly; honest template-only degradation when Atlas unavailable (DE-HF1-001, DE-CS-002) |
| `advisors/lens_pipeline.py` | Off-hours lens pipeline (Cycle 4): `run_pipeline(*, dry_run=False) -> dict`; 4-pass (per-lens exception isolation → citation validation via `build_citation` → configurable-model synthesis (ADVISOR_SYNTHESIS_MODEL env var, default claude-opus-4-8) → MARKET_PRISM persistence); always writes one `advisor_role="MARKET_PRISM"` `advisor_observations` row per non-dry_run call even when all lenses unavailable (`verdict="limited-inputs"`); D-1 contract (`type(exc).__name__` only); CC-2 boundary (lazy import inside `app.py` daemon thread, never module-level); scheduled daily at 03:00 via `run_scheduler()`. `database.get_latest_market_prism_summary()` returns the most recent row for Cycle-5 Overview tab. |
| `advisors/lens_technicals.py` | Technicals lens producer: `_fetch_technicals(universe) -> dict`; calls `_get_bars` (test seam) → `synthetic_history.fetch_bars` for 270-day Alpaca daily bar history; computes MA posture (50/200-day SMA per ticker), breadth (fraction above SMA50, excludes tickers with insufficient history), 20-day momentum (Jegadeesh & Titman 1993); bounded retry (`_MAX_ATTEMPTS=3`); honest-availability / D-1 / CC-3 (never raises); off-execution-path (CC-2 lazy import); wired into `ai_advisor._build_technicals_section()` (`ai_advisor.py:439-504`); universe = live `logic_holdings` (from `database.load_state()`) unioned with a named `_PROXY_UNIVERSE` floor (SPY/QQQ/IWM/EFA/AGG/GLD + XLF/XLE/XLV/XLI) — the proxy is an unconditional floor so the 03:00 nightly Prism always gets a real breadth universe even when flat (DE-TECH-002) |
| `advisors/lens_gdelt.py` | GDELT 2.0 tone/events producer: `_fetch_gdelt_sentiment(universe) -> dict` (events-OR-tone gate, `_extract_events()` English filter + domain-dedup + `_GDELT_MAX_EVENTS=7`, bounded 429 retry `BASE=20s/MAX=4/CAP=60s`, D-1, never raises); `_GDELT_TONE_URL` and `_GDELT_ARTLIST_URL` (maxrecords=50, sourcelang:eng) reused by `advisors/news_corpus` via CC-2 lazy import; `_fetch_gdelt_sentiment` is the GDELT test seam — patching it propagates into `_build_sentiment_section` via `news_corpus.build_news_corpus()` (DE-NC-001-C1); contract: `.claude/gdelt-contract.md` (DE-GDELT-001..005) |
| `advisors/news_corpus.py` | Multi-source news corpus builder: `build_news_corpus() -> dict`; Facet A = GDELT AvgTone scalar from one `lens_gdelt._fetch_gdelt_sentiment([])` call (inline in `build_news_corpus`); Facet B = ranked corpus from GDELT artlist + 8 RSS/Atom feeds via `feedparser` (`_fetch_all_feeds()`); named-constant scoring (`W_RECENCY=0.40`, `W_RELEVANCE=0.35`, `W_AUTHORITY=0.25`, `TAU_HOURS=24`, `TOP_K=25`); `SOURCE_AUTHORITY` table (1.0 for .gov, 0.9 wire, 0.7 tier-1, 0.4 default); three-step dedup (`_dedup`: URL canonical + Jaccard>=`DEDUP_JACCARD_THRESHOLD=0.85` + per-domain cap 3); topic-tagging (`_tag_topics`: macro/fundamentals/technicals/derivatives, multi-label, defaults `broad-sentiment`); `_UA_GOV` for .gov feeds, `_UA_STD` for commercial; D-1 / never-raises / off-execution-path (CC-2); primary consumer of `ai_advisor._build_sentiment_section`; requires `feedparser>=6.0` (DE-NC-001) |
| `advisors/prism_render.py` | Render-layer prose guard: `humanize_lens_summary(lens_name: str, lens_entry: dict | None) -> str` (R1) — detects structured JSON vs prose via `json.loads` result shape, dispatches to per-lens humanizers (`_humanize_technicals`, `_humanize_sentiment`, `_humanize_derivatives`, `_humanize_macro`, `_humanize_fundamentals`) via `_LENS_HUMANIZERS` table, degrades to `_EMPTY_STATE = "limited inputs — data unavailable"` for null/missing/degenerate inputs; `humanize_obs_preview(raw_response: object) -> str` (R2) — prefers `note` field from raw_response dict, else `_EMPTY_STATE`; never dumps raw JSON; pure stdlib, no I/O, D-1 never-raises; called by `app.py:ai_advisor_tab()` — R1 mutates `per_lens_digest[lens]["summary"]` in-place (app.py:2966–2984), R2 stamps `_preview_text` on non-MARKET_PRISM observations (app.py:2892–2902); producer-agnostic (council prose passthrough, lens_pipeline JSON humanized) |
| `advisors/lens_warehouse.py` | Nightly Lens Data Warehouse: a SEPARATE append-only `alphabot_warehouse.db` (the THIRD DB — distinct from state + optimization; NO cross-DB joins). `init_warehouse_db(path)` (idempotent, WAL), `persist_lens_snapshot(lens, symbol, source, available, raw_payload, ...) -> int` (parameterized, append-only, recursive `_strip_secrets` of `{api_key,token,secret,password,Authorization}`, D-1), `get_lens_snapshots(lens, symbol=None, since=None) -> list[dict]` (ordered by `fetch_ts`). Pytest sentinel: opening the real `alphabot_warehouse.db` under pytest raises — tests pass a temp `db_path`. Wired (non-hollow): `ai_advisor._build_sentiment_section` (GDELT) + `_build_macro_section` (FRED) persist after each fetch (lazy import, off-execution-path). Advisory-only; `raw_json` keeps payloads engine-agnostic (future DuckDB/parquet). (DW-1) |
| `advisors/frontrunner_signals.py` | Live Atlas signal ingestion, IN-MEMORY (frontrunner-signals cycle, 2026-07-16, `DE-FR-SIGNALS-001`): `load_frontrunner_signals(*, force_refresh=False, db_path=None) -> dict` pulls `captplanet.frontrunners` (~3,402 docs) through a DEDICATED daily (`ttl_days=1`) `atlas_cache` key — distinct from the weekly `strategies` key used elsewhere; bounded fetch (`_bounded_fetch_fn`, 30s, DE-CS-002 SRV/DNS-hang pattern), projected (`_id`/`backtest.equity_curve` excluded); persists every fresh (non-cache-hit) pull to the warehouse third-DB (`alphabot_warehouse.db`, `frontrunner_signal_snapshots` table — the ONLY table this module owns). `classify_fr_checks(fr_checks, signal_rows) -> list[dict]` (AC-4) joins extracted FR-checks to signal rows by exact `fr_key` AND live `comparator=="gt"` AND live `fn=="relative-strength-index"` (guards the SPY:10:30 mis-join trap and the cumulative-return fn-mismatch trap); classifies `remove`/`prune` (`FR_WEAK_SHARPE_THRESHOLD=0.38`, p10 of the 152 matched live FR-check Sharpe distribution)/`keep`/`no_edge_data`, never invented — called on EVERY `frontrunner_builder` build run, gating generation and attaching provenance. **De-productized (AC-R2, `6715d654`):** a PM-ruling classification-persistence extension (`persist_classification_run`/`get_latest_classifications`/`get_latest_run_marker`, a second warehouse table pair, an AI Advisor dashboard subsection) was built and briefly wired, then REMOVED per the operator's verbatim ruling — the cull analysis was a one-time PM deliverable, never a product ask; only ingest+in-memory-classify remain. `pymongo` lazy-imported (CC-2); D-1 never-raises; off-execution-path; advisory-only. |
| `advisors/frontrunner_{detector,builder,acceptance}.py`, `advisors/composer_draft_client.py` | Frontrunner Builder cluster (PR #96 wave-1/wave-2, `DE-FRONTRUNNER-001`/`002`; AC-3/AC-6 rebuild + AC-5 additions + AC-G2 Gate#2 fix + AC-R1..R5 de-productization in the frontrunner-signals cycle, `DE-FR-SIGNALS-001`). Pipeline: **detect** (`frontrunner_detector.detect_frontrunner_cascades`) → gather weekly-cached Atlas patterns → **generate** a candidate overlay via Fable (`claude-fable-5`) → compile+splice → independently re-backtest incumbent+candidate → gate (`backtest_gate_engine.evaluate_candidate_batch`, mandatory; Gate#2's incumbent baseline is fold-matched to the candidate's own validation-fold sum, AC-G2-1..6, fixing a fold-vs-full ~5x unit-bias defect + a fix-introduced thin-incumbent regression) → **Calmar acceptance** (`frontrunner_acceptance.evaluate_calmar_acceptance`, two paths: IMPROVE or PRESERVE+SIMPLIFY) → queue for operator approval; on approval, `composer_draft_client.save_symphony` creates a NEW UNDEPLOYED symphony (structurally no invest/deploy method exists anywhere in this module — adversarial source-scan enforced, `tests/security/test_frontrunner_no_trade_boundary.py`). **`frontrunner_detector.py` gained a full AC-6 rebuild this cycle:** `detect_frontrunner_cascades`'s cascade-recognition rule was replaced from a size-cliff heuristic (wave-1, matched 0/11 real trees, empirically falsified — preserved as a dated "Superseded" section, never deleted) to a direction-explicit rule (a condition's TRUE branch reaches a VIX-family ticker); new `extract_fr_checks(tree) -> list[FRCheck]` (AC-3) walks the whole tree for per-condition FR-check extraction, feeding `frontrunner_signals.classify_fr_checks`. **A genuine tree-semantics discriminator was proposed, then itself falsified by a second adversarial pass** (an `rhs-fn`-key-presence hypothesis was proposed, produced an incorrect 8→5 correction to the operator's real symphony count, then was falsified by fr-falsifier2's evidence in favor of the correct `rhs-val`-numeric rule — operator's original count vindicated) — see `docs/generated/advisors_frontrunner_detector.md`'s dedicated discriminator section for the full account, never abbreviate this to "detector fixed" alone. **`frontrunner_builder.py` gained five AC-5 signal-gating functions this cycle** (`filter_positive_edge_signal_keys`, `candidate_contains_tier1_remove_key`, `build_signal_provenance`, `format_crossover_fr_key`/`format_vs_fr_key`, `build_classification_row_for_crossover`) — unit-tested AND WIRED into `_run_build_for_symphony` (`95dac72c`): every build run classifies live signal data in memory, gates generation on positive-edge keys + a Tier-1 remove veto, and attaches provenance to accepted candidates. **A sixth function, `resolve_signals_unavailable_marker`, plus a classification-persistence warehouse table pair and an AI Advisor dashboard subsection, were built, wired, then REMOVED the same day** per the operator's de-productization ruling (AC-R1..R5, `6715d654`) — the cull analysis was a one-time PM deliverable, never a product ask. Weekly-scheduled (`strategy_builder_scheduler.run_weekly_build`) + on-demand (`POST /ai-advisor/frontrunner-builder/run`, wave-2, `app.py`). Advisory-only, never raises, off-execution-path. **Operator-gated task-zero live Composer create test still pending** — `approve_frontrunner_proposal` has only been exercised against mocked Composer responses to date. |
| `advisors/prism_numeric_verifier.py` | Post-council anti-fabrication numeric verifier (DE-PRISM-NUMERIC-VERIFY-001): `verify_cited_numbers(run_id, market_prism_row, lens_sections=None) -> dict` recomputes every `cited_numbers` tuple the council declares against the authoritative `_build_*_section()` payload via `_INDICATOR_REGISTRY` (VIX/VIXCLS/VXVCLS/DGS10/UNRATE/CPIAUCSL/FEDFUNDS/tone/breadth/`<TICKER>.<CONCEPT>`), classifies `pass`/`flagged`/`overridden`/`unverifiable` by magnitude only (`_OVERRIDE_FACTOR=3.0`; deliberately not sign-aware — a sign-flip check would false-override legitimate near-zero GDELT tone drift, contra AC-13); `_derive_verdict` yields `no-numeric-claims`/`no-verifiable-claims`/`clean`/`flags-detected`/`overrides-detected` (malformed/non-list `cited_numbers` never silently reads as "clean" — nvreview Finding 1); `persist_verification` writes one idempotent append-only `MARKET_PRISM_VERIFICATION` `advisor_observations` row (no migration, mirrors `MARKET_PRISM_SOURCES`); D-1 never-raises; off-execution-path; advisory-only; residual limitation — only numbers the council DECLARES as tuples are checked, prose-only numbers are unverified by design |
| `prism_scheduler.py` | Nightly Market Prism council scheduler (Option B, daemon-decoupled): standalone script invoked by OS scheduler (cron/systemd or Windows Task Scheduler via schedule_prism.ps1 for test-green); vanilla-primary headless `claude -p` subprocess; scheduler generates `run_id = str(uuid.uuid4())` in `main()` and threads it into the prompt via `_run_prism(run_id)` — single authoritative join key for all audit rows, MARKET_PRISM observation, and spend_log; 5/5 orchestration directives (PRISM_RUN_PROMPT): (a) run_id threaded (not council-minted), (b) embed run_id + immediate-initial_read kickoff in each analyst's spawn prompt, (c) capture agentIds and pass to synthesizer for agentId-addressing, (d) audit-DB wait-barrier before synthesis (Hard Rule), (e) no false attribution of spawned-but-silent analysts; idempotency guard, bounded retry (MAX_ATTEMPTS=3, BACKOFF_BASE_SECONDS=30, BACKOFF_CAP_SECONDS=60), spend logging (total_cost_usd), MAX_BUDGET_USD=15.0; D-1 error contract; prism-synthesizer has no Agent/spawn tool — primary spawns all 6; **F-4 row-verification**: per-attempt success = rc==0 AND MARKET_PRISM row confirmed via `_get_market_prism_row_for_run(run_id)` seam (D-1, never raises) — rc==0-without-row is a failed attempt, retried up to MAX_ATTEMPTS, loud exit 1 after exhaustion (eliminates silent false-green); see DE-PRISM-COUNCIL + DE-PRISM-COUNCIL-F4; **DE-PRISM-SUB-AUTH-001 (PR #60):** `_run_prism()` builds `_council_env = os.environ.copy()`, pops `ANTHROPIC_API_KEY` (none-safe), passes `_council_env` to the council `subprocess.run` — forces `claude -p` to fall back to `CLAUDE_CODE_OAUTH_TOKEN` (Claude SUBSCRIPTION billing, not metered API; Claude Code precedence = API key beats sub, and `load_dotenv` re-adds it, so it must be stripped); `ANTHROPIC_API_KEY` stays in `.env` for the on-demand Flask advisor (SDK path, never a subprocess); 3 tests `tests/prism_scheduler/test_council_sub_auth.py`. **Droplet runs the daemon+council as non-root `planetstopper` (UID 997) from `/opt/planetstopper`** (root can't run headless `claude -p`); OAuth token via the council systemd timer's EnvironmentFile; **DE-PRISM-SOURCES-001:** after F-4 row-verification, `main()` calls `_patch_provenance(run_id, row)` — re-fetches lens data live via `ai_advisor._build_*_section()` builders at patch time (minutes after council exits), deduplicates by url, inserts `advisor_role="MARKET_PRISM_SOURCES"` append-only row; D-1 never-raises; AC-6 idempotency guard (skip if run_id row exists); citations are rebuilt-at-patch-time per-lens sources — NOT a snapshot of the exact articles the council analyzed (macro/fundamentals/derivatives stable; sentiment artlist may drift slightly); see DE-PRISM-SOURCES-001 §Provenance honesty; **DE-PRISM-DIAG-001:** `_run_prism` logs redacted subprocess stderr/stdout tail on non-zero exit via `_redact_secrets` (sweeps `_council_env` credential-keyed values by `_CREDENTIAL_KEY_MARKERS`, `_MIN_SWEEP_SECRET_LEN=8` over-redaction floor, explicit `ANTHROPIC_API_KEY`, then `sk-ant-`/`sk-`/`oat_` shape regexes); `_STDERR_LOG_CAP=4000`/`_STDOUT_LOG_CAP=2000`; D-1 never-raises (AC-7 inner guard logs diagnostic-suppressed on error); diagnosability infra for unattended systemd nightly failures; **DE-ADVISOR-LATENCY (2026-06-29):** `_patch_provenance` ALSO captures the 5 structured `_build_*_section` outputs (technicals via a separate call — not in `_BUILDERS`) and calls `ai_advisor.persist_market_lens_cache(sections)` in a DEDICATED inner try/except AFTER the SOURCES write — council-safety boundary: a `MARKET_LENS_CACHE` persist failure can never make `_patch_provenance` mis-report a successful run (return True always reached); feeds the advisor cache-serve path (one extra DB write, no new network/timer/migration); **DE-PRISM-NUMERIC-VERIFY-001:** `main()` runs one shared `_fetch_lens_sections()` patch-time fetch feeding BOTH `_patch_provenance` (new `lens_sections=` kwarg, `None`-default byte-identical to prior behavior) AND `_run_numeric_verification` (post-council numeric fact-check, never gates `sys.exit(0)`) |
| `.claude/agents/prism-*.md` | Six Market Prism council role files: `prism-synthesizer.md` (team lead — receives run_id + analyst agentIds from primary at spawn; addresses analysts by agentId not canonical name; Hard Rules: (1) never synthesize until 5 initial_read rows confirmed in audit DB for this run_id — honest limited-inputs degradation naming missing lenses on timeout; (2) never falsely attribute spawned-but-silent as "did not spawn"; drives Q&A + conditional debate <=3 rounds; writes MARKET_PRISM observation row; no Agent/spawn tool); `prism-technicals-analyst.md`, `prism-sentiment-analyst.md`, `prism-derivatives-analyst.md`, `prism-macro-analyst.md`, `prism-fundamentals-analyst.md` (each: begin initial_read immediately on session start from run_id in spawn prompt, file via prism_audit_write CLI, participate in Q&A and conditional debate, D-1 contract, advisory-only; dormancy-triggering "wait for kickoff" pattern removed in prism-council-5of5 cycle); see DE-PRISM-COUNCIL |
| `advisors/` | Phase-1 Advisor producers: `overfitting_conscience.py`, `spec_critic.py`, `divergence_explainer.py`. Narrator deferred. AI Advisor proposal suite: `correlation_diagnostic.py`, `composer_backtest_client.py`, `backtest_gate_engine.py`, `asset_swap_engine.py`, `logic_change_engine.py`, `advisor_chat.py`. **Prism Phase 1 (Cycle 5)**: `prism_audit_write.py` — agent-callable CLI (`python -m advisors.prism_audit_write --run-id <id> --role <r> --phase <p>`, content from STDIN, prints row id; D-1 error contract; no Flask dependency; `load_dotenv(find_dotenv(usecwd=True))` at import ensures `DB_PATH` from `.env` is honored regardless of cwd (DE-PRISM-DOTENV)). `asset_swap_engine.py` (Cycle-3): `extract_lens_scores(context)` extracts per-ticker scores from the 5 honest-availability lens blocks; `generate_objective_directed_candidates` / `propose_operator_swap` / `suggest_swaps` accept `lens_scores=None` kwarg — lens evidence blended into ranking via `_apply_lens_blend` (weight `LENS_BLEND_WEIGHT=0.25`); persistence writes `lens_evidence` + `sources` into `raw_response`; BHY-FDR gate unchanged. `advisor_chat.py` supports M1–M4 + M6 (`strategy_proposal`) artifact types; `CHAT_ARTIFACT_ALLOWED_FIELDS` extended with 13 M6 fields (Phase 4) + Cycle-1 multi-lens fields; `explain_artifact` re-validates internally (defense-in-depth). All observations write to `advisor_observations` keyed by `symphony_id`. **Gate-strength parity (AC-4/AC-5, `DE-ADVISOR-R1-001`, 2026-07-13):** `asset_swap_engine.py` and `logic_change_engine.py` now thread `dated_returns=` (into `BacktestCandidate` construction at the shared `_evaluate_single_variant` site) and a new `_spy_returns_fn_for(symphony_id)` helper (mirroring `strategy_builder_engine.py:807-826`) into every real `evaluate_candidate_batch` call. PBO veto and the real SPY-relative OOS baseline now fire for these two engines exactly as they already did for Strategy Builder — previously they gated on "beats a flat 0.0% return" with PBO structurally incapable of firing (the audit's F2 finding). The `_PBO_MIN_CONFIGS=2` guard is untouched, so PBO stays `None` at N=1 (the operator's single-candidate Evaluate buttons) by design. **R2-2 reasoning port (`DE-ADVISOR-R2-2-001`, 2026-07-14):** `logic_change_engine.py`'s entire fixed-multiplier generator family — `generate_objective_directed_candidates`, `generate_objective_directed_logic_candidates`, `_parse_change_description_to_tweak`, `_fallback_direction_factor` — is DELETED, replaced by `generate_reasoned_logic_candidates` (real Anthropic tool-use call, resolves every edit against the REAL tree, never trusts an LLM-claimed `old_value`); net-new `symphony_schema.validate_tree` guard on every edited tree before backtest (structural safety an LLM-reasoned edit needs that a fixed-percentage script never did); `run_id`/`provenance` (`DE-ADVISOR-R2-1-001`'s 4-key contract) on every return path of both `propose_operator_logic_change` and `suggest_logic_changes`; reuses `ai_advisor.build_reasoning_context` verbatim. **R2-3 reasoning port (`DE-ADVISOR-R2-3-001`, 2026-07-14), closes the R2 program 3-of-3:** `asset_swap_engine.py`'s deterministic generator `generate_objective_directed_candidates` + its `_select_incumbent_asset` helper are DELETED, replaced by `generate_reasoned_swap_candidates` (real Anthropic tool-use call proposing objective-directed incumbent→candidate swap PAIRS, resolved against the real tree/tradeable universe, never trusting an LLM claim); both `propose_operator_swap` (new REASONED branch alongside byte-preserved EXPLICIT-PAIR) and `suggest_swaps` route through the same generator via a shared `_evaluate_explicit_pair` core; net-new `validate_tree` guard before backtest; `run_id`/`provenance` on every return path. `_apply_lens_blend` preserved byte-unchanged (AC-12) but orphaned — zero production call sites — lens evidence now enriches rationale/persistence only, never candidate ranking. |
| `templates/ai_advisor.html` | Single unified AI Advisor SPA template — all 6 tabs (Overview, Correlations, Asset Swaps, Logic Changes, Chat, Strategy Builder) rendered in one server-side render; tab switching in-place via JS; Overview tab includes always-on Market Prism block (sentiment chip, rationale, per-lens digest, cited sources, informative empty state when no nightly row exists); chip modifier maps both canonical (`risk-on`/`risk-off`) and synonym (`bullish`/`bearish`) verdict forms to semantic CSS classes; **RF-1 R2:** `.obs-raw-preview` cell renders MARKET_PRISM rows as `{{ obs.verdict | e }}` and all other rows as `{{ obs.get('_preview_text', '') | e }}` — no raw JSON exposure, no `| safe` (DE-RF1-PROSE-RENDER); **DE-SOURCES-CAROUSEL-001:** Overview Market Prism sources render as a bounded single-row horizontal carousel of clickable cards (`.prism-sources-carousel` overflow-x:auto + scroll-snap; `.prism-source-card`; http(s) urls → whole-card `<a target=_blank rel=noopener noreferrer>`; non-http/`javascript:` urls → non-clickable citation card via `startswith` guard; all fields `| e`-escaped; design tokens only, no raw hex; `{% if _all_sources %}` empty-state preserved; replaces old vertical `<ul class="prism-sources-list">`); **DE-PRISM-SOURCES-PER-LENS-001 (2026-06-30):** Sources section renders one `.prism-sources-carousel` per NON-EMPTY prism lens in canonical order (technicals/sentiment/derivatives/macro/fundamentals), each with a `.prism-lens-carousel-label` header + `data-testid="prism-sources-lens-{lens}"` wrapper; a source cited by multiple lenses appears in each lens carousel (per-lens `per_lens_digest` loop, no cross-lens dedup); empty lenses suppressed via a `namespace(any_sources)` pre-scan over `(sources or article_corpus)`; per-card `.prism-source-lens-tag` badge removed (span+CSS); escaping/URL-guard/outer-empty-state preserved; supersedes the DE-SOURCES-CAROUSEL-001 flat single-carousel); **R2-2 tab-attribution flip (`DE-ADVISOR-R2-2-001`, 2026-07-14):** the Logic Changes tab's `.cap-tab-attribution` no longer reads "Deterministic — no AI reasoning" — now `{{ advisor_suggestion_model | e }} — reasons over your live tree`; the Asset Swaps tab's identical label flipped the same way in R2-3 (`DE-ADVISOR-R2-3-001`, 2026-07-14) — both AI-reasoned tabs now read the live suggestion model instead of "Deterministic"; **Frontrunner Builder tab (2026-07-11, `eb1b612`; applied to this table 2026-07-17):** 7th tab panel (`tab-panel-frontrunner-builder`), following the same in-place-tab pattern as Strategy Builder. Persistent non-dismissible risk banner (the one tab where an operator action — Approve — creates a real Composer symphony). Pending-approval cards render incumbent-vs-candidate Calmar/CAGR/MDD deltas + node-count delta (columns conditionally shown via `is_fr = p.proposal_source == 'frontrunner_builder'`); a collapsible raw-candidate-preview `<details>` block (never raw JSON, no `| safe`). Approve/Reject buttons call `frApprove`/`frReject` (JS). **Frontrunner-signals cycle added, then removed, a "Live Signal Classification" subsection (2026-07-16)** — built (`ae5fe22d`), removed the same day per the operator's de-productization ruling (`f563f16c`); this tab panel is byte-identical to its wave-2 (2026-07-11) shape as of `f563f16c` — do NOT re-add that subsection |
| `static/ai_advisor.js` | AI Advisor client logic: `initTabSwitcher`, suggestion card rendering with per-symphony assessment block, accept/reject, autotune run feed, Strategy Builder tab functions (`sbRunAnalysis`, `openChatWithArtifact`); **DE-ADVISOR-LATENCY AC-3 (2026-06-29):** lens-cache staleness stamp — `#advisor-lens-as-of` populated via `textContent` (HTML-safe, no innerHTML) from the suggest JSON `lens_data_as_of`/`lens_data_stale` ("market context as of <ts>"); **DE-ADVISOR-SUITE-FIX-001 AC-1/AC-2 (2026-07-13):** `sbRunAnalysis()` now RENDERS the run result IN-PLACE on the Strategy Builder tab (survivors / rejected-candidate accordions / "Evaluated N — K passed the gate" / 0-survivor status / error) — the old `window.location.href='/ai-advisor'` discard+navigate bug is gone; a second run REPLACES the prior result (not append); **Advisor-remediation-r1 Checkpoint-3 (`DE-ADVISOR-R1-001`, 2026-07-13, commits `fa691f6a` + `f6688ed4`):** `sbRunAnalysis()` now consumes the AC-7/AC-9/AC-11/AC-12 route-JSON fields the three Evaluate routes gained this cycle — an r1-review finding that the route-JSON tests proved the fields reached the response but never proved this specific render path consumed them. New: a `data-testid="sb-live-provenance"` built-new/Atlas count line and a `data-testid="sb-live-mode-notice"` degraded-run notice (AC-11); a `data-testid="sb-live-screens-skipped"` indicator (AC-12); an `error_category` parenthetical on the error branch (AC-11); a `proposal-card--low-power` CSS modifier on survivor cards (AC-9, caveat TEXT still server-sourced via `c.caveats`, never re-derived in JS); a new `SB_LIVE_REJECTION_COPY` map (4 entries, byte-identical wording to the Jinja/Asset-Swaps/Logic-Changes siblings) rendering a "Gate withheld" line on rejected cards keyed off `rejection_reason` (AC-7), with unmapped/null reasons rendering nothing.; **advisor-outage-degrade AC-4/AC-5 (`DE-SB-DEGRADE-001`, commit `14adb451`, 2026-07-13):** `sbRunAnalysis()` renders a `data-testid="sb-live-backtest-unavailable"` notice (boolean-guarded on `data.backtest_unavailable`, mirrors the `screens_skipped` pairing pattern) when Composer's `/backtest` was unreachable during the run — distinct from a normal "0 passed the gate" empty-state; **R2-1 generation provenance render (`DE-ADVISOR-R2-1-001`, commit `4063ec33`, 2026-07-13):** `sbRunAnalysis()` renders `data.provenance` (model + injected-evidence manifest + run-id) in a new `data-testid="sb-live-generation-provenance"` block, guarded on truthy `data.provenance` (absent on both error branches); deliberately disambiguated from the pre-existing per-candidate `sb-live-provenance` built-new/Atlas rollup — same English word, two independent concepts; **Note (R2-2, 2026-07-14):** the Logic Changes tab's equivalent render lives in a SEPARATE file, `static/ai_advisor_logic_changes.js` (not a function inside this file) — it renders the same provenance shape in a `data-testid="lc-live-generation-provenance"` block, disambiguated from this file's `sb-live-generation-provenance`; this file itself carries zero diff for R2-2; **Frontrunner Builder tab functions (2026-07-11, `eb1b612`; applied to this table 2026-07-17):** `frRunBuild()` — on-demand build trigger, POSTs to `/ai-advisor/frontrunner-builder/run`, does NOT auto-navigate (route returns 202 before results exist); shows a "reload later" status message. `frDispatchProposalAction(action, proposalId)` — shared approve/reject dispatch; disables both card buttons + dims the card during the request (prevents double-submit). `frApprove`/`frReject` — thin `window`-exposed wrappers for Jinja `onclick` handlers |
| `static/index.js` | Client-side dashboard controller: `loadState()` polls `/api/state` + calls `updateDashboard(data)`; `EventSource` subscription on `/api/events` triggers `loadState()` on `cycle-complete` (primary ~1 s update path); 30 s `setInterval` is the resilience fallback; **`showConnectionLost()` (AC-8, 2026-06-23):** targets `#engine-status-dot` (red), `#engine-status-label` (text), `#hero-data-as-of` (freshness stamp) — the real production element IDs; prior code targeted nonexistent selectors, making the staleness cue a silent no-op on connection loss |
| `tests/conftest.py` | Pytest configuration: `pytest_configure()` hook sets `DB_PATH` to a session temp path before collection; autouse `_isolate_db` per-test fixture; `_disable_csrf_for_tests` autouse fixture; **`_disable_auth_for_tests` autouse fixture** (sets `app._auth_check_enabled=False` + clears `_AUTH_FAILED_ATTEMPTS` between tests — guards the ~7000 existing route tests from the DE-AUTH-001 dashboard auth gate) |

## Build / Run
```
python app.py          # run daemon
/run-tests             # pytest (wraps exclusions)
/lint                  # ruff format + check
```

## Architecture Constraints (hard rules for workers)
1. Engine runs 1-minute cadence during market hours — **no blocking I/O on the execution path.**
2. Dashboard has two guarded write paths: `POST /api/settings` (`app.py:2186`) writes allowlisted .env keys; `POST /api/symphony-settings/<name>` (`app.py:2265`) calls `database.set_symphony_live_mode`. Both are CSRF-protected and enforced by `_SETTINGS_WRITE_ALLOWLIST`. **The dashboard is NOT a live-trade-action surface** — settings are operator config, not trade orders. `LIVE_EXECUTION` and all credential keys are excluded from the allowlist.
3. Two-DB pattern: state DB owns live positions/decisions; optimization DB owns Optuna studies. **Never cross-join across DBs in app code** — copy needed rows.
4. `is_live=True` is explicit, never a default.
5. Templates open SQLite read-only; UI never reruns the engine.
6. **AI Advisor Composer hash rule:** all Advisor routes that call Composer endpoints must use the Composer hash ID, not the normalized display name. The route resolves NAME→hash from `bot_state`; `assemble_advisor_context` receives the hash via `composer_symphony_id`. Passing the name causes HTTP 400 from Composer's `/score` API.

## Coding Standards (project additions)
- No magic numbers in `math_engine.py` — every constant named + source comment
- Every change to math layers requires a golden-fixture test
- API calls must be testable from a fixture (Composer + Alpaca)
- Schema migrations: additive-first, NULLable + DEFAULT, never destructive in one step

## Merge & PR Workflow (HARD REQUIREMENT - operator-mandated)
No feature reaches `main` (local OR origin) by self-merge. EVERY feature passes this gate:
1. Feature branch off `origin/main`. Never commit feature work straight to main; never squash-self-merge a cycle branch into local main as a substitute for shipping.
2. Open a PR to origin. Cycle-complete is NOT done - done = PR open, reviewed, functionally tested, and merged to origin.
3. Run the `/review` skill (the built-in "Review a pull request" skill) on the PR - this is the invocable PR-review skill. Do NOT rely on the project `quant-code-reviewer` alone, and do NOT use `/code-review` or the (disabled) pr-review-toolkit. Resolve or explicitly accept findings before merge.
4. Behavioral + functional test, LIVE, run BY THE PM AFTER the `/review` - against the real environment (live DB, running daemon, real API calls / rendered page). Tests-green is necessary, NEVER sufficient.
5. Merge to origin only after 2-4 pass. Then reconcile local main.

"Merged"/green in any status doc means merged to ORIGIN via a reviewed + functionally-tested PR. Work only on local main is "local-only, unshipped" - never marked done.

## Known Gotchas
| Issue | Fix |
|-------|-----|
| Composer.trade API is poorly documented | Assume drift; invoke `composer-api-researcher` before any client change |
| Walk-forward study names | Use `<timestamp>__<symphony>`; never reuse a study name |
| Minute scheduler in `app.py` spawns subprocesses at :00 | Blocking changes to `alpha_bot_execution.py` impact live ops — flag in A/C |
| Default Optuna trial floor | 100 trials (statistical stability) |
| `test_live_*.py` files | Excluded by default — opt-in via `--include-live` |
| Migration 021 listed before 020 in `_MIGRATION_FILES` | Intentional — see ARCH-002 inline comment in `database.py`. Reordering would corrupt live DBs that already have 021 applied. |
| Blast-radius scanners sweeping `.claude/worktrees/` | `rglob("*.py")` scanners must exclude `.claude/worktrees/` and `.claude/audit-worktrees/`; stale pre-deletion .py files in orphan worktrees produce false-positive import violations. |
| Test writing to production DB | `database._db_file()` raises `RuntimeError` under pytest if `DB_PATH` resolves to `alphabot_state.db`. Fix: ensure `tests/conftest.py` `pytest_configure()` runs before any DB import. Per-test isolation via `_isolate_db` autouse fixture. |
| AI Advisor empty suggestions (most symphonies) | Expected. The CRRA-EU + Harvey-Liu FDR gate is intentionally strict. `build_assessment_from_context` explains why — `oos_alpha=None` means all trials were haircut-rejected, not an error. |
| Advisor tab templates deleted | The 4 old per-tab advisor templates (`ai_advisor_correlations.html`, etc.) were deleted in the advisor-cleanup cycle (2026-06-10). Do not recreate them — the unified SPA at `templates/ai_advisor.html` is canonical. |
| Strategy Builder standalone template deleted | `templates/ai_advisor_strategy_builder.html` was deleted in the spa-port cycle (2026-06-13) when the Strategy Builder was folded in as the 6th tab of the unified SPA. Do not recreate it. `GET /ai-advisor/strategy-builder` now 302-redirects to `/ai-advisor`. |
| Full pytest run can commit >67 GB on Windows (Kernel-Power 41, 2026-06-21) | Total-job OS cap auto-installed via `tests/_mem_cap.py` + `conftest.pytest_configure` (DE-TEST-MEMCAP-001 + DE-TEST-MEMCAP-002); env knob `ALPHABOT_TEST_MEM_CAP_GB` (default 24 GB). Hardened (2026-06-22): `KILL_ON_JOB_CLOSE` flag kills orphaned children on controller exit; `IsProcessInJob` verify-or-fail-loud in `install_total_memory_cap` — `_CAP_INSTALLED` stays False and emits UserWarning if membership cannot be confirmed; `_assert_safe_worker_count` in `conftest.pytest_configure` rejects `-n auto` / `-n>4` with loud SystemExit before install runs. Never run full suite uncapped or with `-n>4`. |
| JS syntax coverage (`node --check`) | Consolidated into one parametrized test in `tests/js_syntax/test_js_syntax.py` (glob-discovers `static/*.js`); ~14 scattered per-method checks in `tests/ui/`, `tests/dashboard/`, `tests/ai_advisor/`, `tests/app/` removed. Do not add new per-file `node --check` test methods to individual test files — extend the parametrized module instead. |

## Project-Local Specialist Agents (`.claude/agents/`)
**Task-engine specialists:**
`risk-engine-specialist` · `optuna-specialist` · `flask-dashboard-specialist` · `sqlite-specialist` · `composer-alpaca-integration` · `quant-test-writer` · `quant-code-reviewer`

**Domain researchers:**
`composer-api-researcher` · `alpaca-api-researcher` · `quant-risk-researcher` · `optuna-methodology-researcher` · `viz-library-researcher` · `trading-compliance-researcher`

**Cross-cutting:**
`security-auditor` · `synthesizer` · `verifier`

## Project-Local Skills (`.claude/skills/`)
`/backtest` · `/optuna-compare` · `/db-inspect` · `/api-fixture` · `/discord-test` · `/run-tests` · `/lint` · `/perf-snapshot` · `/symphony-diff`

## Agent Team Composition (HARD REQUIREMENT)
**All new codepaths — and bug fixes that introduce new codepaths — MUST be built via the Toxic Pair TDD composition of Agent Teams.** The PM may NOT approximate a team with sequential solo-agent RED → GREEN → review dispatches. Real Agent Teams only: shared worktree, one branch, `SendMessage` handoffs, autonomous Toxic Pair (test-writer ⇄ implementer) cycling with reviewers wrapped around it.

**Skill-driven TDD is MANDATORY (not ad-hoc RED/GREEN).** Every dev cycle is driven through the TDD skills, and the kickoff prompt MUST say so explicitly:
- Planning artifact first: each feature has a `/scaffold`-format plan in `feature-plans/<name>.md` with `Status: ready` (Summary / Acceptance Criteria as `AC-N` / Architecture / Edge Cases / Security Considerations / Testing Strategy / Scope Boundaries). Use the `/scaffold` skill to produce it — never hand-roll.
- **test-writer** runs `/tdd <feature-plan>` → writes RED tests + creates `.claude/tdd-handoff.md`.
- **implementer** runs `/tdd-implement` → reads the handoff (NOT the plan — deliberately blind), writes minimal GREEN.
- finalize the cycle with `/tdd-finalize` before cycle-complete.
This requires the `Skill` tool in the agent's `tools:` frontmatter — all dev specialists (quant-test-writer, sqlite-specialist, risk-engine-specialist, optuna-specialist, flask-dashboard-specialist, composer-alpaca-integration) carry it. If a specialist lacks a needed capability, fix its frontmatter — never work around it.

Standing TDD team: **Quint** (test-writer + implementer + `quant-code-reviewer` + domain specialist matched to the surface touched + **doc-writer**).
**A `doc-gen` doc-writer is a MANDATORY member of EVERY team (global hard rule) — added on top of the Quad/whatever composition.** It documents the cycle's changes into `docs/generated/` / `DECISIONS.md` / README and commits them on the shared branch before cycle-complete; the cycle is not complete until those updates are in. Never omit it (TDD, audit, or research teams alike).
Math-layer changes always add `quant-test-writer` as the adversarial test author.
**doc-writer goes on EVERY team** (TDD and non-TDD): owns feature-plans/ docs for the cycle, audits docstrings/constant comments (files findings to the owning teammate — never edits their files), and drafts CLAUDE.md key-files updates for PM approval. Bad documentation is a shippable defect.

**Exceptions — no TDD required (but often still a team):** config/doc-only edits, one-line fixes to existing codepaths, behavior-preserving refactors fully covered by existing tests, and pure research/diagnosis tasks (no code written). These skip the Toxic Pair because there is no code to drive RED→GREEN — but a multi-surface diagnosis or audit should still run as a **non-TDD Agent Team** (communicating auditors + a synthesizing lead), not a swarm of disconnected solo agents. Reserve solo background agents for genuinely independent single-surface tasks. See `~/.claude/CLAUDE.md` §"Agent Teams" for the composition catalogue.
