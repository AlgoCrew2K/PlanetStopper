# Feature: Exit-Tuning Friction Term + Realized-Basis $-Saved + Turnover Surfacing
Status: ready
Created: 2026-07-23
Amended: 2026-07-24 (PM rulings on ga2-tw pre-RED recon findings A/B/C, cross-verified independently by ga2-opt on B — AC-2, AC-5, AC-6, AC-8 updated in place below; Decisions table appended. Frozen contract lives in .claude/tdd-handoff.md v1.)

## Summary
Close the transaction-cost gap identified in docs/research/methodology-validation-2026-07.md (internal audit: the exit-parameter tuning loop is 100% gross-of-cost; the "$-saved" headline is decision-time-snapshot based; turnover is unquantified despite `exit_triggers` holding the data). Three additive parts, one cycle: (1) a named, non-tunable friction constant applied per simulated exit round-trip inside the autotuner's replay so the CRRA-EU objective optimizes net-of-friction; (2) a realized-basis $-saved computed from the first post-rebalance observed value, reported alongside (never replacing) the existing snapshot basis; (3) per-symphony exit-frequency/turnover stats read from `exit_triggers`, including an estimated annual friction drag. No live-engine decision-path changes anywhere.

## Acceptance Criteria
- [ ] AC-1: New named constant `SIM_EXIT_FRICTION_PCT` (proposed 0.005, i.e. 0.5% per round-trip [PM-ASSUMED — sourced for consistency from the Composer backtest client's existing `slippage_percent=0.005` assumption at `composer_backtest_client.py:237-296`; operator may override with a measured value]) with a source comment citing both the Composer client assumption and the validation report.
- [ ] AC-2 (AMENDED 2026-07-24, PM ruling — frozen as AC-2v2): The friction term is applied uniformly at ALL THREE replay-layer exit-accounting sites that share the `triggered_return = tick['return'] + penalty` pattern — `_collect_sim_returns` (autotuner.py:1523-1526), `_collect_sim_returns_dated` (autotuner.py:1717-1720), and `run_simulation` (autotuner.py:1984-1987) — each as a distinct subtraction, NOT folded into the existing −0.20% deviation-dict penalty (different meaning: modeling-uncertainty haircut vs trading friction; both retain their own named constants). WHY three sites, not one: `_collect_sim_returns_dated` feeds `cscv_date_returns` into `math_engine.compute_pbo` (the Phase-3 PBO overfitting veto); `run_simulation` computes `oos_alpha`/`fallback_oos_alpha`/`default_oos_alpha` consumed by the acceptance-gate accept/reject cascade. Single-site application would leave the PBO veto and the OOS-alpha acceptance gate on a gross-of-friction basis while the trial objective (which reaches `_collect_sim_returns` via `run_simulation_crra_eu`) went net-of-friction — reintroducing the gross-vs-net inconsistency this feature exists to fix, just relocated from objective to gate.
- [ ] AC-3: `SIM_EXIT_FRICTION_PCT` is NOT in the Optuna search space, NOT env-tunable, and a test asserts it is absent from the tunable-parameter set (a tunable friction would let the optimizer game its own cost model).
- [ ] AC-4: Golden fixtures updated/added: identical replay scenario with friction 0 vs `SIM_EXIT_FRICTION_PCT` shows exactly the expected objective delta; existing objective fixtures re-pinned with the constant's value explicit in the fixture (never silently absorbed).
- [ ] AC-5 (AMENDED 2026-07-24, PM ruling — recon Finding A): Stage-2 post-mortem (16:00 ET, post-rebalance) additionally records, per triggered symphony, the first post-rebalance observed value as additive JSON field(s). Sourcing MUST mirror Stage-1's shadow_history query pattern (`database.load_latest_shadow_row`/`load_earliest_shadow_row`) at a post-rebalance cutoff (latest shadow_history row of the trading day / post-16:00 ET) — NEVER `bot_state`'s live `current_return`, whose action-phase clobbering is the exact DE-GUARD-ALPHA-SAVED-001 defect (PR #80: 7 of 11 audited days sign-flipped). No new external API calls — this reuses the shadow_history table Stage 1 already reads, not a new fetch. Absent data (no qualifying shadow_history row) → field absent, never fabricated. HONESTY ADDENDUM: this is an EOD-MARKS basis — it captures post-snapshot price drift through the actual rebalance window, NOT fill-level execution slippage (true fill reconciliation stays OUT per Scope Boundaries); docs and the panel tooltip must say "marks basis" plainly, never implying fill-level truth.
- [ ] AC-6 (AMENDED 2026-07-24, PM ruling — realized_source provenance): `saved_dollars_realized` computed per triggered symphony using the post-rebalance observed value (AC-5) in place of the decision-time snapshot; each realized entry carries a `realized_source` provenance field mirroring Stage-1's `if_held_source` so a silent degradation is impossible. `/api/guard-alpha-summary` returns BOTH `saved_dollars` (snapshot basis, unchanged math — DE-GUARD-ALPHA-SAVED-001 semantics preserved: if-held from `shadow_history.current_return`) and the realized aggregate with an honest `realized_coverage` count (how many exits have realized data). Dashboard headline shows both bases, labeled ("snapshot basis" / "realized basis (marks), N of M exits") — the "marks" qualifier is mandatory per AC-5's honesty addendum.
- [ ] AC-7: When realized data is unavailable for an exit (pre-feature post-mortems, missing Stage-2), that exit is excluded from the realized aggregate and counted in the coverage denominator — never silently substituted with the snapshot value.
- [ ] AC-8 (AMENDED 2026-07-24, PM ruling — recon Finding C): New read-only turnover stats: per-symphony exit count over trailing 30/90/365 days from `exit_triggers` (`migrations/005_exit_triggers.sql`), plus `est_annual_friction_drag_pct = exits_per_year × SIM_EXIT_FRICTION_PCT`, exposed on the same or a sibling GET route and rendered beside the preconditions/turnover area of the Performance tab. EVERY window carries a `coverage_days` field (actual days of retained history behind that count — mirrors AC-7's honesty pattern) because `exit_triggers` is pruned daily via `database.prune_old_triggers`/`TRIGGER_TELEMETRY_RETENTION_DAYS` (default 90 days, operator-configurable, `app.py:788`) — the 365-day window is structurally capped by this retention setting and MUST NEVER imply full-year coverage the table can't back. This is operator knob #2 alongside `SHADOW_HISTORY_RETENTION_DAYS`; both belong in the docs' operator-knobs section. Re-entry legs are NOT counted (exit-leg-only; documented limitation — re-entry is implicit in Composer's rebalance and not discretely logged).
- [ ] AC-9: Post-mortem JSON schema change is additive-only; History/Performance tabs and the Discord embed continue to parse old and new files (malformed/old-file-safe, per existing consumer patterns).
- [ ] AC-10: Zero changes to live exit-decision codepaths in `alpha_bot_execution.py`/`math_engine.py` decision functions; a test (or blast-radius assertion in review) confirms the friction constant is referenced only from replay/reporting/analytics surfaces.

## Architecture
- **Replay math**: constant `SIM_EXIT_FRICTION_PCT` lives at `autotuner.py` module scope (RULED 2026-07-24 — `math_engine.py` is imported by the live engine; this constant is a replay-environment parameter, not engine math), with a source comment citing both the Composer client's `_DEFAULT_SLIPPAGE_PERCENT` assumption (`composer_backtest_client.py:71`) and the validation report. Subtraction applied at the exit-accounting site in ALL THREE replay functions (AC-2v2): `_collect_sim_returns` (autotuner.py:1523-1526), `_collect_sim_returns_dated` (autotuner.py:1717-1720), `run_simulation` (autotuner.py:1984-1987). Blast radius: objective values, PBO estimates, and OOS-alpha acceptance-gate comparisons all shift for future autotune runs → next retune may select different parameters (intended — the whole replay layer becomes net-of-friction, not just the trial objective). Flag in cycle kickoff per the scheduler gotcha (engine spawn at :00 unaffected — no engine file behavior change).
- **Post-mortem producer**: `reporting.py` Stage 2 — additive fields; Stage-1 freeze math untouched (`reporting.py:50-61` snapshot basis preserved exactly). Stage-2's new realized-basis fields source from `shadow_history` via the SAME `database.load_latest_shadow_row`/`load_earliest_shadow_row` accessors Stage 1 uses (RULED 2026-07-24 — see AC-5), never from `bot_state`'s live `current_return`.
- **API**: extend `guard_alpha_summary()` (`app.py:2172`) response additively; turnover either in the same payload or `GET /api/exit-turnover` (implementer's call; both read-only, global auth hook, not in `_SETTINGS_WRITE_ALLOWLIST`).
- **UI**: `dollar-saved-panel` headline gains the second labeled figure; turnover column/section on Performance tab; JS follows the 401-guarded fetch pattern.
- **DB**: no schema migration required (exit_triggers exists; post-mortems are JSON files). If the cycle finds a migration necessary, additive-first NULLable per house rule.
- **Dependencies**: none new.

## Design-System Mapping
No design system declared. House conventions: extend the existing `dollar-saved-panel` markup/classes for the dual-basis headline; Performance-tab table classes for turnover; no inline styles; semantic class names for the coverage annotation.

## Edge Cases
- Friction larger than a simulated exit's gain → negative guard-alpha for that event (correct; must flow through, not clamp at zero).
- Multiple triggers same symphony/day → per-event friction application; realized matching uses the event timestamp, not the date alone where possible.
- Market holiday after trigger (no next-day observation) → realized field waits for first available observation; if none within the post-mortem's horizon, absent (AC-7 path). Check the NYSE calendar behavior in tests (known deploy gotcha class).
- Old post-mortem files without new fields → parsed cleanly, counted as no-coverage.
- `realized_coverage = 0` → realized headline renders "no realized data yet", never $0.00.
- Epoch boundary: realized aggregate is epoch-additive like the snapshot basis (same accumulation semantics — SUM across epochs of per-epoch values).
- Friction constant edge in fixtures: 0.0 must reproduce today's objective exactly (backward-compatibility fixture).

## Security Considerations
- All new/changed routes read-only; no write-path or allowlist changes; no `LIVE_EXECUTION` interaction; auth via global hook.
- Post-mortem JSON is producer-owned (our own daemon) but consumers stay malformed-file-safe (existing hard rule) — treat as untrusted at parse time.
- No raw Composer/Alpaca response bodies logged or echoed (logging-redaction house rule); realized values are derived numbers only.
- No user input on any new surface; XSS surface limited to symphony names already escaped in templates.

## Testing Strategy
- **Golden fixtures (mandatory — math layer)**: friction-off vs friction-on delta across ALL THREE replay sites (AC-2v2/AC-4) — `_collect_sim_returns`, `_collect_sim_returns_dated`, `run_simulation`; backward-compat 0.0 fixture covering all three functions' outputs; negative-guard-alpha flow-through case. Realized-basis fixtures reuse the captured-from-producer `tests/fixtures/prod_droplet/shadow_postclose_rows.json` (real droplet rows at/after 16:00 ET, `is_post_trigger: 1`) rather than synthesizing new shadow_history rows — provenance-clean per house fixture rule.
- **Unit**: constant-not-tunable assertion (AC-3); Stage-2 additive-field writer with missing-data paths; realized-basis math from fixture post-mortems (values derived from fixture, never hardcoded producer literals — house test rule); coverage accounting (AC-7); turnover queries against a seeded temp DB incl. 30/90/365 windows and empty table, PLUS near-retention-limit and mature-history cases so `coverage_days` (AC-8) is proven honest at both extremes.
- **Route tests**: extended guard-alpha-summary schema, old+new post-mortem mix, turnover route, honest empty states.
- **Suite scope**: run the execution+engine suites (changes adjacent to `alpha_bot_execution` consumers mock it — known blast-radius class) plus reporting/app suites; full-tree gate before merge per house merge protocol.
- **JS**: extend the parametrized syntax test module.
- **Behavioral (PM live gate)**: droplet dashboard render showing dual-basis headline + turnover with real post-mortems; one real Stage-2 cycle observed writing the new fields (dry-run acceptable if market-closed; check DRY RUN before market-gating the deploy).

## Decisions
| Decision | Rationale |
|----------|-----------|
| Friction value from Composer client's slippage assumption (0.005) [PM-ASSUMED] | Internal consistency: the Strategy Builder gate already validates candidates under this assumption; one friction worldview across both research surfaces until a measured value replaces it |
| Friction is a fixed environmental constant, never tunable | The optimizer must not be able to game its own cost model |
| Keep deviation penalty and friction as separate constants | Different semantics (modeling uncertainty vs trading cost); folding them would destroy the audit trail of both |
| Realized basis reported alongside snapshot basis, not replacing it | Snapshot basis has shipped semantics (DE-GUARD-ALPHA-SAVED-001); dual display is honest and preserves comparability |
| Exit-leg-only turnover | Re-entry is implicit in Composer's daily rebalance and not discretely logged; inferring round-trips from `bot_state["triggered"]` transitions is deferred |
| No new external API integration for fills | Stage 2 already observes post-rebalance state; true fill-level reconciliation would need Composer API research (separate cycle if the dual-basis gap proves material) |
| Realized-basis sourced from shadow_history, never bot_state (RULED 2026-07-24) | `bot_state.current_return` is action-phase-clobbered — the exact DE-GUARD-ALPHA-SAVED-001 defect (PR #80); `shadow_history` is the only trustworthy if-held source, per Stage-1's own established pattern |
| Realized basis is documented/labeled as a marks basis, not fill-level (RULED 2026-07-24) | Captures post-snapshot price drift through the rebalance window; true fill reconciliation needs new Composer/Alpaca API work, explicitly out of scope |
| Friction applies at all three replay exit-accounting sites, not just `_collect_sim_returns` (RULED 2026-07-24, AC-2v2) | Single-site application would leave the PBO veto (`_collect_sim_returns_dated` → `compute_pbo`) and the OOS-alpha acceptance gate (`run_simulation`) gross-of-friction while the trial objective went net — reintroducing the gross-vs-net gap this feature exists to close |
| Every turnover window carries `coverage_days` (RULED 2026-07-24) | `exit_triggers` is pruned via `TRIGGER_TELEMETRY_RETENTION_DAYS` (default 90d); a bare 365-day count would silently imply coverage the table can't back |

## Scope Boundaries
- **IN**: replay friction constant + application, dual-basis $-saved (producer + API + panel), exit-leg turnover stats + friction-drag estimate, golden fixtures.
- **OUT**: live engine decision changes; Optuna search-space changes; fill-level reconciliation via new Composer/Alpaca endpoints; round-trip (re-entry) turnover inference; Discord embed redesign; retroactive backfill of realized data for historical post-mortems; any DB migration unless the cycle proves one necessary.
