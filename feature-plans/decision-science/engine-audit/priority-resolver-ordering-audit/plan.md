# Plan — Engine Audit: Priority Resolver Ordering Audit

| Field | Value |
|---|---|
| Feature | Read-only audit of `resolve_trigger_priority` and `_TRIGGER_PRIORITY_ORDER` (`math_engine.py:659-692`) against the decision-science changes. Confirms (a) the Phase-1 floor introduces zero new firing flags (M1 is offline; M2 is diagnostic-only); (b) the Phase-2 co-signal extension's position in the order is documented and structurally enforced; (c) the resolver's tie-breaking is total and deterministic; (d) no caller bypasses the resolver and inspects the raw flags. |
| Phase | Engine audit (post-Phase-1; re-runs at Phase-2 entry) |
| Owner agent-type | `quant-code-reviewer` (audit lead) + `risk-engine-specialist` |
| Source-of-truth refs | `math_engine.py:659-692` (`_TRIGGER_PRIORITY_ORDER`, `resolve_trigger_priority`); `docs/handoff/council-attack-rubric.md` G-3 (per-layer fate explicit + resolver successor stated); `docs/handoff/decision-science-council-synthesis.md` §3.2 (6 heuristics retained, unchanged), §5.3 (Phase-2 co-signal enters as one additional boolean), `decision-science-baseline-2026-05-22.md` §2.2 (layers do NOT conflict — one total deterministic order) |
| Why | The "7+ conflicting heuristics" claim was the load-bearing pitch refuted by baseline §2.2 and §6 — the layers have a total deterministic priority order. Any Phase-1 PR that *seems* small but adds a new firing flag or bypasses the resolver violates G-3 silently. The Phase-2 co-signal's position in the order is also load-bearing (per the cvar-cosignal-hysteresis test overlay: it must be after the hard layers because it never solely fires). |
| Deliverables | (1) `docs/handoff/engine-audit/priority-resolver-audit-findings.md` — confirms each Phase-1 change touches zero firing flags; each existing flag's source is recorded. The Phase-2 co-signal flag's position is committed. (2) `tests/math_engine/test_priority_order_is_total_and_deterministic.py` — for every subset of the 4 (Phase 1) or 5 (Phase 2) firing flags, the resolver returns a deterministic winner and a deterministic co-fired list. (3) `tests/math_engine/test_no_caller_bypasses_resolver.py` — grep test: no caller of `compute_exit_confirmation`, `compute_vwap_break_decision`, etc. inspects the raw boolean flag and acts on it; the resolver is the single arbiter. (4) `tests/math_engine/test_phase_2_cosignal_priority_position.py` — Phase-2-conditional: `_TRIGGER_PRIORITY_ORDER` contains `CVaR Co-Signal` at position 5 (last among firing-eligible signals); never earlier than the trailing stop. (5) The audit explicitly verifies the "co-signal never solely fires" structural property holds with the resolver design — cross-references the cvar-cosignal-hysteresis test overlay. |
| Dependencies | The MC-blast-radius plan (defines `CVaRAssessment` shape, which feeds the co-signal flag); the cvar-cosignal-hysteresis test overlay; nothing in Phase 1 changes the resolver (audit confirms this). |
| Golden-fixture tests required | Tests 2, 3, 4 are property/structural tests. |
| Definition of Done | (a) Audit findings document confirms Phase-1's zero-flag-addition claim. (b) Three tests pass GREEN; test 4 fails-RED before the Phase-2 co-signal lands. (c) `_TRIGGER_PRIORITY_ORDER` is the single source of order truth — no parallel ordering list exists. |
| Risk callouts | **R1 — A "performance" PR bypasses the resolver and inspects a raw flag**: G-3 fail; test 3 catches. **R2 — Phase-2 co-signal slides up the priority order in a future cycle**: test 4 hard-pins the position. **R3 — A new flag is added without an audit trail**: test 2's exhaustive subset coverage catches a flag addition that isn't covered. |
| Out of scope | The actual priority constants in any layer (separate audit per layer if needed); the resolver's signature (already audited under MC-blast-radius); the co-signal's hysteresis logic (separate test overlay). |
