# Plan — M2 Second-Window Residue (operator-optional `cvar_5pct_long` / `cvar_n_tail_long`) — no signed divergence

| Field | Value |
|---|---|
| Feature | The only surviving residue from the divergence-idea evaluation. The operator may run M2 over a second, longer window; the second-window CVaR is persisted in two additive NULLable columns on `cvar_diagnostics` (`cvar_5pct_long`, `cvar_n_tail_long`); each window is rendered **independently** under its own full four-part S-3 contract. **Binding:** no `cvar_divergence`, no `regime_recency_weight`, no signed-delta column, no threshold-shaped UI affordance — the moment any such value appears on a display the REJECT (§B) is undone. |
| Phase | 1 (additive to M2; operator-optional) |
| Owner agent-type | `risk-engine-specialist` (the second computation) + `sqlite-specialist` (the additive migration) + `quant-test-writer` + `quant-code-reviewer` |
| Source-of-truth refs | `docs/handoff/decision-science-v3-and-divergence-evaluation.md` §B.6 (operator-optional residue, no signed-divergence quantity surfaced), §B.7 (the only conceivable detector path is Phase-2-or-never with external label set), §B.9 (verdict REJECT, residue unlocks nothing); §A.8 A3 (named exclusion list for Gate-1); the council-converged migration plan §3.1 row 023 (`cvar_diagnostics` extended additively) |
| Why | The user correctly identified a real (minor) M2 weakness — a CVaR number with no temporal reference frame. The fix is two independent diagnostic windows, never a derived divergence. The REJECT routes (squeeze + circular-provenance kill per §B.3) hold regardless of how the second window is constructed. Any divergence/threshold/recency-weight column reintroduces the rejected detector affordance. |
| Deliverables | (1) Schema additive: `cvar_5pct_long` (REAL, NULLable, DEFAULT NULL) and `cvar_n_tail_long` (INTEGER, NULLable, DEFAULT NULL) added to `cvar_diagnostics` in `023_cvar_diagnostics.sql` per the council-converged plan §3.1 row 023 (additive). **Never `NOT NULL`** — when `CVaR_long` is insufficient (thin 3-year tail, early-life symbol), the row writes NULL, mirroring the `run_monte_carlo` / `CVaRAssessment` `None` sentinel; a `NOT NULL` would force a fabricated zero. (2) Computation: the second-window CVaR uses the existing rolling Alpaca pool the kNN MC already pulls (3-year window per `alpha_bot_execution.py:272`), seeded by the same `derive_cycle_mc_seed` discipline; **no second replay-determinism anchor** is introduced (the second statistic is off the same `cycle_id`-seeded resample), so the Phase-1 anchor count remains exactly 1. (3) `tests/app/test_m2_display_two_windows_independent.py` — fixture: render M2 with both `cvar_5pct` and `cvar_5pct_long` populated; assert each is presented under its own full four-part S-3 contract; assert NO computed delta, ratio, signed difference, or threshold-shaped affordance is rendered. (4) `tests/database/test_cvar_long_columns_nullable.py` — open a `cvar_diagnostics` row with `cvar_5pct_long IS NULL` and `cvar_n_tail_long IS NULL`; assert the schema accepts it (DEFAULT NULL). (5) `tests/integration/test_no_cvar_divergence_column_anywhere.py` — grep-test across the codebase: zero occurrences of `cvar_divergence`, `regime_recency_weight`, `divergence_threshold`, or any signed-delta-named column or variable. (6) `tests/integration/test_no_cvar_divergence_in_persisted_payload.py` — render the dashboard's M2 payload; the dict's keys do NOT contain any divergence/delta token. |
| Dependencies | Migration `023_cvar_diagnostics.sql` (persistence-architect-owned, extended additively per this plan); the M2 plan; the dashboard side-effect ban plan (no engine recompute from the dashboard, even for the second window — the second window is computed on the engine's cycle, not on dashboard render). |
| Golden-fixture tests required | Tests 3, 4, 5, 6 are golden-fixture / grep tests. The `cvar_5pct_long` fixture is captured from the actual 3-year Alpaca pool the engine already maintains — non-circular. |
| Definition of Done | (a) Two additive NULLable columns ship; four tests pass GREEN. (b) Zero divergence-named columns or variables exist in the codebase — test 5 grep-asserts. (c) Dashboard rendering never surfaces a signed delta — test 3 asserts. (d) The §B reject framing is in the M2 plan's docstring and in the column comments in `023_cvar_diagnostics.sql` — a future contributor reading the migration sees "no signed-divergence quantity surfaced" before adding one. |
| Risk callouts | **R1 — A "user-friendly" PR computes `cvar_5pct - cvar_5pct_long` and renders it**: rejected per §B.6; test 3 and test 6 catch. **R2 — `cvar_5pct_long` is treated as a Phase-2 stepping-stone signal**: §B.9 — "the residue unlocks no architecture, no finalist, and no validation." **R3 — The second-window seed bleeds**: the same `cycle_id`-seed and `np.random.default_rng` shape applies; the live-vs-replay plan's grep-tests catch any new RNG path. **R4 — A "compromise" non-persisted divergence is computed at render time only**: also REJECTED — §B.6 is binding on display affordances, not just persisted columns. |
| Out of scope | The detector-validation path (Phase-2-or-never per §B.7, requires external regime-chronology label set); any signed-divergence quantity in any form; the regime-recency weighting as a `signal` (already covered by S-3 element b — `tail_obs_count`; per §B.8 the divergence framing adds nothing S-3 doesn't already require). |
