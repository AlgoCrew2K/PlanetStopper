# SUPERSEDED — see `m1-crra-eu-autotuner-objective/plan.md` §"Deliverables — S-2 t-stat slice"

This plan has been folded into the canonical M1 integration plan per README §4 option (ii). The slice content — `compute_crra_eu_tstat` signature (takes `daily_returns` + `gamma`), `statistics.stdev` (n-1) discipline, degenerate-series `0.0` guard, `_haircut_select` `tstat_fn` parametrization, `compute_sortino_tstat` retention + warning docstring, H-6 inline comment update, and tests T3/T4/T5/T6 — is now captured verbatim in the canonical plan.

**See:** `feature-plans/decision-science/phase-1/m1-crra-eu-autotuner-objective/plan.md`
**Section:** "Deliverables — S-2 t-stat slice"

This file is preserved because README §3 coverage-matrix row "S-2 — re-derived t-stat `compute_crra_eu_tstat`" links to it.
