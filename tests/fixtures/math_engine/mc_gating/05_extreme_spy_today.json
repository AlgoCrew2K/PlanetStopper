{
  "name": "extreme_spy_today_far_from_neighbors",
  "description": "spy_today_return is far from every historical SPY return; neighbors are still selected, but current_symphony_return is large relative to simulation distribution. Exercises the searchsorted/below_count tail of the distribution. Captures the documented behavior where the probability collapses to 0.0 when current return dominates the sim distribution.",
  "function": "run_monte_carlo",
  "inputs": {
    "holdings": [
      {"ticker": "AAA", "allocation": 1.0, "last_percent_change": 0.01}
    ],
    "historical_data_spec": {
      "kind": "alternating",
      "num_days": 30,
      "spy_amplitude": 0.005,
      "aaa_amplitude": 0.005
    },
    "spy_today_return": 50.0,
    "simulation_paths": 5000,
    "neighbor_k": 150,
    "numpy_seed": 42
  },
  "expected": 0.0,
  "derivation": "Captured behavioral pin under numpy.random.seed(42). current_symphony_return = 0.01 * 100.0 * 1.0 = 1.0. The simulation distribution is constructed from low-amplitude alternating returns scaled by 100 (range [-0.5, 0.5]); current_symphony_return=1.0 lies above the entire distribution => searchsorted returns simulation_paths, below_count=simulation_paths, result = ((P - P) / P) * 100.0 = 0.0."
}
