{
  "name": "squeeze_multiplier_small_positive_tightens",
  "description": "para_armed=True with a small POSITIVE squeeze_mult (0.2). The squeeze tightens the stop but the result stays strictly positive. Replaces the retired 12_squeeze_multiplier_zero_collapses_output fixture: AC-5 (audit M-2) now REJECTS parabolic_squeeze_multiplier <= 0 with a ValueError, so a 0.0 multiplier is no longer a valid golden-fixture input. The multiplier==0 rejection is covered by tests/math_engine/test_squeeze_multiplier_rejection.py (case multiplier_zero_rejected). This fixture keeps the golden suite's coverage of the squeeze-applied path with a valid in-range multiplier.",
  "function": "compute_active_trailing_stop",
  "inputs": {
    "symphony_vol": 2.0,
    "dynamic_multiplier": 1.5,
    "dynamic_min_stop": 0.30,
    "para_armed": true,
    "breakeven_locked": false,
    "parabolic_squeeze_multiplier": 0.2
  },
  "expected": 0.6,
  "derivation": "safe_vol = 2.0. raw = safe_vol * dynamic_multiplier = 2.0 * 1.5 = 3.0. max(3.0, dynamic_min_stop 0.30) = 3.0. para_armed=True -> active *= parabolic_squeeze_multiplier 0.2 -> 3.0 * 0.2 = 0.6. Output = 0.6 (strictly positive — the squeeze tightens but does not collapse the stop). The dynamic_min_stop floor is NOT re-applied after the squeeze; this fixture pins the producer's exact ordering (floor before squeeze). R3-c NOTE (MA-11): this is the 6-arg golden, so the new optional squeeze_floor defaults to None -> NO post-squeeze clamp is applied and the output stays 0.6. squeeze_floor is a SEPARATE opt-in post-squeeze DISTANCE floor (min(floor, pre_squeeze)), inert here; it never re-applies dynamic_min_stop. The floor-binding path is covered by tests/fixtures/math_engine/squeeze_floor/."
}
