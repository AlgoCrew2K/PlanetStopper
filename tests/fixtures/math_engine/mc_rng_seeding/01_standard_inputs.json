{
  "name": "mc_rng_seeding_standard_inputs",
  "description": "Standard 50-day history used for all RNG seeding reproducibility tests. Holdings, history spec, and call params are fixed; mc_prob is NEVER hardcoded — tests assert two independent runs with the same seed agree with each other (not against a literal).",
  "function": "run_monte_carlo",
  "notes": "PA-18: expected field intentionally absent — reproducibility is asserted as run_a == run_b. Any field named 'expected' here would be a producer-computed literal and a PA-18 violation.",
  "inputs": {
    "holdings": [
      {"ticker": "AAA", "allocation": 0.6, "last_percent_change": 0.005},
      {"ticker": "BBB", "allocation": 0.4, "last_percent_change": -0.003}
    ],
    "historical_data_spec": {
      "kind": "alternating",
      "num_days": 50,
      "spy_amplitude": 0.005,
      "aaa_amplitude": 0.008,
      "bbb_amplitude": 0.004
    },
    "spy_today_return": 0.3,
    "simulation_paths": 2000,
    "neighbor_k": 10,
    "explicit_seed": 12345
  },
  "seed_a": 12345,
  "seed_b": 99999,
  "derivation": "50 days > MC_MIN_HISTORY_DAYS=20; full MC path runs. explicit_seed is arbitrary — its value is not a producer output. seed_b != seed_a to test that distinct seeds drive distinct draws."
}
