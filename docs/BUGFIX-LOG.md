# Bug-Fix Log

Chronological record of genuine code-correctness bugs fixed in this fork. Each row maps to a real commit in this repo's history. Ordered oldest → newest. Source: `git log --author="critic"`.

SHA range covered: `0a3bd20` (2026-05-22) through `006de9c` (2026-06-09).

Test-infrastructure fixes, comment corrections, and cosmetic adjustments are excluded; only defects where production or decision-math behaviour was wrong are listed.

---

| Date | Commit | Area / Module | Bug (what was wrong) | Fix (what changed) |
|------|--------|--------------|---------------------|--------------------|
| 2026-05-22 | `0a3bd20` | `database.py` / migrations | Migration 016 was missing; shadow_history composite index was not registered in `_MIGRATION_FILES` and schema applied outside the migration runner | Added `migration 016` + RM-M1 citation; migration runner now covers the index |
| 2026-05-22 | `8443c13` | `autotuner.py`, `synthetic_history.py` | Bootstrap Sortino SE used wrong denominator; frozen-eval data was not reset between cycles; VWAP helper missing; EXECUTION_START_TIME grace logic absent; `clamp` resize out of bounds | GREEN — bootstrap Sortino SE + clamp resize + frozen-eval reset + EXECUTION_START_TIME grace + VWAP helper |
| 2026-05-24 | `f17c3c9` | `app.py` (logging paths) | `account_id` and exception `str()` were emitted in log/print paths, leaking PII and internal detail | Stripped `account_id` and `str(exc)` from all client-wrapper print paths |
| 2026-05-25 | `a46c236` | `app.py` (`flush_resync`) | Fleet-dismiss and flush-resync called synchronously on the Flask thread, blocking the scheduler | Restored state-reset via background-thread dispatch |
| 2026-05-25 | `c0d527b` | `app.py` (fleet-alert dismiss) | Dismiss UI was broken after background-thread refactor — dismiss button had no effect | Restored dismiss via `_DISMISS_EXECUTOR` background-thread dispatch |
| 2026-05-25 | `312c070` | `database.py` (`advisor_ro_query`) | `advisor_ro_query` COALESCE wrap failed for tables that had no `fold_role` column, producing incorrect empty results instead of falling back | COALESCE wrap fallback for fold_role-absent tables |
| 2026-05-25 | `decf8b8` | `app.py` (`write_telemetry_row`) | SQL table name and column identifiers in `write_telemetry_row` were not validated, creating a SQL-injection surface | Validate table_name and column identifiers via allowlist (CC-002) |
| 2026-05-25 | `91f1762` | `app.py` (`_DISMISS_EXECUTOR`) | `_DISMISS_EXECUTOR` was not registered with `atexit`, so in-flight dismiss tasks were silently abandoned on daemon shutdown | Register `_DISMISS_EXECUTOR` with `atexit.register(shutdown(wait=True))` (CC-003) |
| 2026-05-26 | `2057295` | `autotuner.py` | `compute_sortino_ratio` was incorrectly called for CRRA-EU validation and frozen-eval metric paths after the M1 objective switch | Suppressed Sortino calls on CRRA-EU validation/frozen paths (M1 compatibility) |
| 2026-05-26 | `f830ea2` | `advisors/` (CVaR M2) | `compute_portfolio_cvar` computed CVaR on the full sample rather than delegating to the R-U estimator on the kNN pool | Delegated to R-U estimator on kNN pool (sp2-m2 Green) |
| 2026-05-26 | `7dd14ed` | `autotuner.py` | OOS read-path used integer spec-bundle `id` instead of the `bundle_hash` string, causing silent mismatches on theory-bundle lookup (REQ-SQLITE-1) | OOS read-path uses `bundle_hash`; `str(int_id)` breach visibility added |
| 2026-05-26 | `754f372` | `app.py` (`flush_resync`) | Log count was computed on the main thread rather than inside the background dispatch, giving a stale count under concurrency; `_FLUSH_STATE_LOCK` was absent, allowing intra-process flush races | `_FLUSH_STATE_LOCK` serializes intra-process flush; log count uses background-thread local (CC-NEW-001 + LATENT-001) |
| 2026-05-26 | `13838fa` | `autotuner.py` | Haircut-select objective used Sortino-like mean instead of CRRA-EU utility `mean(U)` (CRRA-001); `compute_n_effective` was not wired into both haircut call sites (NEFF-001); `save_autotune_run` was missing nine EUT audit columns (ARCH-001) | CRRA-EU U-transform applied in `_haircut_select`; `compute_n_effective` wired; `save_autotune_run` extended with 9 EUT columns |
| 2026-05-26 | `0b21719` | `autotuner.py` | Live call sites that construct Optuna studies were not wired to the Phase-1 THEORY spec bundle, causing bundle-ID-null rows | Wired all live call sites to `get_or_create_phase1_theory_bundle_id()` (T28/T29) |
| 2026-06-01 | `587fe2f` | `advisors/overfitting_conscience.py` | `None` `s_count` in the I-3 drift series check caused a `NoneType` crash before the comparison | Guard `None s_count` before comparison in I-3 drift check |
| 2026-06-01 | `fdfc471` | `autotuner.py` | CPCV path assignment used a non-canonical slot algorithm, producing unbalanced path assignments | Canonical first-available-slot CPCV path assignment |
| 2026-06-02 | `da8c55f` → `e1a4615` | `database.py` / migrations | `shadow_history` composite index (migration 031) was applied outside `_MIGRATION_FILES`; migration header comment wrongly read "016" | Registered as migration 031; corrected header comment (BUG-001) |
| 2026-06-02 | `769d457` + `0486402` | `autotuner.py` | Locked variables in `locked_vars` were not excluded from the Optuna search space, allowing the optimizer to override user-locked params | Enforce `locked_vars` in search space definition and in result application |
| 2026-06-02 | `a2d0207` | `static/*.js` (dashboard) | 6 blockers: JS parse error in settings modal; badge injection broken; toggle reactivity broken; a11y issue; wrong param keys; table gear icon broken (bare HTML entity failed scanner) | JS parse error fixed; badge injection fixed; toggle reactive; a11y fix; param keys corrected; gear replaced with SVG |
| 2026-06-02 | `7156deb` | `app.py` (symphony-settings) | `POST /api/symphony-settings` accepted non-boolean `live_mode` values, allowing the confirmation gate to be bypassed by sending a truthy non-bool | Reject non-bool `live_mode` (AC-3 confirm-gate bypass closure) |
| 2026-06-03 | `39cb1a1` | `app.py`, `analytics.py` | Guard Alpha dry_run path used absolute shadow cumulative `if_held + (prod_shadow - 1)*100` instead of the divergence formula `if_held + (prod_shadow - prod_current)*100`; untriggered symphonies fabricated non-zero phantom alpha | Changed to divergence formula; untriggered symphonies correctly show 0.0% Guard Alpha + 0 MDD-divergence |
| 2026-06-04 | `86ca6e0` | `app.py`, `analytics.py` | Lifetime Guard Alpha was computed as a single global product, chaining a prior position's market returns across epoch resets (forbidden) — correctness fix bundled inside a feature commit (`feat(engine): AC-1 lifetime cross-epoch Guard Alpha`) | Lifetime Guard Alpha = epoch-additive SUM; never-triggered → exactly 0 |
| 2026-06-04 | `9f4c4fe` | `app.py` (advisor routes) | `fetch_symphony_score` was called with the display name instead of the Composer hash; Composer API returned empty/404 silently | Resolve display NAME → Composer hash before calling `fetch_symphony_score` (AC-8) |
| 2026-06-04 | `dd93369` + `b21bc74` | `app.py` (advisor accept/reject) | `record_llm_suggestion` was never called on accept/reject — the `llm_suggestions` audit trail was always empty; and the call was on the render path, blocking response | Wired `record_llm_suggestion` into `/accept` and `/reject`; moved off render path (AC-5) |
| 2026-06-04 | `169fbe3` | `advisors/overfitting_conscience.py` | OC crashed silently when `s_count` was NULL, swallowing exceptions; producer-persistence failures were not surfaced | OC tolerates NULL `s_count`; producer-persistence outages surface loudly |
| 2026-06-04 | `8bfc42d` | `app.py` (advisor modal) | Advisor observations modal queried by Composer hash instead of normalized symphony name, returning empty results | Corrected hash→name resolution in `/api/advisor-observations` query |
| 2026-06-04 | `bd3cda0` | `app.py`, `analytics.py` | TWR-fallback outlier was included in portfolio VW aggregate, skewing the headline guard-alpha number | Exclude TWR-fallback outlier from portfolio VW aggregate (F4) |
| 2026-06-04 | `36f50a5` + `5d3d5cd` + `b16d7dd` | `static/index.js` (dashboard) | Hero chart read `cumulative_return` instead of `guard_alpha` directly; `renderGuardAlpha` called `fetchWindowedStrip` recursively; frozen-path had no fallback when `guard_alpha` absent | Headline reads `guard_alpha`; `fetchWindowedStrip` hoisted to IIFE scope with recursion guard; frozen-path fetches windowed strip as fallback |
| 2026-06-04 | `86b4366` | `app.py` (frozen path) | `account_all_time_cr` was absent from `portfolio_strip` on the frozen/pre_market path, causing a KeyError on the dashboard render | Expose `account_all_time_cr` in `portfolio_strip` on frozen path |
| 2026-06-05 | `8003153` | `static/index.js` (dashboard) | Cold-start account-stats renderer (`updateAccountAllTime`) was absent, leaving the account row blank until first poll cycle | Added `updateAccountAllTime` renderer; called on load |
| 2026-06-05 | `c63c6de` | `analytics.py` | `database` was not imported at module scope in `analytics.py`; `get_history_summary` raised `NameError: name 'database' is not defined` (F821) | Added module-scope `import database` |
| 2026-06-05 | `dbe3b19` + `f1e3ede` | `app.py`, `static/*.js` | Raw nginx 413 HTML was passed through to the operator result box when a backtest payload exceeded nginx's body limit | `_translate_backtest_error` converts raw 413 HTML to a clean operator message; promoted to module scope; covers both asset-swaps and logic-changes routes (AC-9c) |
| 2026-06-05 | `9c2b1e7` | `static/index.js` (dashboard) | Symphony cards did not re-partition between Active/Standby sections on poll cycle when a symphony's status changed | Auto-partition symphony cards between Active/Standby on every poll |
| 2026-06-08 | `010869d` | `advisors/` (backtest gate engine) | Backtest gate used unmatched baseline instead of fold-matched; guard-alpha floor was implicit; boolean param coercion passed Python `True`/`False` to Composer API which expects `0`/`1` | Fold-matched baseline; explicit `0.0` floor; explicit `0`/`1` numeric params (H6/H5/L1) |
| 2026-06-08 | `8eb0311` | `app.py`, `advisors/` | Advisor persistence only wrote observations for non-"reject" verdicts; persistence failures were swallowed silently (RC-4/RC-5) | Verdict-agnostic write; surface persist failure to caller |
| 2026-06-08 | `f92447d` | `app.py` (advisor routes) | Logic-changes route silently passed display name to the engine when hash resolution failed, producing empty results; DE observations were surfaced as actionable advisor output | Fail-loud name resolution on hash-miss (RC-6); DE retired from advisor surface |
| 2026-06-08 | `9fbfb4d` | `app.py` (suggest route) | `/ai-advisor/suggest` exception handler returned `str(exc)` in the JSON body, potentially leaking API keys or internal paths to the browser (D-1) | Returns `type(_exc).__name__` only |
| 2026-06-08 | `f6179c8` | `math_engine.py` | PBO-tie rank used strict `<` comparison, biasing the rank toward 0 instead of the correct pessimistic `sum(s<best)+1` | Restored `sum(s<best)+1` PBO-tie rank |
| 2026-06-08 | `4852194` | `autotuner.py` | CPCV `daily_returns` aggregation counted each path's returns multiple times per date (path-dup t-stat inflation, C2b) | Per-date aggregation deduplicates paths before computing daily returns |
| 2026-06-08 | `7f7ae75` | `autotuner.py` | `cscv_date_returns` was stored in fractional units but downstream PBO expected percent units (C1) | Store `cscv_date_returns` in decimal units at source |
| 2026-06-08 | `3607b89` | `autotuner.py`, `math_engine.py` | H3 `filtered_trials` index was misaligned after in-place filtering; PBO-tie rank was optimistic | H3 filtered-trials index-align + pessimistic PBO-tie (A4) |
| 2026-06-08 | `b636c34` | `autotuner.py` | Sentinel-mean gap detection operated at the trial level instead of the path-score level, missing sentinel detections | Path-score-level sentinel detection (sentinel-mean gap) |
| 2026-06-09 | `59dc46b` | `alpha_bot_execution.py`, `database.py` | Triggered-guard MC history was built without filtering to eligible dates (M4); `record_exit_trigger` used a hardcoded EDT offset instead of `ZoneInfo`, breaking during EST (M5) | M4: filter MC history to eligible days; M5: replace hardcoded offset with `ZoneInfo("America/New_York")` |
| 2026-06-09 | `7fbbe04` | `app.py` (suggest route) | `/ai-advisor/suggest` passed the Composer hash to `get_latest_autotune_run` (keyed by normalized name), getting `None` for every symphony | Resolve hash → normalized name before DB lookup |
| 2026-06-09 | `2039f62` | `ai_advisor.py` | `assemble_advisor_context` called `symphony_logic.get_condensed_logic` with the normalized name; Composer `/score` API requires the hash → HTTP 400 → empty logic struct for every call | Pass `composer_symphony_id` (hash) to `get_condensed_logic`; add `composer_symphony_id` parameter to `assemble_advisor_context` |
| 2026-06-09 | `25af143` | `ai_advisor.py` | `_build_optuna_section` did not guard against a non-dict `autotune_run` (e.g. a test mock); `oos_alpha` format call crashed on non-float inputs | Guard `isinstance(autotune_run, dict)`; guard `oos_alpha` format with `isinstance` check |
| 2026-06-09 | `da44f69` | `ai_advisor.py`, `app.py` | `_build_volatility_regime` returned `available: True` with all-null vol/atr fields when the schema columns were absent, fabricating regime context for Claude — correctness fix bundled inside a feature commit (`feat(advisor-informative): assessment block, honest regime, D-1 fix`) | Returns `available: False` with explicit `reason` when `symphony_vol`/`atr_pct_14d` are absent |
| 2026-06-09 | `eb40ded` | `app.py`, `static/*.js` | D1: `ai_advisor_correlations.py` import line raised `ImportError` on missing package; D2: correlation route passed a 3-tuple to the correlation engine instead of the returns list; D3: "Chat about this" did not pass artifact context via sessionStorage handoff, loading chat with no context; null guard absent in `loadRecentRuns` | D1: import guard added; D2: tuple destructure fixed (pass `_live_rets`); D3: sessionStorage handoff implemented; D4: `loadRecentRuns` null guard added |
| 2026-06-09 | `006de9c` | `app.py` (logic-changes route) | `gate_reason` derived from `gr.verdict.reason` — field does not exist on `AcceptanceVerdict`; every logic-change response raised `AttributeError` | Derive `gate_reason` from `gr.verdict.decision.replace("_", " ").title()` + `vetoes_passed` check |
| 2026-06-09 | `fcaa438` | `database.py`, `tests/conftest.py` | Advisor producer modules hold their own `import database` reference not covered by test mocks; without `DB_PATH` isolation, tests silently wrote to the production `alphabot_state.db` | `database._db_file()` raises `RuntimeError` under pytest if path resolves to `alphabot_state.db`; `conftest.pytest_configure()` sets `DB_PATH` to a temp path before collection; autouse `_isolate_db` fixture provides per-test isolation |
