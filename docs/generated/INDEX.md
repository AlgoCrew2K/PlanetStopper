# Planet Stopper -- Generated Module Reference Index

**Last regenerated:** 2026-07-11 (DE-ATLAS-SLOW-QUERY-001 + DE-ATLAS-SHARPE-FIELD-001: `advisors/community_strats` fetch restructured to a three-step, client-ranked query with no server-side sort at all (captplanet.strategies has no usable index besides _id), plus a corrected `oos_metrics['Sharpe']` field path (was reading a lowercase key present on 0/11,227 live docs); `_MAX_FETCH_DOCS` tightened 500→100→50; prior: 2026-07-03 DE-PRISM-MOMENTUM-REGISTRY-001)

All pages in this directory are auto-generated from source. Do not hand-edit generated sections. Sections marked `<!-- manual -->` are preserved across regenerations.

---

## Module Index

| Module | File | Description | Last Updated |
|--------|------|-------------|--------------|
| `advisors/lens_technicals` | [advisors_lens_technicals.md](advisors_lens_technicals.md) | Technicals lens producer: MA posture (50/200-day SMA per ticker), market breadth (fraction above SMA50), and 20-day momentum from Alpaca daily bars via synthetic_history cache; bounded retry; honest-availability; never raises; off-execution-path | 2026-06-15 |
| `advisors/lens_gdelt` | [advisors_lens_gdelt.md](advisors_lens_gdelt.md) | GDELT 2.0 tone/events producer: _fetch_gdelt_sentiment() (events-OR-tone gate, _extract_events, sourcelang:eng, _GDELT_MAX_EVENTS=7, bounded 429 retry, D-1); _GDELT_ARTLIST_URL maxrecords bumped 10->50 for news_corpus feed; _GDELT_TONE_URL + _GDELT_ARTLIST_URL reused by news_corpus via CC-2 lazy import | 2026-06-18 |
| `advisors/news_corpus` | [advisors_news_corpus.md](advisors_news_corpus.md) | Multi-source news corpus builder: build_news_corpus() -> {available, tone, corpus, reason}; Facet A = GDELT AvgTone [-1,1]; Facet B = ranked cross-source-deduped topic-tagged corpus from GDELT artlist + 8 RSS/Atom feeds (4 commercial _UA_STD + 4 .gov _UA_GOV) via feedparser; named-constant scoring (W_RECENCY=0.40, W_RELEVANCE=0.35, W_AUTHORITY=0.25, TAU_HOURS=24, TOP_K=25); three-step dedup (URL canonical + Jaccard>=0.85 + per-domain cap 3); topic-tagging (macro/fundamentals/technicals/derivatives); D-1; never raises; primary consumer of ai_advisor._build_sentiment_section | 2026-06-18 |
| `advisors/lens_options_proxy` | [advisors_lens_options_proxy.md](advisors_lens_options_proxy.md) | Derivatives/volatility lens producer: FRED VIXCLS/VXVCLS; freshness-guarded (stale obs -> available=False, reason=stale_data); rolling 90-day window; term-structure regime + risk read; D-1; never raises; off-execution-path | 2026-06-16 |
| `advisors/strategy_builder_engine` | [advisors_strategy_builder_engine.md](advisors_strategy_builder_engine.md) | Phase-2 Strategy Builder proposal engine: C4 real C1->C2->C3 pipeline; C5 community_candidate_infos adapter DELETED (147a181) — canonical path is build_plan_generator.load_atlas_candidates(objective); community_candidates= kwarg preserved; C5b: PBO veto + SPY-OAS baseline + rejection_reason; provenance tags built-new/atlas-suggested (never T1-T7, never community); sole production callers: app.py:3813 (route) + strategy_builder_scheduler.py (weekly); autotuner.py does NOT call propose_strategies; never-raises; off-execution-path | 2026-06-20 |
| `advisors/strategy_builder_scheduler` | [advisors_strategy_builder_scheduler.md](advisors_strategy_builder_scheduler.md) | Weekly Strategy Builder Scheduler (AC-18, C5 dual-mode): runs the real builder for all four objectives once per ISO week; per-objective load_atlas_candidates(objective) injection INSIDE the loop (each objective ranked by its own stat); Atlas failure -> built-new-only (D-1); same-ISO-week idempotency; MAX_ATTEMPTS=3 bounded retry; never-raises; off-execution-path; advisory-only | 2026-06-20 |
| `advisors/backtest_gate_engine` | [advisors_backtest_gate_engine.md](advisors_backtest_gate_engine.md) | M2 fold-transform + batch BHY/Yekutieli FDR gate: slices Composer backtest return series into walk-forward validation folds; C5b adds batch PBO veto (math_engine.compute_pbo over dated_returns intersection), SPY-OAS-fold baseline (spy_returns_fn seam, date-aligned), and rejection_reason precedence (pbo_veto > below_spy_alpha > fdr_not_winner); never raises; off-execution-path | 2026-06-20 |
| `advisors/universe_provider` | [advisors_universe_provider.md](advisors_universe_provider.md) | Tradeable US-equity membership provider (Strategy Builder Component 1): fetches active assets from Alpaca PAPER host, filters by ALLOWED_EXCHANGES, caches weekly via atlas_cache, persists warehouse snapshots; membership-only frozenset (no ranking); never raises; D-1 error contract | 2026-06-20 |
| `advisors/build_plan_generator` | [advisors_build_plan_generator.md](advisors_build_plan_generator.md) | Opus Build-Plan Generator (Strategy Builder Component 2+2b + C2-fix + C2-fix Revise-1 + C2-fix Revise-2 + DE-SB-GEN-TRUNCATION: MAX_OUTPUT_TOKENS=16384 + MAX_GENERATION_ATTEMPTS=3 bounded-retry): SDK tool-use generation of N_PLANS_PER_OBJECTIVE=12 diverse objective-shaped build-plans; _build_generation_prompt seam (5 layers: DSL grammar + flat if.condition + compound if_compound union + THREE compiler-verified examples (_EXAMPLE_PLAN/diversify, _EXAMPLE_IF_PLAN/flat-if, _EXAMPLE_IF_COMPOUND_PLAN/mixed-compound) + _OBJECTIVE_SIGNATURES); full Composer condition grammar (binary/binary_compound/compound) is generation-reachable and schema-constrained; tightened _EMIT_BUILD_PLANS_TOOL (kind/scheme/condition-union enum-constrained); _prune_node unknown-kind->None + zero-ticker guard; AC-8 via plan_matches_objective(); D-1 never-raises; off-execution-path | 2026-06-20 |
| `advisors/plan_tree_compiler` | [advisors_plan_tree_compiler.md](advisors_plan_tree_compiler.md) | Plan->Tree Compiler (Strategy Builder Component 3): deterministic DSL-to-Composer-tree compilation via symphony_schema constructors only (pure dispatch table); MAX_REPAIR_ATTEMPTS=3 bounded validate+tradeability repair loop; STATUS-driven 400/422 error-envelope split; in-tree ticker cross-reference for prune-target identification (Revise-1); market_cap scheme producer-deprecated (dropped pre-compilation, reason="market_cap_scheme_deprecated"; see DE-SB-MARKETCAP-DEPRECATED); backtest_fn injected seam (Component 5 wiring deferred); D-1 never-raises; advisory-only | 2026-06-20 |
| `advisors/community_strats` | [advisors_community_strats.md](advisors_community_strats.md) | Weekly-cached algo-db.com community symphonies loader (reads algo-db.com's captplanet.strategies Atlas collection): three-step client-ranked fetch, NO server-side sort (DE-ATLAS-SLOW-QUERY-001 — captplanet.strategies has no usable index besides _id, so any Mongo-side sort is an unindexed COLLSCAN; ranking runs client-side in Python via _parse_sharpe/_oos_sharpe); real Sharpe field is oos_metrics['Sharpe'] (capital S, string-valued — the prior lowercase 'sharpe' key matched 0/11,227 live docs, DE-ATLAS-SHARPE-FIELD-001); _MAX_FETCH_DOCS tightened 500→100→50 for live-Atlas timeout headroom (edn_string ~153KB/doc); validates via symphony_schema, deduplicates by SHA-256 tree-structural hash, sharpe-filters; D-1 never-raises; production callers via build_plan_generator.load_atlas_candidates(objective) (DE-SB-C5) | 2026-07-11 |
| `advisors/atlas_cache` | [advisors_atlas_cache.md](advisors_atlas_cache.md) | Weekly read-through cache for algo-db.com's captplanet MongoDB Atlas pulls: dedicated SQLite DB (alphabot_atlas_cache.db), TTL-gated upsert (default 7 days), never-raising, secrets-isolated (no MONGO_URI); upsert now uses json.dumps(..., default=str) -- defense-in-depth against stray BSON types (DE-ATLAS-CACHE-001); production callers: community_strats.py + universe_provider.py | 2026-06-21 |
| `advisors/symphony_schema` | [advisors_symphony_schema.md](advisors_symphony_schema.md) | Pure-stdlib Composer decision-tree schema: 16 constructors (incl. 6 compound AC-4..AC-8), 4 read-only inspection fns (validate_tree / lint_tree / extract_tickers (now collects binary-leaf lhs_ticker/rhs.ticker via _collect_condition_tickers) / render_rules_text), corpus-verified grammar vocabulary; iterative traversal, never-raises, deep-copy; **make_root now emits `description: ""`** + **make_inverse_vol now emits `window-days: 30`** (both live-required by Composer /backtest -- 2026-06-18) | 2026-06-18 |
| `advisors/lens_pipeline` | [advisors_lens_pipeline.md](advisors_lens_pipeline.md) | Off-hours lens pipeline (daily 03:00): 4-pass orchestrator (per-lens isolation, citation validation, configurable-model synthesis via ADVISOR_SYNTHESIS_MODEL env var (default claude-opus-4-8), MARKET_PRISM persistence); always writes one MARKET_PRISM advisor_observation per run; never raises; lazy-imported by app.py scheduler | 2026-06-17 |
| `prism_scheduler` | [prism_scheduler.md](prism_scheduler.md) | Nightly Market Prism council scheduler (Option B, daemon-decoupled): vanilla-primary headless claude -p subprocess; 5/5 orchestration directives (embed-kickoff, agentId-addressing, audit-DB wait-barrier); idempotency guard, bounded retry (MAX_ATTEMPTS=3), spend logging, MAX_BUDGET_USD=15.0; **F-4**: rc==0 AND MARKET_PRISM row confirmed; **DE-PRISM-SOURCES-001**: `_patch_provenance` appends MARKET_PRISM_SOURCES row; **DE-ADVISOR-LATENCY**: `_patch_provenance` now also captures `_lens_cache_sections` per builder + calls `ai_advisor.persist_market_lens_cache` in isolated try/except (council-safety: cache failure never prevents `return True`); **DE-PRISM-NUMERIC-VERIFY-001**: `main()` now runs one shared `_fetch_lens_sections()` patch-time fetch feeding BOTH `_patch_provenance` (via new `lens_sections=` kwarg, default-`None` behavior unchanged) AND `_run_numeric_verification` (post-council anti-fabrication numeric check, never gates `sys.exit(0)`); D-1 | 2026-07-02 |
| `advisors/prism_numeric_verifier` | [advisors_prism_numeric_verifier.md](advisors_prism_numeric_verifier.md) | Post-council anti-fabrication numeric verifier (DE-PRISM-NUMERIC-VERIFY-001): `verify_cited_numbers(run_id, market_prism_row, lens_sections=None) -> dict` recomputes every `cited_numbers` tuple the council declares against the authoritative `_build_*_section()` payload via a named indicator registry (VIX/DGS10/UNRATE/CPI/FEDFUNDS/tone/breadth/momentum-per-proxy-ticker/`<TICKER>.<CONCEPT>`), classifies pass/flagged/overridden/unverifiable (`_OVERRIDE_FACTOR=3.0`; magnitude-only, no separate sign-flip check — deliberate, see DECISIONS.md); `persist_verification` writes one idempotent append-only `MARKET_PRISM_VERIFICATION` observation (no migration); **DE-PRISM-MOMENTUM-REGISTRY-001 (2026-07-03):** registry grows 13→23 keys — 10 literal `momentum_<TICKER>_20d` entries for the technicals momentum family (SPY/QQQ/IWM/EFA/AGG/GLD/XLF/XLE/XLV/XLI), `_MOMENTUM_TOLERANCE=0.001` absolute, closing the only real-world unverifiable gap found by the 2026-07-03 post-deploy verification; D-1 never-raises; off-execution-path; advisory-only | 2026-07-03 |
| `advisors/prism_render` | [advisors_prism_render.md](advisors_prism_render.md) | Render-layer prose guard: `humanize_lens_summary(lens_name, lens_entry) -> str` detects structured JSON vs prose via json.loads result shape, applies per-lens key extraction via `_LENS_HUMANIZERS` dispatch table, degrades to `_EMPTY_STATE` sentinel; `humanize_obs_preview(raw_response) -> str` prefers `note` field, else empty-state -- no raw JSON in obs-raw-preview cell (AC-5); pure stdlib, no I/O, never raises, producer-agnostic; called by app.py:ai_advisor_tab() via in-place mutation (R1) and _preview_text stamping (R2) | 2026-06-18 |
| `advisors/asset_swap_engine` | [advisors_asset_swap_engine.md](advisors_asset_swap_engine.md) | Offline asset-swap proposal engine: objective-directed candidate generation with lens-informed ranking (Cycle-3), BHY-FDR gating, and audit-trail persistence -- advise-only, never executes | 2026-06-13 |
| `ai_advisor` | [ai_advisor.md](ai_advisor.md) | Claude-backed config advisor: context assembly, per-symphony assessment, structured-output Claude call via ADVISOR_SYNTHESIS_MODEL (default claude-opus-4-8), safety gates (7-item allowlist, risk-direction check, OOS re-validation); **DE-ADVISOR-LATENCY (2026-06-29)**: `assemble_advisor_context` serves 5 market-wide lenses from nightly MARKET_LENS_CACHE row (no per-click live fetches); new `persist_market_lens_cache(sections)` producer; `_LENS_CACHE_MAX_AGE_HOURS=36`; `build_assessment_from_context` empty-state reword; backward-compat lens aliases preserved | 2026-06-29 |
| `alpha_bot_execution` | [alpha_bot_execution.md](alpha_bot_execution.md) | Core per-cycle execution engine: fetches live Composer state, runs per-symphony exit decisions with regime-exit adjustment and per-symphony live-mode dispatch, calls autotuner post-market; **startup seed (2026-06-21):** `ensure_bot_state_seeded` + `seed_symphonies_into_bot_state` + `_SEED_RESERVED_KEYS` — idempotent off-hours bootstrap of bot_state symphony entries on daemon start; see DE-SEED-STARTUP-001 | 2026-06-21 |
| `app` | [app.md](app.md) | Flask daemon: scheduler, dashboard routes, AI Advisor SPA (6 in-place tabs); **DE-PRISM-NUMERIC-VERIFY-001 (2026-07-02):** `ai_advisor_tab()` additively fetches the MARKET_PRISM_VERIFICATION row by run_id and attaches per-check fact-check badges (`market_prism_verification` context key) to the Overview render — honest empty-state, never mutates the MARKET_PRISM row; **DE-EOD-BASIS-001 (2026-07-02):** EOD/frozen-snapshot `portfolio_strip` recompute unified onto the same per-field Tier-1(last-good)/Tier-2(honest-floor) account-basis fallback as the live path (`_account_totals_last_good`, `_account_totals_last_success_at`); **B-2 today-change account-basis (2026-06-26):** `_compute_portfolio_strip` today-change warm-cache branch wired to `analytics.get_portfolio_today_change_account_basis()` — eliminates phantom bot-vs-held divergence when no guard has fired (DE-TODAY-BASIS-001); `_symphony_value_sum` hoisted before both CR and TC blocks; **DE-PRISM-SOURCES-001 (2026-06-24):** `ai_advisor_tab()` fetches MARKET_PRISM_SOURCES row by run_id and merges article_corpus into per_lens_digest before render; **DE-PRISM-SOURCES-PER-LENS-001 (2026-06-30):** Sources section renders one carousel per non-empty prism lens (technicals/sentiment/derivatives/macro/fundamentals), each labeled; empty lenses suppressed; per-card lens tag removed; supersedes DE-SOURCES-CAROUSEL-001 flat carousel; **DE-ADVISOR-LATENCY (2026-06-29):** `ai_advisor_suggest` response JSON gains `lens_data_as_of` + `lens_data_stale` for AC-3 staleness stamp; **realtime push (2026-06-23):** `GET /api/events` SSE + `_notify_cycle_complete()` + `_StaleFlagDict` freshness mask + `data_as_of` fix (DE-SSE-PUSH-001); dashboard auth gate; guard-alpha-panel; CSRF infrastructure | 2026-07-02 |
| `autotuner` | [autotuner.md](autotuner.md) | Optuna walk-forward optimizer: 250-day window, CPCV folds (N=6, k=2, 15 splits, 5 paths), CRRA-EU objective, Harvey & Liu BHY haircut, CSCV PBO acceptance gate, NN1 spec-freeze enforcement; **calibration sweep (2026-06-19):** `run_calibration_sweep` sweeps exactly 2 params (PARABOLIC_VELOCITY_THRESHOLD + VWAP_CROSS_HWM_PCT) per symphony with Harvey-Liu BHY haircut + PBO veto surface; AC-4/5/6/7; study names `__calsweep`; advisory-only, no DB writes (see DE-CALSWEEP-001); **DE-AUTOTUNE-OOM (2026-06-29):** `_AUTOTUNE_REPLAY_N_JOBS=1` constant bounds replay n_jobs on the autotune path; passed to `generate_synthetic_history(n_jobs=_AUTOTUNE_REPLAY_N_JOBS)` — prevents OOM on 2-core / 3.0 GiB droplet (n_jobs=1 peak 2.03 GiB, 990 MB headroom) | 2026-06-29 |
| `scripts/vwap-calibration-report` | [scripts_vwap_calibration_report.md](scripts_vwap_calibration_report.md) | Advisory report renderer for the VWAP calibration sweep: reads rows from autotuner.run_calibration_sweep, renders per-symphony Markdown with PBO-veto and operator-review banners; generate_report() programmatic API + _format_markdown() + CLI (--rows-json / --out); no DB writes, no live-engine imports, stdlib only | 2026-06-19 |
| `database` | [database.md](database.md) | SQLite state management: schema, 32 migrations (001-032), all read/write accessors, pytest sentinel guard in _db_file(); **DE-PRISM-NUMERIC-VERIFY-001 (2026-07-02)**: `get_latest_market_prism_verification_for_run(run_id)` RO accessor — structural mirror of the SOURCES accessor (exact json_extract match; stale-bleed guard); no migration (MARKET_PRISM_VERIFICATION is a new advisor_role value only); **DE-PRISM-SOURCES-001**: `get_latest_market_prism_sources_for_run(run_id)` RO accessor (exact json_extract match; stale-citation-bleed guard); `update_advisor_observation_raw_response` + dead `get_latest_market_prism_sources()` removed (append-only invariant + zero callers); **DE-ADVISOR-LATENCY (2026-06-29)**: `get_latest_market_lens_cache() -> dict | None` (ORDER BY id DESC LIMIT 1 on MARKET_LENS_CACHE role; D-1 never-raises; get_ro_connection) | 2026-07-02 |
| `engine/exit_authority` | [engine_exit_authority.md](engine_exit_authority.md) | Display helpers for the exit-authority badge and restart-notice context (decision-path functions removed in Sprint 3 SITE-C1) | 2026-05-27 |
| `math_engine` | [math_engine.md](math_engine.md) | Pure risk-math primitives: trailing-stop mechanics, sqrt-time squeeze (1-sqrt(1-t)), CRRA-EU utility, CVaR diagnostics, PBO (CSCV), regime-match guard, Monte Carlo gating, VWAP signals, 6-layer exit resolver | 2026-06-02 |
| `reporting` | [reporting.md](reporting.md) | Discord webhook notifications and QuickChart-embedded EOD post-mortem generation | 2026-05-27 |
| `synthetic_history` | [synthetic_history.md](synthetic_history.md) | 250-day Alpaca historical fetcher with parallel download, file cache, and eligibility guards -- feeds the autotuner replay; **DE-AUTOTUNE-OOM (2026-06-29):** `generate_synthetic_history` gains keyword-only `n_jobs=None` param; `effective_n_jobs = n_jobs if n_jobs is not None else _resolve_replay_n_jobs()` — autotune path passes `n_jobs=1`, all other callers inherit env behavior unchanged | 2026-06-29 |
| `advisors/advisor_chat` | [advisors_advisor_chat.md](advisors_advisor_chat.md) | Explain-only chat backend (M5): scopes client artifacts to the known M1-M4 allowlist (with Cycle-1 ADD_CANDIDATE + citation fields), calls Claude (model via ADVISOR_SYNTHESIS_MODEL env var, default claude-opus-4-8) to explain a surfaced artifact, enforces hard boundary against any write/trade/config-mutation path | 2026-06-17 |
| `advisors/divergence_explainer` | [advisors_divergence_explainer.md](advisors_divergence_explainer.md) | Sprint 3 Stream B producer: surfaces two independent CVaR window values; permanently forbids signed divergence quantities | 2026-05-27 |
| `advisors/overfitting_conscience` | [advisors_overfitting_conscience.md](advisors_overfitting_conscience.md) | Sprint 3 producer: characterises overfitting risk via S-counter vs N_effective; verdicts CLEAR / WATCH / BREACH | 2026-05-27 |
| `advisors/spec_critic` | [advisors_spec_critic.md](advisors_spec_critic.md) | Sprint 3 producer: critiques Phase-1 spec bundle structural integrity (facet completeness, freeze-discipline validity, age, phase-scope leaks) | 2026-05-27 |
| `static/ai_advisor.js` | [static_ai_advisor_js.md](static_ai_advisor_js.md) | Client-side AI Advisor SPA: in-place tab switching (initTabSwitcher), suggestion card rendering with per-symphony assessment block + **AC-3 lens-cache staleness stamp** (`#advisor-lens-as-of` populated via textContent on suggest completion — DE-ADVISOR-LATENCY 2026-06-29), accept/reject lifecycle, autotune run feed, symphony selection, Strategy Builder tab functions (sbRunAnalysis, openChatWithArtifact) | 2026-06-29 |
| `static/index.js` | [static_index_js.md](static_index_js.md) | Dashboard controller: SSE `EventSource` subscription (primary `cycle-complete` update path) + 30 s poll fallback + `showConnectionLost()` staleness cue (AC-8) + guard-alpha dollar-saved panel + window-picker | 2026-06-23 |
| CI / test harness | [ci-harness.md](ci-harness.md) | GitHub Actions `tests` workflow: 3-step harness (ruff format, ruff lint, pytest); ruff==0.15.11 pinned; per-file-ignores with justifications; credential-gated test skipif; ATLAS_CACHE_DB_PATH isolation in conftest; **DE-TEST-MEMCAP-001:** conftest installs total-job Windows Job-Object cap (ALPHABOT_TEST_MEM_CAP_GB, default 24 GB) before xdist workers spawn | 2026-06-21 |
| `tests/_mem_cap` | [tests_mem_cap.md](tests_mem_cap.md) | Windows Job Object total-tree memory cap for the pytest suite: install_total_memory_cap(cap_bytes) + install_from_env() + _is_process_in_job_seam (verify-or-fail-loud seam, AC-5); KILL_ON_JOB_CLOSE kills orphaned children (AC-4); _assert_safe_worker_count in conftest rejects -n auto/-n>4 (AC-6); JOB_OBJECT_LIMIT_JOB_MEMORY total-tree; DEFAULT_CAP_GB=24.0; ALPHABOT_TEST_MEM_CAP_GB env knob; Linux/CI no-op; guard tests: 17 (9 original + 3 kill-on-close + 5 verify-or-fail-loud) | 2026-06-22 |
| `market_calendar` | *(no generated page)* | Market session state helpers -- `get_market_state`; imported by `app.py` | -- |
| `composer_backtest` | *(no generated page)* | Composer backtest client; imported by `advisors/asset_swap_engine.py` | -- |
| `regime_classifier` | *(no generated page)* | Regime classification helpers; label cache consumed by `database.save_regime_label` | -- |

---

- **Dashboard auth gate (dashboard-auth cycle, 2026-06-19):** `app.py` gains a single-password Flask signed-session gate over the entire Flask surface (AC-1..AC-13). `_auth_before_request` `@before_request` hook (registered before CSRF) enforces the gate; `_AUTH_EXEMPT_ENDPOINTS` frozenset (`login`, `logout`, `static`, `get_csrf_token`, `health`) allows pre-auth access. `_resolve_dashboard_credential()` reads `DASHBOARD_PASSWORD_HASH` (preferred) or `DASHBOARD_PASSWORD` (fallback); `_secret_key_configured()` checks `SECRET_KEY`/`FLASK_SECRET_KEY`. Fail-closed: either missing -> all requests denied. `login()` / `logout()` implement AC-4 (session.clear + authenticated flag + redirect), AC-11. In-memory throttle (`_AUTH_FAILED_ATTEMPTS`, `_check_throttle`, `_record_failed_attempt`, `_clear_failed_attempts`) enforces AC-9 (default 10 attempts / 300s lockout). `_is_api_or_xhr()` routes unauthenticated requests to 401 JSON (API/XHR) vs 302 redirect (HTML). `SESSION_COOKIE_HTTPONLY=True`, `SESSION_COOKIE_SAMESITE=Lax`, `SESSION_COOKIE_SECURE` env-gated (AC-10). Test isolation: `_auth_check_enabled` module flag + `_disable_auth_for_tests` autouse fixture in `tests/conftest.py` (mirrors `_csrf_check_enabled` pattern). `templates/login.html` is the only pre-auth visible page. See `DE-AUTH-001` in `DECISIONS.md`.
- **Guard Alpha Value Panel (guard-alpha-panel cycle, 2026-06-19):** `GET /api/guard-alpha-summary` (new, `app.py:2172`) globs `analytics._POST_MORTEMS_DIR` for `post_mortem_*.json` files, aggregates `saved_dollars` and trigger count, extracts date range from filenames, returns `{cumulative_saved_dollars, guard_event_count, date_range, basis_label}`. Dollar basis is snapshot-time (captured by `reporting.py:71` at exit) -- labeled as such, not mark-to-market. Malformed files skip + log basename only (AC-6). Honest empty-state (AC-5). Auth-gated by global `_auth_before_request` (AC-8). No DB writes; not in `_SETTINGS_WRITE_ALLOWLIST`. AC-2 dropped (pre-existing on main). **UI layer (d0fb04c):** `fetchGuardAlphaSummary()` in `static/index.js` (called once on DOMContentLoaded) fetches the route and populates `#dollar-saved-headline` / `#guard-event-count` / `#dollar-saved-basis-label` in the `data-testid="dollar-saved-panel"` section of `templates/index.html:1028-1034`. See `DE-GAP-001` and `DE-GAP-002` in `DECISIONS.md`. **Superseded (intraday fallback) -- see DE-LIVE-DASH-001-AC-1b:** The original implementation queried `position_value` from a phantom `bot_state` column; the real schema is a single-row JSON blob. Fixed in 93bd62c -- `guard_alpha_summary()` now uses `database.load_state()` for the intraday fallback position_value lookup. See `docs/generated/app.md` for the current dual-path description.
- **Strategy Builder Component 2+2b (build-plan-generator cycle, 2026-06-20):** `advisors/build_plan_generator.py` (new) provides the Opus SDK-driven build-plan generator and objective-matched Atlas admission. The build-plan DSL is the canonical generator/compiler contract (1:1 pre-image of symphony_schema constructors; `kind`/`scheme` dispatch table for Component 3 compiler). 4-value `Objective` enum (adds `volatility_mitigation`); `N_PLANS_PER_OBJECTIVE=12`; structural deduplication by json-fingerprint; AC-9 degenerate-prune guard; AC-12 admission ranked by objective stat (cut_drawdown=lowest drawdown, vol_mit=lowest vol, lift_ra=best sharpe, diversify=low cross-corr), missing-stat KEPT-LAST; AC-13 explicit provenance tags (built-new/atlas-suggested) as top-level keys on all pool items. `strategy_builder_engine.py` and `community_strats.py` unchanged in this phase. Engine rewire (_generate_candidate_trees replacement) is Component 3. See `DE-SB-GEN-001` in `DECISIONS.md`.
- **Strategy Builder C2-fix (generator prompt-steer + schema-tighten + robustness, 2026-06-20):** PM live exam (real Opus SDK call, no mocks) revealed 0 admitted plans across all 4 objectives — Opus emitted `kind:"weighted"` (drift) and `{node, weight}` children (should be `{node, pct}`) because the original prompt was a terse f-string with no grammar spec and the tool schema was a loose `items:{type:object}`. Three-part fix: (1) `_build_generation_prompt` seam — extracted prompt embeds the full kind/scheme vocabulary, WRONG-vs-CORRECT `{node,pct}` contrast, `_EXAMPLE_PLAN` (concrete conforming DSL), and `_OBJECTIVE_SIGNATURES[obj]` (per-objective structural description with explicit negative examples); (2) tightened `_EMIT_BUILD_PLANS_TOOL` schema — `kind` enum-constrained to 6 valid values (excludes `"weighted"`), `scheme` to 3 values, plan fields typed and required; (3) `_prune_node` unknown-kind now returns `None` (was pass-through), `_validate_and_prune` adds post-prune zero-ticker guard. Design principle: prompt-STEER + schema-tighten + AC-8 enforcement GUARANTEE — steer toward the correct vocabulary AND reject anything that drifts. See `DE-SB-GEN-DRIFT-FIX` in `DECISIONS.md`.
- **Strategy Builder C2-fix Revise-1 (if.condition sub-grammar, 2026-06-20):** PM live re-exam after C2-fix found 8/12 plans compiled clean but regime-gate (`if`-node) plans still dropped — Opus emitted `condition` as a STRING LABEL (e.g. `"spy_above_200d_sma"`) instead of the structured dict the C3 compiler expects. Root cause: C2-fix taught node-vocabulary (`kind`/`scheme`) but left the nested `condition` sub-grammar undescribed. Three-part extension: (1) new `if.condition` grammar section in `_build_generation_prompt` (WRONG-vs-CORRECT string-vs-dict contrast, all 5 fields listed, both `rhs` shapes); (2) `_EXAMPLE_IF_PLAN` constant — compiler-verified `if`-node plan embedded in every prompt (primary for `cut_drawdown`, supplementary for all objectives); (3) `condition` property added to `_EMIT_BUILD_PLANS_TOOL` (typed object, `lhs_fn`/`lhs_ticker`/`window`/`comparator`/`rhs` required, `comparator` enum-constrained). Known residual: `if_compound` compound-condition union is prompt-taught only — not separately schema-constrained. See `DE-SB-GEN-DRIFT-FIX-R1` in `DECISIONS.md`.
- **Strategy Builder C2-fix Revise-2 (if_compound compound-condition union CLOSED, 2026-06-20):** Revise-1 sufficiency review found that `if_compound` compound-condition gates had no worked compiling example and the compound-union (`type`/`operator`/`conditions[]`) was not schema-constrained. PM escalated to required under the operator v1 directive ("compound conditions ALL in v1"). Three-part extension: (1) `_EXAMPLE_IF_COMPOUND_PLAN` constant — compiler-verified compound-gate plan (`{type:"compound", operator:"all", conditions:[...]}`), embedded as the third worked example in every prompt; (2) compound-condition union section in `_build_generation_prompt` (type discriminator, operator, conditions[], tickers[] broadcast, rhs:{const}, WRONG-vs-CORRECT contrast); (3) `_EMIT_BUILD_PLANS_TOOL` condition property extended with union fields (`type`/`operator`/`conditions`/`tickers`/`fn` enum-constrained). The Revise-1 "known residual" is CLOSED: the full Composer condition grammar (flat if + compound if_compound, all three condition types) is now generation-reachable, prompt-taught, and schema-constrained, each with a compiler-verified worked example. See `DE-SB-GEN-DRIFT-FIX-R2` in `DECISIONS.md`.
- **Strategy Builder binary-encoding-fix (canonical-flat binary condition, 2026-06-20):** PM targeted compound-gate live probe found 2/4 compound-with-binary-leaf plans dropping with `KeyError "lhs"` — a dual binary encoding: the flat-if path + generator examples used `lhs_fn`/`lhs_ticker`/`window`/`rhs:{fixed}` but `_compile_condition`'s binary branch read nested `lhs:{fn,ticker,window}`/`rhs:{const}`. Opus emitted the flat shape it learned from `_EXAMPLE_IF_PLAN` inside compound leaves → compiler mismatch → graceful D-1 drop. Canonical-flat unification (three production files): (1) `_compile_condition` binary branch reads flat field names; (2) `_EXAMPLE_IF_COMPOUND_PLAN` updated to a mixed compound (flat-binary + binary_compound leaves — gives Opus both sub-shapes in one example); (3) `_collect_condition_tickers` extended to collect binary-leaf `lhs_ticker`/`rhs.ticker` (pre-existing `extract_tickers` blind spot for binary operands). Composer output tree byte-identical. This is the LAST grammar gap: the full Composer condition grammar (all types, both binary sub-shapes) is generation-reachable, compiler-clean, and has worked examples for every construct. See `DE-SB-BINARY-ENCODING` in `DECISIONS.md`.
- **Strategy Builder Component 3 (plan-tree-compiler cycle, 2026-06-20):** `advisors/plan_tree_compiler.py` (new) is the deterministic compiler from the Component-2 build-plan DSL to Composer `raw_value` trees. Pure dispatch table: each DSL `kind`/`scheme` maps 1:1 to one `symphony_schema` constructor (no hand-built node dicts). `compile_plan(plan, *, backtest_fn=None) -> CompileResult`: (1) `_has_market_cap` pre-check drops `scheme=="market_cap"` plans before compilation (Composer deprecated market-cap weighting — HTTP 422 `node-type-not-supported`; evidence at `tests/fixtures/strategy_builder/wt_marketcap_deprecated_envelope.json`); (2) `_compile_node` dispatches to constructors; (3) `symphony_schema.validate_tree` gates before backtest; (4) bounded repair loop (MAX_REPAIR_ATTEMPTS=3): HTTP 400 tradeability -> `_find_prune_target` cross-references in-tree tickers, prunes, validates, retries; HTTP 422 grammar -> immediate drop (no ticker prune). `backtest_fn` is an injected seam (Component 5 production wiring deferred). `CompileResult.reason` is D-1 (`type(exc).__name__` only on error). `symphony_schema` constructor count stays 16 (no `make_weight_marketcap` added — PM Option A). See `DE-SB-COMPILE-001` and `DE-SB-MARKETCAP-DEPRECATED` in `DECISIONS.md`.
- **Startup symphony seed (startup-seed-symphonies cycle, 2026-06-21):** `alpha_bot_execution.py` gains `seed_symphonies_into_bot_state(bot_state) -> int` (creates baseline entries for symphonies not yet in bot_state; mirrors DATA PHASE create-block; no shadow_history write; per-account fail-safe) + `ensure_bot_state_seeded() -> None` (idempotent presence check via `_SEED_RESERVED_KEYS`, loads + saves on first run, wrapped in try/except) + `_SEED_RESERVED_KEYS` frozenset (extends `database._WIPE_RESERVED_KEYS` with fleet_correlation_alert + last_successful_cycle_at). `app.py` calls `ensure_bot_state_seeded()` once in the `__main__` block after pidfile acquire and before scheduler thread (lazy import, noqa: PLC0415). Fixes dashboard showing 0 symphonies after off-hours DB wipe or first start. Known design limitation: two separate create-block implementations (no shared helper); tracked as future refactor in DE-SEED-STARTUP-001. See DE-SEED-STARTUP-001 in DECISIONS.md.
- **Market-lens cache-serve (DE-ADVISOR-LATENCY, 2026-06-29):** eliminates the 6-minute advisor hang caused by 17-29 sequential external API calls per `/ai-advisor/suggest` click. `ai_advisor.assemble_advisor_context` now serves the 5 market-wide lens blocks from a nightly `MARKET_LENS_CACHE` advisor_observations row instead of invoking the live `_build_*_section()` builders per request. The producer (`ai_advisor.persist_market_lens_cache`) is wired into `prism_scheduler._patch_provenance`, which already runs the 5 builders nightly — the cache adds one DB write and zero extra network calls. Cold-start (no row yet) degrades to `available=False, reason="lens_cache_unavailable"` — live builders are never the silent fallback. Staleness metadata (`lens_data_as_of`, `lens_data_stale`) flows through `app.py:ai_advisor_suggest` → `static/ai_advisor.js` → `#advisor-lens-as-of` DOM element (`class="prism-as-of"`). `MARKET_LENS_CACHE` role is excluded from `app.py _ADVISOR_ROLES` (stays out of Overview loop, exactly like `MARKET_PRISM_SOURCES`). `_LENS_CACHE_MAX_AGE_HOURS=36`. See `DE-ADVISOR-LATENCY` in `DECISIONS.md`.
- **Prism council numeric verifier (prism-numeric-verifier cycle, 2026-07-02):** `advisors/prism_numeric_verifier.py` (new) closes the last unchecked gap in the Market Prism anti-fabrication chain — the council's citation-*shape* validation (`build_citation`) and post-council *source-list* re-fetch (`_patch_provenance`, DE-PRISM-SOURCES-001) never checked that a NUMBER the council stated actually matched its source. The synthesizer + 5 analyst role files (`.claude/agents/prism-*.md`) now additionally emit every numeric indicator as a structured `{indicator, value, lens}` tuple in `cited_numbers` (AC-2 — chosen over prose regex-extraction, which is brittle and can't be bounded). `verify_cited_numbers` resolves each cited indicator's ground truth via a named registry against the SAME lens payloads `prism_scheduler._fetch_lens_sections()` already fetches once (AC-4, no duplicate external call — threaded through a new `_patch_provenance(..., lens_sections=)` kwarg, default-`None` behavior byte-identical to before), and classifies pass/flagged/overridden/unverifiable by magnitude (`_OVERRIDE_FACTOR=3.0` — deliberately NOT sign-aware, to avoid false-overriding legitimate near-zero GDELT tone drift; see DECISIONS.md for the full rationale). Result persists as one idempotent append-only `MARKET_PRISM_VERIFICATION` observation (`database.get_latest_market_prism_verification_for_run`, no migration — same zero-schema-change pattern as `MARKET_PRISM_SOURCES`). `app.py:ai_advisor_tab()` additively renders per-check badges + a "council cited X; source says Y" annotation for overrides (AC-10, honest empty-state, never mutates the MARKET_PRISM row). Residual limitation: only numbers the council DECLARES as tuples are checked — a number in prose but absent from `cited_numbers` is unverified by design (full NLP prose-extraction is OUT). See `DE-PRISM-NUMERIC-VERIFY-001` in `DECISIONS.md` and [advisors/prism_numeric_verifier](advisors_prism_numeric_verifier.md).
- **Momentum registry expansion (momentum-registry cycle, 2026-07-03):** `advisors/prism_numeric_verifier.py` gains `_MOMENTUM_TOLERANCE = 0.001` and 10 literal `_INDICATOR_REGISTRY` entries (`momentum_<TICKER>_20d` -> `("technicals", "momentum.<TICKER>", "absolute", _MOMENTUM_TOLERANCE)` for SPY/QQQ/IWM/EFA/AGG/GLD/XLF/XLE/XLV/XLI, the full `lens_technicals._PROXY_UNIVERSE`), closing the only real-world unverifiable gap the 2026-07-03 post-deploy check of the numeric verifier found (4 of 24 real council citations that day, all momentum, all unverifiable). 16-line diff; `_classify`/`_resolve_dotted_path`/`_lookup_registry_entry` untouched. See `DE-PRISM-MOMENTUM-REGISTRY-001` in `DECISIONS.md` and [advisors/prism_numeric_verifier](advisors_prism_numeric_verifier.md).

## Pruned (Sprint 3)

The following modules were deleted in Sprint 3 port-level deprecation and have no doc pages:

| Deleted Module | Reason |
|----------------|--------|
| `engine/dual_altitude` | Removed in SITE-C1 (port-level deprecation) |
| `engine/multi_cycle` | Removed in audit-fix B (port-level deprecation) |
| `port_aggregator` | Removed in audit-fix B (port-level deprecation) |
| `port_selector` | Removed in audit-fix B (`compute_composition_hash` promoted to `database.py`) |

---

## Deleted Templates

The following templates were deleted as dead code after their content was folded into the unified AI-Advisor SPA at `templates/ai_advisor.html`. Their GET routes 302-redirect to `/ai-advisor`; the templates are never rendered. Do not recreate any of them.

**Advisor-cleanup cycle (2026-06-10)** -- 4 per-tab advisor templates deleted after the 5-tab SPA migration (DE-ADV-007):
- `templates/ai_advisor_correlations.html` (deleted)
- `templates/ai_advisor_asset_swaps.html` (deleted)
- `templates/ai_advisor_logic_changes.html` (deleted)
- `templates/ai_advisor_chat.html` (deleted)

**Spa-port cycle (2026-06-13)** -- standalone Strategy Builder template deleted after fold-in as the 6th tab (DE-SPA-001):
- `templates/ai_advisor_strategy_builder.html` (deleted)

---

## Architecture Notes

- **Two-DB pattern:** State DB (`alphabot_state.db`, this module index) and Optuna optimization DB (`optuna_studies.db`) are never cross-joined in application code.
- **Advisor wall:** All Advisor DB reads go through `database.advisor_ro_query`. Direct connection access from Advisor code is prohibited and CI-enforced.
- **NN1 spec-freeze:** `autotuner.OPTUNA_SEARCH_SPACE_KEYS` must never contain `gamma`, `utility_family`, `wealth_argument`, `generator_family`, `horizon_conversion`, or `lambda`. The calibration sweep uses a separate 2-key space and does NOT modify `OPTUNA_SEARCH_SPACE_KEYS`.
- **Return units:** `synthetic_history.py` emits returns in **percent**. The CRRA-EU branch converts at its boundary via `RETURN_PCT_TO_FRACTION = 100.0`.
- **Per-symphony live mode:** `database.set_symphony_live_mode` / `get_symphony_live_mode` (migration 030) + `POST /api/symphony-settings/<name>` (CSRF-protected). Default is dry-run (0).
- **DB isolation guard:** `database._db_file()` raises `RuntimeError` under pytest if path resolves to `alphabot_state.db`. `tests/conftest.py` `pytest_configure()` hook sets a session temp path before any module import.
- **AI Advisor SPA:** All 6 advisor panels render from a single `templates/ai_advisor.html`; tab switching is in-place JS (`initTabSwitcher` in `static/ai_advisor.js`). The 4 old per-tab templates were deleted in the advisor-cleanup cycle (2026-06-10); the standalone strategy-builder template was deleted in the spa-port cycle (2026-06-13).
- **Multi-lens scaffold (Cycle-1):** Five lens blocks (`technicals`, `sentiment`, `derivatives`, `macro`, `fundamentals`) are added to the `assemble_advisor_context` output dict. Two new advisor roles (`MARKET_PRISM`, `ADD_CANDIDATE`) added to `_ADVISOR_ROLES` with `is_advisory_only=1`. See `DE-ML-001` through `DE-ML-004` in `DECISIONS.md`. The honest-availability contract and citation convention (`build_citation`) are established; producers connect real sources per cycle.
- **Technicals lens (lens-technicals cycle, 2026-06-15):** `advisors/lens_technicals.py` is the technicals producer. `ai_advisor._build_technicals_section()` (`ai_advisor.py:439-482`) replaced its Cycle-1 stub with a real implementation: lazy-imports `advisors.lens_technicals` (CC-2), derives the universe from `database.load_state()` holdings, calls `_fetch_technicals(universe)`, propagates honest-availability. Indicators: MA posture (50/200-day SMA per ticker), breadth (fraction above SMA50), 20-day momentum. No new Alpaca client -- reuses `synthetic_history.fetch_bars` (AC-5). Never raises (D-1). See `DE-TECH-001` in `DECISIONS.md`.
- **GDELT tone/events producer (lens-news-events cycle, 2026-06-18):** `advisors/lens_gdelt.py` supplies `_GDELT_TONE_URL` and `_GDELT_ARTLIST_URL` (both with `sourcelang:eng`; artlist `maxrecords` bumped 10->50). `_fetch_gdelt_sentiment()` retains its events-OR-tone availability gate, `_extract_events` helper, and D-1 contract. `_GDELT_TONE_URL` and `_GDELT_ARTLIST_URL` are reused by `advisors/news_corpus` via CC-2 lazy import. See `DE-GDELT-005` in `DECISIONS.md`.
- **Multi-source news corpus (lens-news-events cycle, 2026-06-18):** `advisors/news_corpus.py` (new) is the primary sentiment lens producer. `build_news_corpus()` produces a two-facet result: Facet A = GDELT AvgTone scalar (independent); Facet B = ranked, cross-source-deduped, topic-tagged corpus from GDELT artlist + 8 RSS/Atom feeds (Google News Business, CNBC, MarketWatch, Yahoo Finance via `_UA_STD`; Fed press, BLS, BEA, SEC 8-K via `_UA_GOV`). Scoring: `W_RECENCY=0.40 * exp(-h/TAU_HOURS=24) + W_RELEVANCE=0.35 * min(1, hits/3) + W_AUTHORITY=0.25 * domain_score`. Three-step dedup (URL canonical + Jaccard>=0.85 + per-domain cap 3). Topic-tagging (macro/fundamentals/technicals/derivatives). `feedparser>=6.0` added. `ai_advisor._build_sentiment_section` restructured to two-path: primary=`news_corpus.build_news_corpus()`, fallback/test-seam=`lens_gdelt._fetch_gdelt_sentiment([])`. See `DE-NC-001` in `DECISIONS.md`.
- **Derivatives lens (derivatives-vix-freshness cycle, 2026-06-16):** the FRED VIX/VXV producer `advisors/lens_options_proxy.py` (shipped + wired to `ai_advisor._build_derivatives_section` in #30) is fixed for data staleness. Two-part fix: (1) rolling 90-day `observation_start` window (replaces hardcoded 2020-01-01 that caused ~6-year-stale VIX); (2) freshness guard (`_OPTIONS_PROXY_MAX_STALENESS_DAYS=10`) rejects obs older than 10 calendar days as `available=False, reason="stale_data"`. `available=True` now requires both a successful fetch AND a fresh observation. Injectable `_today()` seam enables deterministic tests. See `DE-DERIV-001` in `DECISIONS.md`.
- **Fundamentals lens (fundamentals-portfolio-fanout cycle, 2026-06-16):** `ai_advisor._build_fundamentals_section()` was permanently unavailable at portfolio level -- it short-circuited to `available=False` when called without a ticker (the only production calling convention). Fix: internal fan-out over live holdings ∪ `_FUNDAMENTALS_PROXY_UNIVERSE` (8 large-cap company tickers -- ETFs excluded, they have no SEC companyfacts). `_fetch_fundamentals_for_ticker(ticker)` helper extracted; single-ticker path preserved. Per-ticker honest degradation; no invented composite ratios; bounded fan-out. See `DE-FUND-001` in `DECISIONS.md`.
- **Lens-informed swap ranking (Cycle-3):** `advisors/asset_swap_engine.py` gains `extract_lens_scores(context)` to pull per-ticker evidence from the 5 lens blocks, and `_apply_lens_blend` for position-based additive reranking (weight `LENS_BLEND_WEIGHT=0.25`). `generate_objective_directed_candidates`, `propose_operator_swap`, and `suggest_swaps` all accept `lens_scores=None` -- pre-Cycle-3 callers are byte-identical. The BHY-FDR gate is unchanged. Persisted observations now carry `lens_evidence` and `sources` in `raw_response`. See `DE-CY3-001` in `DECISIONS.md`.
- **Off-hours lens pipeline (Cycle-4):** `advisors/lens_pipeline.py` adds a daily 03:00 scheduled pipeline. `run_pipeline()` runs 4 passes (per-lens isolation, citation validation, Claude synthesis, MARKET_PRISM persistence) and always writes exactly one `MARKET_PRISM` advisor_observation per non-dry_run call, even when all lenses are unavailable. Lazy-imported by `app.py` scheduler (CC-2 import boundary). `database.get_latest_market_prism_summary()` returns the most recent row for the Cycle-5 Overview tab. See `DE-CY4-001` in `DECISIONS.md`.
- **Strategy Builder (spa-port cycle):** `STRATEGY_BUILDER` observations are prefetched by `ai_advisor_tab()` and passed as `sb_observations`/`sb_card_artifacts` to the unified template. `sbRunAnalysis` and `openChatWithArtifact` moved from the deleted standalone template's inline script into `static/ai_advisor.js`. The POST action route (`POST /ai-advisor/strategy-builder/run`) is unchanged. See `DE-SPA-001` in `DECISIONS.md`.
- **Market Prism Overview surface (Cycle-5):** `ai_advisor_tab()` prefetches `database.get_latest_market_prism_summary()` and passes it as `market_prism_summary` to the template. The Overview tab renders a `data-testid="market-prism-block"` container: sentiment chip, rationale, per-lens digest (all 5 lenses, honest-availability), cited sources as `<a href>` links with `noopener noreferrer`. Empty state ("No overnight market read yet -- the off-hours pipeline runs daily at 03:00") when `None`. Advisory-only, no trade affordances. **RF-1 update:** `app.py:ai_advisor_tab()` calls `advisors.prism_render.humanize_lens_summary()` (R1) to humanize each per-lens digest summary in-place and `advisors.prism_render.humanize_obs_preview()` (R2) to stamp `_preview_text` on non-MARKET_PRISM observations before template render -- structured JSON summaries are humanized to prose, null/empty inputs degrade to `_EMPTY_STATE`. See `DE-CY5-001` and `DE-RF1-PROSE-RENDER` in `DECISIONS.md`.
- **Atlas read cache (atlas-cache cycle):** `advisors/atlas_cache.py` adds a weekly read-through cache for captplanet MongoDB Atlas pulls. `cached_pull(collection_name, fetch_fn, *, ttl_days=7)` gates Atlas access to at most once per week per collection; cache lives in a dedicated `alphabot_atlas_cache.db` (path: `ATLAS_CACHE_DB_PATH` env). Never-raising; secrets-isolated (no MONGO_URI in this module). See `DE-ATLAS-001` in `DECISIONS.md`.
- **Community-strats loader (community-strats cycle):** `advisors/community_strats.py` routes the `captplanet.strategies` Atlas read through the weekly cache (operator bill-protection directive). Validates via `symphony_schema.validate_tree`, deduplicates by tree-structural SHA-256 hash (strip ids -> json.dumps sort_keys -> sha256; NOT `database.compute_composition_hash`), sharpe-filters keeping no-sharpe docs. Wired into production at `POST /ai-advisor/strategy-builder/run` via `community_candidate_infos` adapter (propose-wiring cycle -- see DE-PSW-001). See `DE-CS-001` in `DECISIONS.md`.
- **Community-candidate wiring (propose-wiring cycle):** `advisors/strategy_builder_engine.propose_strategies` gains a `community_candidates: list[CandidateInfo] | None = None` kwarg. The `community_candidate_infos(community_result, *, max_candidates)` adapter maps `load_community_strategies` output to `CandidateInfo` objects (`template_id="community"`, provenance in `params`). Community and template candidates are gated together in one `evaluate_candidate_batch` call -- the single-batch FDR correction is the anti-overfit invariant. `MAX_COMMUNITY_CANDIDATES_PER_RUN=20` caps injection. Per-candidate backtest failure isolation: `backtest_error` set, candidate excluded from gate, run continues. `community_candidates=None`/`[]` -> byte-identical to pre-wiring path. Rebuilt via Agent Team after solo-built rip. See `DE-PSW-001` in `DECISIONS.md`.
- **Render-layer prose guard (rf1-prose-render cycle, 2026-06-18):** `advisors/prism_render.py` (new) provides two public functions. `humanize_lens_summary(lens_name, lens_entry)` detects structured JSON vs prose via `json.loads` result shape (dict/list -> humanize via `_LENS_HUMANIZERS` dispatch; scalar/error -> passthrough); degrades to `_EMPTY_STATE` sentinel for null/missing inputs. `humanize_obs_preview(raw_response)` prefers the `note` field, else `_EMPTY_STATE` -- never dumps raw JSON (AC-5). `app.py:ai_advisor_tab()` wires both at render time: R1 mutates `per_lens_digest` summaries in-place before `render_template` (template's existing `_lens.get('summary')` reads humanized prose -- no new context key); R2 stamps `_preview_text` on each non-MARKET_PRISM observation (template renders `obs.get('_preview_text','')` for those rows, `obs.verdict` for MARKET_PRISM). Neither producer (`lens_pipeline`, Prism council) is modified. Pure stdlib, no I/O, never raises. See `DE-RF1-PROSE-RENDER` in `DECISIONS.md`.
- **Prism council 5/5 orchestration + F-4 row-verification (prism-council-5of5 cycle, 2026-06-18):** `prism_scheduler.py` `PRISM_RUN_PROMPT` updated with four directives (DE-PRISM-5OF5): (a) generate run_id before spawning, (b) embed run_id + immediate-initial_read kickoff in each analyst's spawn prompt (eliminates dormancy window), (c) capture agentIds at spawn and pass to synthesizer for agentId-addressing, (d) synthesizer must not synthesize until 5 initial_read rows confirmed in audit DB. All 5 analyst role files updated: removed dormancy-triggering "Do not proceed until you have received the run_id"; added immediate-first-turn action. `prism-synthesizer.md` Step 3 updated: analysts self-start on spawn, no kickoff messages; synthesizer addresses analysts by agentId not canonical name. Root cause (independently falsified): transient by-canonical-name resume failure of dormant subagents -- NOT analyst crash or data unavailability. **F-4 (row-verification):** `main()` now verifies a MARKET_PRISM row exists for the run_id before declaring success; rc==0-without-row is a failed attempt that retries up to MAX_ATTEMPTS and fails loudly after exhaustion -- eliminating the prior silent false-green. New seam: `_get_market_prism_row_for_run(run_id)` (D-1, never raises). See `DE-PRISM-COUNCIL` and `DE-PRISM-COUNCIL-F4` in `DECISIONS.md`.
- **Composer required-fields fix (symphony-fields cycle, 2026-06-18):** `advisors/symphony_schema.py` `make_root` now emits `"description": ""` and `make_inverse_vol` now emits `"window-days": 30` -- both fields are live-required by the Composer `POST /api/v0.1/backtest` API (HTTP 400 / HTTP 422 without them). All 13 T1-T7 candidate trees backtest HTTP 200 after the fix; the FDR gate now runs (0 survivors = intentional strict gate). `composer_backtest_client.py` and all template builders are unchanged. **Note (C4, 2026-06-20):** `_generate_candidate_trees` no longer uses the 7-template T1-T7 stamper -- the C4 body swap replaced it with the real C1->C2->C3 pipeline. The T1-T7 constructor paths in `symphony_schema.py` remain valid for direct callers but are no longer the source of built-new candidates. See `DE-SB-C4-001` in `DECISIONS.md`.
- **Calibration sweep (calibration-sweep cycle, 2026-06-19):** `autotuner.run_calibration_sweep` sweeps exactly 2 params (PARABOLIC_VELOCITY_THRESHOLD + VWAP_CROSS_HWM_PCT) per symphony, 100 trials, Harvey-Liu BHY haircut + PBO veto surface, AC-4 (<125-day skip), AC-5 (PBO veto per symphony), AC-6 (`__calsweep` study name), AC-7 (>2x trigger-freq flag). Advisory-only, no DB writes, no auto-apply. `scripts/vwap-calibration-report.py` renders operator-facing Markdown. Bleed-arm clamps and CONFIRM_TICKS remain hand-set on guardrail grounds (Kaminski & Lo 2014; Dai et al. 2021; data-sufficiency at 100-trial floor). DSR excluded (D3 category error, carried from walk-forward-overhaul). See `DE-CALSWEEP-001` in `DECISIONS.md`.
- **C5b overfitting-cull strengthening (strategy-builder-real cycle, 2026-06-20):** `advisors/backtest_gate_engine.evaluate_candidate_batch` closes two pre-C5b gaps. (1) Batch PBO wired: `math_engine.compute_pbo` is computed over the intersection of all candidates' `dated_returns` keys and threaded into every `acceptance_gate.evaluate_acceptance_gate(pbo=_batch_pbo)` call — the PBO veto is now active on the Advisor path (previously disabled via `pbo=None`). Fires only when `len(configs) >= _PBO_MIN_CONFIGS=2` AND `len(intersection_dates) >= _PBO_MIN_ALIGNED_DATES=8`; thinner batches pass `pbo=None` (no false reject). (2) Real SPY-OAS baseline: a `spy_returns_fn` seam injects SPY's date-keyed returns; SPY is date-aligned to the candidate span (intersection, not positional) then fold-transformed via the same `_fold_transform_single`, producing `_effective_default_oas_alpha`. SPY-unavailable: `_SPY_UNAVAILABLE_DEFAULT_OAS_ALPHA=float("+inf")` sentinel makes `oas_alpha <= default_oas_alpha` always-true for finite candidates → conservative WITHHOLD with `rejection_reason="below_spy_alpha"` (never silent fallback to beats-zero; edge-14 bug: original was `float("-inf")` which made clause always-false, fixed in 4ccea92). `propose_strategies` wires SPY sourcing and `BacktestCandidate.dated_returns` population internally (5d6e04a); callers do not need to supply these. (3) Per-candidate `rejection_reason` with deterministic precedence: `pbo_veto` (Stage-1, dominant) > `below_spy_alpha` (Stage-2) > `fdr_not_winner` (catch-all); survivors = `None`. Atlas community candidates receive identical treatment (same PBO + SPY + FDR); advertised `oas_metrics` never influence survival. 17 new tests GREEN at ddcbb24; 675 passed / 2 skipped / 0 failed across `tests/advisors/`. See `DE-SB-CULL-001` in `DECISIONS.md`.
- **C4 body swap + weekly scheduler (strategy-builder-real cycle, 2026-06-20):** `advisors/strategy_builder_engine._generate_candidate_trees` was the old 7-template stamper (T1-T7 via `symphony_schema` constructors). C4 replaces the entire body with the real pipeline: C1 (`universe_provider.get_tradeable_set()`, self-sourced when `universe=[]`) -> C2 (`build_plan_generator.generate_build_plans`) -> C3 (`plan_tree_compiler.compile_plan`). Built-new candidates carry `template_id="built-new"` (or `plan.provenance`), never `"T1"`-`"T7"`. `sbe.Objective` extended from 3 to 4 values (adds `volatility_mitigation`, Q1-A). Empty `universe=[]` triggers C1 self-sourcing (Q2-A); route and scheduler both pass `[]`. AC-19: zero route code change (route at `app.py:3816` was already structured for `universe=[]` + objective parsing). AC-18: new `advisors/strategy_builder_scheduler.py` with ISO-week idempotency (`_already_ran_this_week` seam), `MAX_ATTEMPTS=3` bounded retry per objective, D-1 never-raises. AC-20: `propose_strategies` public signature unchanged -- sole production callers are `app.py:3816` and the scheduler (`autotuner.py` never called `propose_strategies` -- prior doc claim was stale, corrected here). 694 tests/advisors + 713 tests/ui GREEN at HEAD a0aca12. See `DE-SB-C4-001` in `DECISIONS.md`.
- **Event-driven dashboard push (feat/dashboard-realtime-push, 2026-06-23):** `app.py` adds SSE (`GET /api/events`), `_StaleFlagDict` freshness mask for `_account_totals_cache`, and `_notify_cycle_complete()` called unconditionally from `trigger_alpha_bot()` after every engine cycle. `static/index.js` subscribes via `EventSource`; on `cycle-complete` calls `loadState()` immediately (primary path); 30 s poll retained as fallback; `showConnectionLost()` flips badge on fetch failure (AC-8). `data_as_of` in `_compute_portfolio_strip()` fixed to derive from engine cycle timestamp. See `DE-SSE-PUSH-001` in `DECISIONS.md`.
- **C5 dual-mode Atlas admission + adapter deletion (strategy-builder-real cycle, 2026-06-20):** Unifies the community-candidate admission path. (1) Route rewire (1d5dd48): `POST /ai-advisor/strategy-builder/run` replaces `load_community_strategies + community_candidate_infos` (unranked) with `build_plan_generator.load_atlas_candidates(objective)` — objective-matched (AC-12/AC-13); Atlas candidates reach `propose_strategies` tagged `template_id=atlas-suggested`; provenance surfaced in response JSON. (2) Scheduler dual-mode (147a181): `run_weekly_build` now calls `_bpg.load_atlas_candidates(objective)` per objective INSIDE the loop and injects via `community_candidates=` — the weekly run is now genuinely dual-mode (built-new + atlas-suggested); Atlas failure degrades to built-new-only (D-1). (3) Adapter deletion (147a181): `strategy_builder_engine.community_candidate_infos` (70 lines, zero production callers post-route-rewire) is deleted; canonical path is `build_plan_generator.load_atlas_candidates`. (4) AC-23 route error-boundary: `run.error` branch now surfaces static `strategy-builder-error` token, never echoes `run.error` verbatim. Tests re-pointed (db4a2bf). See `DE-SB-C5` in `DECISIONS.md`.
