f5fc010

Routes verified by tw (406/406 PASS):
- All 7 parity BLOCKs resolved across 2 GREEN commits (49ea299 + f5fc010)

Delta since v2 review (f26e29e → f5fc010):
- static/performance.js renderMetrics: emits data-testid="metric-bar" SVG per row
- static/history.js renderReasonCards: emits data-testid="reason-bar" SVG + reason-description + avg-per-exit per card
- static/ai_advisor.js renderSuggestions: emits confidence-ring SVG arc, projected-impact-bar, 4x gate-badge per card
