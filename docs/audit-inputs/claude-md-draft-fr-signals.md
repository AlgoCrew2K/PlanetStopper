# Frontrunner Signals — CLAUDE.md Key-Files Draft (DRAFT — NOT APPLIED)

**Author:** fr-doc | **Status:** DRAFT, COMPLETE as of this writing — the AC-5 wiring gap (Cluster D) and the final PM-authoritative verification are still open; this draft will need a follow-up amendment once those land (see the caveats inline). For PM/team-lead application only. This doc-writer never edits `.claude/CLAUDE.md` directly (project convention).
**Source:** `docs/generated/advisors_frontrunner_signals.md`, `advisors_frontrunner_detector.md`, `advisors_frontrunner_builder.md` (all reconciled this cycle), `DECISIONS.md` `DE-FR-SIGNALS-001`, plus the four wave-1 module doc pages (`advisors_frontrunner_acceptance.md`, `advisors_composer_draft_client.md`, unchanged this cycle) — verified against the current worktree source line-by-line, not copied from the plan.
**Branch:** `feature/frontrunner-signals` · **Worktree:** `.claude/worktrees/frontrunner-signals`

**Pre-existing gap this cycle inherited (flagged explicitly, not fixed quietly):** as of this cycle's start, `.claude/CLAUDE.md`'s Key Files table had **zero rows for any of the four wave-1 frontrunner modules** (`frontrunner_detector.py`, `frontrunner_builder.py`, `frontrunner_acceptance.py`, `composer_draft_client.py`) — verified directly via `grep -i frontrunner .claude/CLAUDE.md`, zero matches. `docs/generated/advisors_frontrunner_builder.md`'s own "Pending CLAUDE.md Key-Files amendments" section had already drafted three wave-2 amendments (`app.py`/`templates/ai_advisor.html`/`static/ai_advisor.js` rows only — routes/UI, never the module itself) back in the wave-2 cycle (2026-07-11), and even those were never applied. This draft closes both gaps in one coherent pass: it adds the two missing module rows AND carries forward the still-unapplied wave-2 amendments, rather than layering a third unapplied draft on top of an already-unmerged one.

---

## 1. NEW ROW — `advisors/frontrunner_signals.py`

**Target:** insert into `.claude/CLAUDE.md`'s `## Key Files` table. Suggested position: immediately after the `advisors/lens_warehouse.py` row (both are third-DB warehouse producers, natural neighbors) — team-lead's call on exact placement.

**New row:**

> | `advisors/frontrunner_signals.py` | Live Atlas signal ingestion (frontrunner-signals cycle, 2026-07-16, `DE-FR-SIGNALS-001`): `load_frontrunner_signals(*, force_refresh=False, db_path=None) -> dict` pulls `captplanet.frontrunners` (~3,402 docs) through a DEDICATED daily (`ttl_days=1`) `atlas_cache` key — distinct from the weekly `strategies` key used elsewhere; bounded fetch (`_bounded_fetch_fn`, 30s, DE-CS-002 SRV/DNS-hang pattern), projected (`_id`/`backtest.equity_curve` excluded); persists every fresh (non-cache-hit) pull to the warehouse third-DB (`alphabot_warehouse.db`, `frontrunner_signal_snapshots` table). `classify_fr_checks(fr_checks, signal_rows) -> list[dict]` (AC-4) joins extracted FR-checks to signal rows by exact `fr_key` AND live `comparator=="gt"` AND live `fn=="relative-strength-index"` (guards the SPY:10:30 mis-join trap and the cumulative-return fn-mismatch trap); classifies `remove`/`prune` (`FR_WEAK_SHARPE_THRESHOLD=0.38`, p10 of the 152 matched live FR-check Sharpe distribution)/`keep`/`no_edge_data`, never invented. PM-ruling extension: `persist_classification_run`/`get_latest_classifications`/`get_latest_run_marker` — a SECOND table pair (`frontrunner_classification_snapshots`, `frontrunner_run_metadata`) in the same warehouse DB; the READ side is wired and live (consumed by `app.py::ai_advisor_tab()`'s AC-7 dashboard subsection); **the WRITE side (`classify_fr_checks`/`persist_classification_run`) has ZERO production call sites as of `bf6f026b`** — fr-review's Cluster-D pass caught this pre-ship; `advisors/frontrunner_builder.py`'s background compute path is designed to call them but does not yet — see `docs/generated/advisors_frontrunner_signals.md` for the current wiring status before citing this as end-to-end functioning. `pymongo` lazy-imported (CC-2); D-1 never-raises; off-execution-path; advisory-only. |

**Verified against:** `advisors/frontrunner_signals.py` (full file read, `bf6f026b`); `docs/generated/advisors_frontrunner_signals.md` (this cycle's own doc, reconciled twice — once for the module content, once for the Cluster-D wiring-gap correction, `e90a1626`).

---

## 2. NEW ROW — Frontrunner detection/build cluster (`frontrunner_detector.py` + `frontrunner_builder.py` + `frontrunner_acceptance.py` + `composer_draft_client.py`)

**Target:** insert into `.claude/CLAUDE.md`'s `## Key Files` table. Team-lead ruling (2026-07-16): ONE cluster row for these four tightly-coupled advisory modules, not four separate rows — the table is already large, and per-module detail belongs in `docs/generated/`, not duplicated here.

**New row:**

> | `advisors/frontrunner_{detector,builder,acceptance}.py`, `advisors/composer_draft_client.py` | Frontrunner Builder cluster (PR #96 wave-1/wave-2, `DE-FRONTRUNNER-001`/`002`; AC-3/AC-6 rebuild + AC-5 additions in the frontrunner-signals cycle, `DE-FR-SIGNALS-001`). Pipeline: **detect** (`frontrunner_detector.detect_frontrunner_cascades`) → gather weekly-cached Atlas patterns → **generate** a candidate overlay via Fable (`claude-fable-5`) → compile+splice → independently re-backtest incumbent+candidate → gate (`backtest_gate_engine.evaluate_candidate_batch`, mandatory) → **Calmar acceptance** (`frontrunner_acceptance.evaluate_calmar_acceptance`, two paths: IMPROVE or PRESERVE+SIMPLIFY) → queue for operator approval; on approval, `composer_draft_client.save_symphony` creates a NEW UNDEPLOYED symphony (structurally no invest/deploy method exists anywhere in this module — adversarial source-scan enforced, `tests/security/test_frontrunner_no_trade_boundary.py`). **`frontrunner_detector.py` gained a full AC-6 rebuild this cycle:** `detect_frontrunner_cascades`'s cascade-recognition rule was replaced from a size-cliff heuristic (wave-1, matched 0/11 real trees, empirically falsified — preserved as a dated "Superseded" section, never deleted) to a direction-explicit rule (a condition's TRUE branch reaches a VIX-family ticker); new `extract_fr_checks(tree) -> list[FRCheck]` (AC-3) walks the whole tree for per-condition FR-check extraction, feeding `frontrunner_signals.classify_fr_checks`. **A genuine tree-semantics discriminator was proposed, then itself falsified by a second adversarial pass** (an `rhs-fn`-key-presence hypothesis was proposed, produced an incorrect 8→5 correction to the operator's real symphony count, then was falsified by fr-falsifier2's evidence in favor of the correct `rhs-val`-numeric rule — operator's original count vindicated) — see `docs/generated/advisors_frontrunner_detector.md`'s dedicated discriminator section for the full account, never abbreviate this to "detector fixed" alone. **`frontrunner_builder.py` gained six AC-5 signal-gating functions this cycle** (`filter_positive_edge_signal_keys`, `candidate_contains_tier1_remove_key`, `build_signal_provenance`, `resolve_signals_unavailable_marker`, `format_crossover_fr_key`/`format_vs_fr_key`, `build_classification_row_for_crossover`) — **correct and unit-tested, but with ZERO production call sites as of `bf6f026b`** (fr-review's Cluster-D finding); candidate generation is NOT yet signal-gated in production — check `docs/generated/advisors_frontrunner_builder.md`'s status caveat and `DE-FR-SIGNALS-001` before citing AC-5 as shipped. Weekly-scheduled (`strategy_builder_scheduler.run_weekly_build`) + on-demand (`POST /ai-advisor/frontrunner-builder/run`, wave-2, `app.py`). Advisory-only, never raises, off-execution-path. **Operator-gated task-zero live Composer create test still pending** — `approve_frontrunner_proposal` has only been exercised against mocked Composer responses to date. |

**Verified against:** `docs/generated/advisors_frontrunner_detector.md`, `advisors_frontrunner_builder.md` (both reconciled this cycle, `04f88d7a`/`e90a1626`), `advisors_frontrunner_acceptance.md`, `advisors_composer_draft_client.md` (unchanged this cycle, read directly to confirm no drift), `DECISIONS.md` `DE-FRONTRUNNER-001`/`002`/`DE-FR-SIGNALS-001`.

---

## 3. APPEND to the existing `app.py` row

**Target:** `.claude/CLAUDE.md` line 1 (the `app.py` row — long, multi-paragraph, already covers the AI Advisor SPA and many prior cycles).

**Append (two amendments — the never-applied wave-2 routes, carried forward, plus this cycle's read-only addition):**

> **Frontrunner Builder wave-2 routes (2026-07-11, `eb1b612` — STILL UNAPPLIED to this file as of the frontrunner-signals cycle, 2026-07-16, drafted here for the first time):** `GET /ai-advisor/frontrunner-builder` → 302 redirect (no standalone page, mirrors the strategy-builder stub); `POST /ai-advisor/frontrunner-builder/run` — async 202 dispatch to a dedicated `_FRONTRUNNER_BUILD_EXECUTOR` (single-worker, `atexit`-registered, deliberately separate from `_DISMISS_EXECUTOR`), fail-fast on missing `ANTHROPIC_API_KEY` before submit; `POST /ai-advisor/proposal/approve` — generic/source-agnostic (`proposal_id`-keyed), the ONLY route in the app that can reach `composer_draft_client.save_symphony` (exclusively via `advisors.frontrunner_builder.approve_frontrunner_proposal`); `POST /ai-advisor/proposal/reject` — status-only DB write. `ai_advisor_tab()` additively prefetches `database.get_pending_frontrunner_proposals()`, bounding `candidate_tree` to a 4000-char JSON preview before template render. See `docs/generated/app.md` §"Frontrunner Builder Routes" and `DE-FRONTRUNNER-002`.
>
> **Frontrunner-signals cycle addition (2026-07-16, `ae5fe22d`):** `ai_advisor_tab()` additively reads `advisors.frontrunner_signals.get_latest_classifications()`/`get_latest_run_marker(symphony_id=<id>)` (once per unique `symphony_id`, never bare) to render AC-7's "Live Signal Classification" subsection on the existing Frontrunner Builder tab — no live Composer/network I/O in the request thread; render-degrade logs a type-name-only message and falls back to the empty state. **This read path is wired and live; the tables it reads are currently empty in production pending the AC-5 wiring gap above.** See `docs/generated/advisors_frontrunner_signals.md` and `DE-FR-SIGNALS-001`.

**Verified against (CORRECTED after a self-check caught an error in this note's first draft):** `docs/generated/app.md` (existing, unchanged this cycle — `git log` confirms its last touch was `5b67ea09`, the R2-program merge, predating this cycle entirely). Direct grep found `docs/generated/app.md` **already documents the wave-2 Frontrunner Builder Routes section in full** (`### Frontrunner Builder Routes`, line ~608) — an earlier draft of this note wrongly claimed that section was "still absent"; it is not, and that was a real mistake in this document, caught before commit rather than left standing. The genuine, still-real gap is narrower than first claimed: **(1)** the top-level `.claude/CLAUDE.md` Key-Files table itself has zero frontrunner rows — independently confirmed via `grep -i frontrunner .claude/CLAUDE.md`, zero matches, which IS this draft's actual target; **(2)** `docs/generated/app.md` does NOT yet mention this cycle's AC-7 addition at all — confirmed via grep for `frontrunner_signals`/`get_latest_classifications`/`get_latest_run_marker`, zero matches — so `app.md` itself has a real doc gap for the frontrunner-signals cycle's `app.py` change, outside this cycle's originally-approved doc scope (`signals.md`/`builder.md`/`detector.md`/`INDEX.md`); flagged to the team lead as a follow-up rather than silently expanding scope here. Also verified: `advisors_frontrunner_builder.md`'s own "Pending CLAUDE.md Key-Files amendments" section (source of the wave-2 text, carried forward verbatim where still accurate); `app.py` source read directly for the AC-7 prefetch call site.

---

## 4. APPEND to the existing `templates/ai_advisor.html` row

**Target:** `.claude/CLAUDE.md` line 43.

**Append (wave-2, never applied, carried forward):**

> **Frontrunner Builder tab (2026-07-11, `eb1b612` — STILL UNAPPLIED to this file, drafted here first):** 7th tab panel (`tab-panel-frontrunner-builder`), following the same in-place-tab pattern as Strategy Builder. Persistent non-dismissible risk banner (the one tab where an operator action — Approve — creates a real Composer symphony). Pending-approval cards render incumbent-vs-candidate Calmar/CAGR/MDD deltas + node-count delta (columns conditionally shown via `is_fr = p.proposal_source == 'frontrunner_builder'`); a collapsible raw-candidate-preview `<details>` block (never raw JSON, no `| safe`). Approve/Reject buttons call `frApprove`/`frReject` (JS). **Frontrunner-signals cycle addition (2026-07-16):** same panel gains a read-only "Live Signal Classification" subsection (AC-7) — per-symphony cards, `fr_key → live RSI → edge stats → keep/prune/remove/no_edge_data` chip; render path wired and live, currently empty pending the AC-5 wiring gap (see the `app.py` row amendment above).

**Verified against:** `docs/generated/advisors_frontrunner_builder.md`'s "Wave-2 UI" section + this cycle's addition to it; `templates/ai_advisor.html` source not independently re-read line-by-line for this append (relying on the already-reconciled `docs/generated/` page, itself verified against source at `bf6f026b`/`ae5fe22d`) — flagged so the PM can spot-check the template directly if desired before applying.

---

## 5. APPEND to the existing `static/ai_advisor.js` row

**Target:** `.claude/CLAUDE.md` line 44.

**Append (wave-2 only — this cycle added no new JS; AC-7's render is server-side Jinja, confirmed via the `ae5fe22d` commit message: "No new JS, no new write endpoints"):**

> **Frontrunner Builder tab functions (2026-07-11, `eb1b612` — STILL UNAPPLIED to this file, drafted here first):** `frRunBuild()` — on-demand build trigger, POSTs to `/ai-advisor/frontrunner-builder/run`, does NOT auto-navigate (route returns 202 before results exist); shows a "reload later" status message. `frDispatchProposalAction(action, proposalId)` — shared approve/reject dispatch; disables both card buttons + dims the card during the request (prevents double-submit). `frApprove`/`frReject` — thin `window`-exposed wrappers for Jinja `onclick` handlers.

**Verified against:** `docs/generated/advisors_frontrunner_builder.md`'s "Wave-2 UI" section; commit `ae5fe22d`'s message explicitly confirming no new JS this cycle (checked directly, not assumed).

---

## Summary for the PM

Five edits total: 2 new rows (frontrunner_signals.py standalone; the detector/builder/acceptance/draft_client cluster), 3 appends (app.py, templates/ai_advisor.html, static/ai_advisor.js — all three carrying forward a wave-2 amendment that has sat unapplied since 2026-07-11, plus this cycle's own additions layered on top where real). **Every AC-5/signal-gating claim in this draft explicitly states the Cluster-D wiring gap** — nothing here describes AC-5 as functioning end-to-end, matching the corrected state of `docs/generated/advisors_frontrunner_builder.md` and `advisors_frontrunner_signals.md` (`e90a1626`) and `DECISIONS.md`'s `DE-FR-SIGNALS-001`. **This draft will need one more pass once Cluster D wiring lands** — the two AC-5-related rows (frontrunner_signals.py, the cluster row) should have their wiring-gap caveats removed and replaced with the landed reality at that point; do not apply this draft as a permanent record if the wiring caveats are still accurate by then — re-verify against the live source before pasting.

**Additional follow-up found while drafting this document, and CLOSED before this draft was committed (not silently left open):** `docs/generated/app.md` did not yet document this cycle's `app.py` change (the AC-7 `get_latest_classifications`/`get_latest_run_marker` prefetch in `ai_advisor_tab()`) — confirmed via direct grep, zero matches for any frontrunner-signals-related term in that file at the time this document was first drafted. Fixed directly: a new paragraph was inserted into `docs/generated/app.md`'s existing `### ai_advisor_tab() — Frontrunner Builder panel prefetch` section documenting the AC-7 addition, with the same wiring-gap caveat carried through. Both files are committed together.
